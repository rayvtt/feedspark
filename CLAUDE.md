# FeedSpark — Claude Code Project

## Who is Ray

Ray works at FeedSpark, a feed optimisation agency within the Dentsu network. Senior client-facing role spanning account management, commercial strategy, and business development. Manages enterprise-level product feed programmes and builds client-facing proposals, retention materials, and strategy decks. Positions FeedSpark's proprietary tooling (Tachyon AI, FeedHero) in competitive situations.

Ray's communication style is terse and iterative — short corrections with an expectation that Claude infers the full implication and rebuilds accordingly. He frames outputs at "Chief of Pricing / Sales Director" standard, expecting client-ready deliverables rather than rough drafts.

---

## FeedSpark service framework

Core: Google Shopping feed optimisation across titles, descriptions, attributes, product highlights, and structured data. AI capabilities (Tachyon AI) as a key differentiator.

Adjacent services: DPA creative, scraping, PPC overlays, Meta Commerce Manager feed architecture, custom labels, image cycling.

Proprietary tools:
- **Tachyon AI** — LLM-powered intent generation, description enrichment, visual attribute harvest from product imagery
- **FeedHero** — Feed management platform for rules, alerts, custom labels, keyword injection, A/B testing

---

## Active client accounts

### Schuh
- UK footwear retailer, EN/DE markets
- AI Readiness Scorecard delivered (10-slide deck)
- Converse Chuck Taylor as worked example product
- Onboarding deck delivered (12 slides, 46 total hours across UK/IE/DE)
- Google Sheets project plan: `1rbr8FwZagdZdctR_fNesixm4-uDWG1VJPnBCJBdjSxc`
- Team: schuh@feedspark.com, Ray, Steven, Venki, Dino, Mike, Gary, Adriana, Mo, Isa, Matt, Will

### Estée Lauder Companies (ELC)
- Multi-brand luxury beauty portfolio, 14+ brands across 5 fiscal cycles
- Account at risk — retention work completed (defence deck, parting gift web app)
- 30+ feed issues documented across Technical/Optimisation/Account Management
- Google's conversational AI attributes positioned as differentiator
- Google Sheets project plan: `1KWrB4IpHGRUnlhVjWP4hpyhpBGs5c-JM_cBa7mj6J0Y` (gid=1574286896)
- Contacts: Jessica Olivia, Cox Tara, Alexandra Perez, Carman Wong, Aysia Bailey
- Pricing: self-managed £498/pcm per brand; DPA/PPC overlay £300/pcm base

### Monsoon / Accessorize
- Two brands under one account
- Monsoon: 12,314 SKUs, 32hr retainer
- Accessorize: 5,414 SKUs, 27hr retainer
- Ratecard: £585+VAT per 8-hour block (£73.125/hr)
- AI Optimisation pricing tool built (HTML web app with retainer comparison, per-cohort AI tier assignment, task library)
- Dress overlay debugging: image_link URL rotation wiping image type tags
- Deployment target: Cloudflare Pages + Access (confidential commercial data)

### YuMOVE (Lintbells)
- Pet supplements, UK market
- Google Shopping + Meta, 2 active channels
- 24hrs scheduled + 21hrs ad-hoc per month
- Strategy Review Jul 2026 deck — delivered, **client-approved (raving reviews)**; now the **master Strategy Review deck template** (clone per client). Live at `/deck/yumove`.
- Key tests completed: brand inclusion wins, benefit copy +24.7%, "Multivitamins" +126.62%, health conditions +30%
- POC scoping: test SKUs, timeline, success metrics, sign-off flow
- Google Sheets project plan: `1RMTN99Cw0J3l5mORwYPpITnoi5HCPt7tET4u8rQbsq0` (gid=841484251)
- Contacts: Simon (YuMOVE), Becca (bundles), Kinase (agency)

### Reiss
- UK fashion retailer, 29 markets, 60 feeds
- Q2→Q3 2026 Strategy Review delivered (23-slide deck)
- AI Readiness: Tier 1 (titles & data fields), climbing to Tier 2
- Golden Record Scorecard: 88.6% attribute completeness
- Tachyon pipeline live: intent generation → Shopping Graph matching → roundel overlay
- Local Language case study: 16 EU/ME markets, LL vs EN Shopping campaigns

### Superdry
- Service review deck (SharePoint): `ms-powerpoint:ofe|u|https://aroxo-my.sharepoint.com/personal/ray_aroxo_onmicrosoft_com/Documents/Superdry%20X%20FeedSpark%20Service%20Review%20V2.pptx`

---

## Design system (all FeedSpark materials)

Source: Reiss–Dentsu introduction PDF (Mar 2026). This is the governing design reference — only use elements, colours, and fonts from this PDF.

- **Colours:** Orange `#F5A623` (primary), deep orange `#ED6F0B`, charcoal `#333333` (body text), white `#FFFFFF` backgrounds, light grey `#F5F5F5` / `#F7F7F5` card backgrounds
- **Typography:** Lato (Google Fonts)
- **Cards:** White with `#E6E6E6` borders and drop shadows
- **Footer:** "FeedSpark · Private & Confidential" left-aligned, page number right-aligned
- **Prohibited:** No FeedSpark logo, no page number circles, no random decorative lines in slide bodies. Orange accents only where purposeful. Clean white backgrounds throughout.
- **Client decks (.pptx)** follow the **core deck template** instead:
  `reference-files/deck-templates/FeedSpark_Core_Deck_Template.pptx` (Inter; slate `#0F172A`;
  orange `#F7941E`; 18 named layouts) — see "Client decks" below.

---

## Build pipelines

### PPTX (pptxgenjs)
```bash
export NODE_PATH=$(npm root -g)
node build.js
python /path/to/rezip.py output.pptx
python /path/to/soffice.py --headless --convert-to pdf output.pptx
pdftoppm -jpeg -r 130 output.pdf slide  # full deck QA
pdftoppm -jpeg -r 130 -f 4 -l 4 output.pdf slide  # single slide QA
```

> ⚠️ **In Claude Code (web/cloud), LibreOffice (`soffice`) and `pdftoppm` are UNAVAILABLE** — the
> pipeline above only runs where those binaries exist. In Code, build decks with **`python-pptx`**
> and QA them with the bundled Pillow previewer (renders `.pptx` → PNG, flags text overflow):
> ```bash
> python tools/preview_tmpl.py deck.pptx /tmp/qa   # -> /tmp/qa_1.png, _2.png, ...
> ```
> Full pipeline + gotchas: [`tools/README.md`](./tools/README.md).

Key gotchas:
- Shadow objects need a factory function (`makeShadow`) to avoid mutation
- Smart quotes in XML stored as hex entities (`&#x201C;` / `&#x201D;`) — use encoded form in replacements
- Icons: `react-icons/fa` rasterised to PNG via `sharp` + `ReactDOMServer`, passed as base64 data URIs
- `pres.layout` must be set before adding slides (default is 10" × 5.625")
- Hex colors: never `#`, never 8 digits

### PPTX (XML editing)
```bash
python unpack.py input.pptx unpacked/
# Edit XML files
python pack.py unpacked/ output.pptx
```
- Slide order: controlled by `<p:sldIdLst>` in `ppt/presentation.xml` (not filename)
- Inserting a slide requires: new XML + `[Content_Types].xml` Override + `ppt/_rels/presentation.xml.rels` Relationship + `<p:sldId>` entry
- Source files with `.pdf` extension may be zip-packaged exports — use `unzip` not PDF parsers

### Client decks — .pptx ONLY (Aug 2026)
**Every new client deck ships as PowerPoint (`.pptx`) — HTML is no longer an output option**
(Ray's standing rule). Builds run through the `/deck-generator` skill: sections are authored
with the HTML component library as an internal intermediate, then exported via
`tools/deck_to_pptx.py --audit` onto the machine template `tools/templates/feedspark_deck.pptx`.
Design/element/colour/text/voice are governed by the **core deck template**
`reference-files/deck-templates/FeedSpark_Core_Deck_Template.pptx` (the Ray-approved Superdry
Strategy Review 2024–2026); Ray deposits further reference `.pptx` files (per deck type or per
client) into `reference-files/deck-templates/` — read that folder before any deck build, and
mirror new deposits into the Deck Generator module's `TEMPLATES` panel
(`docs/FeedSpark_DeckBuilder.html`). New decks are NOT wired into the worker's `/deck/` pages.

### HTML decks (legacy live decks only — no new ones)
Decks already live before the pptx-only rule (e.g. `/deck/yumove`) keep the inline edit +
JSON patch sync system:
- Edit mode toggle, export/import edits, download clean HTML, data-check flags
- See `docs/FeedSpark_Deck_LiveEdit_Feature.md` for full spec
- **Parallel editing** (Ray edits copy live; Claude Code edits structure): `docs/WAYS_OF_WORKING.md`.
  Merge exported edit patches onto a template with `tools/apply_edits.py`; live host = `cloudflare/feedspark-deck/`
- Cloudflare Pages + Access for confidential commercial data (not public GitHub Pages)

### Google Drive
- Use file ID (alphanumeric string between `/d/` and `/edit`) for API calls
- Read with `Google Drive:read_file_content` using the raw file ID

---

## Email drafting rules

- Always offer two tone variants: confident/direct vs collaborative/consultative
- Flag technical distinctions proactively before drafting (e.g. conversational attributes vs GPC mapping are separate concepts)
- Source info from authoritative platform docs (Meta Business Help Center, Google Merchant Center) — not from memory
- Defer to Ray on commercial variables (POC fees, pricing) — flag items for him to confirm, don't invent figures

---

## Cloudflare infrastructure

### FeedSpark-specific (do NOT use NAC resources)
- KV namespace: `FEEDSPARK_DECK_EDITS` (id: `d93b5ac576c74f0d8a315c5b92dc8e16`)
- Worker: `feedspark` (dir `cloudflare/feedspark-deck/`) at `feedspark.ray-vtt.workers.dev` — the command center (landing hub at `/` + strategy decks at `/deck/<slug>`) with live-edit KV persistence
- Requires Cloudflare Access gating for commercial data

### ⚠️ Access architecture (Jul 2026 — affects EVERY session's live checks)
- The Worker URL is **Public at the Workers layer** (Settings → Domains & Routes). Auth lives in
  **Zero Trust**: a site-wide Access app (Allow) + ONE path-scoped **Bypass** app for
  `/api/gmail/push` (key-gated — the Apps Script can't log in through Access).
- **NEVER flip the Workers toggle back to "Restricted"** — it intercepts BEFORE Zero Trust, which
  silently kills the Gmail-push bypass (the script then gets the login page as an HTTP 200).
- Consequence for sessions/CI: **any unauthenticated HTTP request to ANY page/API returns the
  Access LOGIN PAGE with HTTP 200 text/html.** A 200 — or scraped HTML — is NEVER proof a deploy
  is live and never real page content. Verify-live = green Deploy Action + Cloudflare MCP
  `workers_get_worker_code` bundle grep (+ `/api/version` by a logged-in human). Playwright/curl
  against the live host from a session will hit the login page — test pages via `file://` with
  stubbed fetches instead.

### Worker API (multi-page command center)
```
GET  /                          → command center landing page (git-bundled + injected editor + Tachyon)
GET  /workflow                  → Workflow control center (brief pipeline: Client→AM→ASPL). ONE MODAL AT A TIME (Ray, 17 Sep 2026: "also, what happened to brief ledger view" — the ledger was intact; what he saw was two overlays at once): the task-edit pop-up must clear Focus mode's full-screen overlay so .tle-scrim sits at z-index 9500 while every other scrim is 200, and the 460px edit card painted straight across the middle of the open ticket; the equal-z pairs stack just as badly and less obviously (later in the DOM wins, backdrop dims twice). soloModal(keep) SWEEPS .scrim.on rather than naming the overlays it knows, so a scrim added later is covered without touching it; all six openers route through it (ticket, task editor, composer, access panel, cockpit tuner — the tuner is in its own <script> so it goes via the window.FCCSolo bridge). ONE EXCEPTION: closeBrief() drops the draft's email context and unpins the client, so auto-closing the COMPOSER would silently destroy a half-written brief — a real loss where the stacking it prevents is only cosmetic; it is never closed for someone else, though opening it does clear the cheap views behind it. AND A MODAL NEVER LANDS ON AN OPEN RAIL (Ray, 18 Sep 2026, opening a brief ticket with the Playbook up: "workflow still have this issue when clickign on brief ticket" — a screenshot of two cards, the right one sliced down its left edge): same picture, different cause, which is why the guard above did not catch it — not two overlays but ONE, because .scrim is position:fixed;inset:0 and centres its modal on the VIEWPORT while the page itself is padded for the rail (body.ck-l-on / ck-r-on), so the centre it aimed at was under the rail and the ticket's right edge crossed the rail's left edge on any window under ~1400px (a 1280px laptop clipped 36px — enough to cut "Accessorize" to "essorize" and "NOT ON THIS ACCOUNT" to "ON THIS ACCOUNT", which reads as a broken panel rather than a covered one). Only the CENTRING moves: the scrim mirrors the body's own padding (calc(var(--ck-w) + 16px), one rule per rail off the same variable so the two can never disagree), the dim stays full-bleed and the rail stays inert behind it. The three modals sized in vw (composer 1460/96vw, access panel, cockpit tuner) now measure 100% of their scrim's column — a vw width is read against the VIEWPORT, so it would step straight back out of the space reserved for the rails while the padding rule still read correct. It stands down at 900px BY DECISION: below that a rail stops being a dock the page is padded for and becomes an overlay sheet, and reserving 420px of an 880px window would leave the ticket a 428px card — a covered rail traded for an unreadable modal. Harness tools/test_modalsolo.mjs (29 assertions — fails any opener that skips the guard, names the wrong overlay to keep, or calls the closure from another script block; then RENDERS the page with the rail up at 1100/1280/1340/1500px and fails if the ticket modal's box intersects the rail's, which is the half that fails on the pre-fix page at every width but 1500) in qa_gate/presync/validate
GET  /leadership                → Ray-only dashboard (OWNER_EMAIL-gated like /activity): book health, commercial burn-down, retention radar + hub for the modules folded under it — /leadership/readiness, /leadership/library, /leadership/roadmap (same gate; legacy /readiness /library /roadmap 301 here)
GET  /deck-builder              → Deck Generator module (brief output = .pptx build on the core deck template; shows the reference-files/deck-templates/ library); WORK-VOLUME CHART WORKBENCH (Ray, 15 Sep 2026: 'a chart generator for any kind of work-volumes type analysis … Reiss for the past six months … project plans, emails, calls, and scheduled work … customised, edited, personalised for AMs'): GET /api/volumes?client=&months=(1–24)&end=YYYY-MM — one brand, one window, SIX STREAMS bucketed per month by src/volumes.js (pure; harness tools/test_volumes.mjs in qa_gate/presync/validate): plan tasks (cron-warmed planlive:<sheet>; cold → the page falls back to the baked PLANTASKS monthly volume and says so) with dims category/status/owner, captured emails (KV gmailinbox + triage decisions; rolling 120 since Aug 2026) decision/kind, call actions (callactions) meeting/owner, briefs (KV briefs) stage/category/source, scheduled work (the shared scheduleTabs loader → schedwork rows for the brand) task/decision/market + HOUR SUMS offered/delivered/skipped, result rounds (kwresults) market/verdict; every record is dated by monthKey (epoch ms, ISO, DD/MM/YYYY, 'Jul 25' section labels, Sheets serials, RFC-2822) or counted as undated — never guessed; scoped per signin. Page section #vz on /deck-builder: brand (follows the deck client until overridden) · 3/6/12-month window · view (volume by stream, or any stream × dim) · form (stacked columns / lines / 100% stacked / horizontal totals) · value labels + table toggles · series chips + legend hide/show · editable title/subtitle (contenteditable) + notes · hover tooltip; palette = the dataviz skill's validated six-slot categorical set (light + dark steps, breakdowns fold past 7 into Other); 📌 PIN TO BUILD BRIEF stores a data snapshot (localStorage fcc-dg-charts) — brief() appends '## Work-volume charts' with a markdown table per pin so the /deck-generator skill builds NATIVE pptx charts, and the in-browser preview gains one slide per pin (__dgChartSlides); ⬇ PNG (SVG→canvas, 2000×980, footer stamped), ⎘ Copy table (TSV), 🔗 Copy link (?chart=<base64url spec> deep link), 💾 named presets per device. Docs: docs/DECK_CHARTS.md
GET  /activity                  → user activity log + Build Log tab (OWNER-only: gated to OWNER_EMAIL via Cloudflare Access identity); /buildlog 301s here
GET  /api/activity?days=N       → activity feed (owner-only 403 otherwise); all API mutations + page views are logged per Access user
POST /api/gmail/push            → Gmail→FCC sync (no-admin path): Apps Script in Ray's mailbox pushes (a) brief replies — briefmatch.js moves ticket stages, TOKEN-ONLY matching (ibfcode/brief-id, never fuzzy unattended) — AND REBUILDS A MISSING TICKET FROM ITS OWN BRIEF EMAIL (Ray, 16 Sep 2026: "when Steven starts briefing using the FCC module, but also includes me in the brief email so I receive a copy — would you be able to scan that and log it back in the FCC from Steven's work?"): the Apps Script query already covered it (`newer_than:7d ("ibfcode:" OR subject:"[FS Brief]")`), so every [FS Brief] Ray is copied on already reached the worker — and was dropped twice, once because the reply matcher skips our own outgoing mail and again because findBrief() has nothing to match when the ticket was never saved. bodyOf() writes a fixed machine-readable block, so briefFromEmail() rebuilds the whole ticket (id from ibfref, client from the body's Client: line since a slug can't be un-slugged, market from the ibfcode's last segment, TASK / SCOPE / DEFINITION OF DONE / ASSETS sections, ibfdue → due, Priority, `by` from the sender) and recoverBriefsFromEmail() files it at stage `briefed` — the email going out IS the act of briefing — stamped rec:'email'. CREATE-ONLY and idempotent: a ticket already in the store is NEVER touched, so an old email can't reset one that has moved to analysis; a reply/forward is a status update not a brief; an absent or ambiguous ibfref is never guessed. Both push lanes (live + rescan) run it; the pushed snippet is 4000 chars (1200 cut off the DoD and assets). AND THE BRIEF THAT STILL WENT MISSING (Ray, 23 Sep 2026, on SUPE-20260908-01, Steven's Everest Jackets brief with Ray copied: "This brief sent by Steven using FCC is not in my brief ledger. Why? every brief sent out by anyone using FCC have to be documented in my view brief ledger"): the save-durability work of 16 Sep only reaches FORWARD, so a brief raised before it whose fire-and-forget PUT failed lives only in the browser that raised it — and THREE further links each lost it on their own. (1) gmail_push.gs searches THREADS, so vimalesh's read-out today pulled the whole thread back including Steven's original, the one message carrying the machine-readable block — and MAX_AGE_MS then threw it away PER MESSAGE at 7 days, leaving only the reply, which briefFromEmail refuses by design (a reply is a status update, never a brief): the ticket was in our hands and discarded. isBriefOriginal(subject) mirrors briefFromEmail's own gate and a brief ORIGINAL now rides its thread's liveness (BRIEF_MAX_AGE_MS 180d, bounded only so a revived ancient thread can't push a huge body — recovery is create-only and mids are deduped, so a re-send costs payload and nothing else) while every REPLY keeps the 7-day window that stops ancient status updates re-applying. (2) BOTH worker lanes ran matchGmailToBriefs BEFORE recoverBriefsFromEmail, so a batch carrying a brief AND its replies — which is exactly what a live thread pushes — matched those replies against a store that did not yet hold the ticket; they were dropped, the ticket was created at `briefed` with no comms and no read-out, and server-side mid dedupe meant they never came back. Recovery now runs FIRST on both, so a rebuilt ticket takes its real stage and read-out from its own thread. (3) Nothing above reaches BACKWARDS, so backfillBriefs() is the path that does: a resumable sweep of subject:"[FS Brief]" from a dated floor (BRIEF_BACKFILL_FROM, 25 threads a page, 4-minute budget, cursor saved to script properties after EVERY page so a timeout never restarts the year, cleared on COMPLETE), pushing each thread's original AND its replies together — the live lane now returns `rebuilt` so each page logs what it recovered. Harness tools/test_briefrecover.mjs (41 assertions — the REAL subject + body shape, plus the Superdry thread as it actually is: isBriefOriginal lifted from the Apps Script BY NAME, the 15-day original clearing the gate its thread already passed, recover-then-match carrying the read-out, and the PRE-FIX order recorded verbatim rebuilding the same ticket with zero comms; 8 fail on the pre-fix code) in qa_gate/presync/validate — and (b) the inbox capture: every email to ray@feedspark.com NOT from @feedspark.com/@aroxo.com/@feedhero.net, classified (client via detectClient cue ladder: dossier dom → sender-domain label → display name → brand mention; ⚡ briefable score) — and (c) Gemini/Meet call-notes emails ("Notes by Gemini"/transcripts, pushed with a 9000-char snippet): parseGeminiNotes extracts the meeting title + action items (Suggested-next-steps section, "<Name> to/will …" owner extraction) into KV callactions (mid-deduped, ≤300) — Ray's carve-out from the emails-are-never-auto-tasks rule; the notes email never enters the triage queue. Auth = GMAIL_PUSH_KEY secret + Access bypass on this exact path (GOOGLE_SETUP.md §8)
A RESULT CAN ALSO ARRIVE AS A REPLY ON THE BRIEF THREAD (Ray, 21 Sep 2026: Vimalesh answered the Silk brief REIS-20260811-02 with "Impressions: 4.63% Increase / Clicks: 5.18% Increase" — "can you update the result directly in Keywords Calendar and Intake also?"): that lane already existed (extractResult on the Gmail push, extractResultTxt on the page's update router, filed as a `Result:` comm), but it read the metric only from the prose forms "N% uplift in X" / "uplift of N% in X". The ASPL reply puts the metric BEFORE the figure in a bullet list, and worse, the RESULT_RE prose grab ran FIRST and short-circuited on the heading "The optimised SKUs results:", returning the single line after it — so the read-out reported its impressions and silently DROPPED its clicks. Figures are collected first now (collectFigures: the label form "<Metric>: N% Increase" + both "in X" forms, deduped on sign+number so a figure seen twice reports once WITH its label), prose is the fallback for a read-out carrying no figure at all, and the page mirror is kept in step — harness tools/test_readout.mjs runs ONE corpus (the real Silk specimen, as sent and as Gmail collapses it) through BOTH copies, in qa_gate/presync/validate. AND THE CALENDAR READS IT: /api/kwresults ties a round to every moment whose live-by date falls in its fortnight, which is right but approximate; a read-out filed on the moment's OWN ticket is that optimisation and nothing else, so KWCal's ticketRes (via the wfFor tie) wins outright over roundRes and the popover drops the batch caveat for it. Dino's own archive: GET /api/kwresults?client=      → scheduled keyword-optimisation RESULTS archive (Ray's ask 27 Aug): Dino Kumar's daily result updates to clients (subject '<Brand> <MKT> x Feedspark - <Mon I/II> - Keyword Optimisation') are captured by a third gmail_push.gs search (internal-sender carve-out — the inbox capture excludes @feedspark.com, these come FROM the team; long snippet like call notes), parsed at push time by parseKwResult (brand/market/period + best-effort %-metric lines + raw body head archived as the safety net; parser is synthetic-until-first-real-specimen) into KV kwresults (mid-deduped ≤400, pre-capture triage copies lifted out, never triage items); the CC brand dossier renders the archive as '📈 Optimisation results' (grouped per batch, rows expand to metric lines + raw email) for future deck-making
GET  /api/gmail/intake          → captured-email queue for the Workflow triage panel (KV gmailinbox; back-fills missing clients on read; each item carries its triage decision) + `calls` (KV callactions → Workflow Intake rows, source chip = simple "📞 Call" with the meeting named in the tooltip; client = detected only — cue ladder action-line brand > meeting title > RAW EMAIL SUBJECT + recipients > body head (Ray 9 Sep: "Monsoon x cate.com x Fispar" subjects parse the client directly; a Gemini-sender email with a notes-shaped body is call notes even when the subject is just the meeting name), detector roster = dossier ∪ DEFAULT_FEEDS estate so a wired brand without a dossier card still attributes, and unattributed stored rows BACK-FILL on every intake read (title+task rescan, tools/test_callclient.mjs in qa_gate); client-attributed actions AUTO-ADOPT into the client's Project Plan sheet via adoptIntoPlan (Ray's rule 21 Aug) so they carry a real status dropdown + editable due — 📞 origin survives the sheet round-trip via fcc-csrc, CSRC doubles as the once-only guard; unattributed rows stay KV-only; delete/hide stick via DELETED/HIDDEN and deleted rows never adopt; WRITE-VERIFIED (26 Aug): every sheet append is confirmed — MANUAL rows carry w:1 only on an {ok:true} response, resyncPlanWrites re-sends unconfirmed rows batched per client — ONE attempt PER ROW per session, not once per tab (Ray 16 Sep 2026: "the 2 themes are not added as Task in Intake nor Reiss' GS Project Plan" — it ran once at boot, synchronously, BEFORE shBoot merged the team's shared rows in, so a colleague's unconfirmed row, which arrives seconds later through shApply, was never re-sent by ANYONE; only its own author's browser could heal it. shApply now re-runs the pass, so whoever has the board open closes the gap, and adoptIntoPlan stamps the same in-flight gate so the two can never send one row at once), and the local "the plan already carries it → settle w:1 without writing" shortcut is EXACT normalised wording only — sameTask is fuzzy and module task families share a prefix, so Steven's two real themes ("Keywords Optimisation - Cashmere/Merino …" vs "… - Gifting …") dice at 0.615, over the 0.6 threshold, and one was marked written against the other and never sent; appendPlanRows skips tasks already in the tab so retries never double-append, and the saved brand roster (fcc-wf-brands) UNIONS with ALL_BRANDS so a newly-onboarded client can never be invisible to adoptCalls)
POST /api/gmail/dismiss         → triage decision memory (KV gmaildismissed): reasons task/briefed/notask/techam, undo:true restores; decided emails never re-enter the queue. Triage rule: emails are NEVER auto-tasks — → Task files to Intake (client = detected only, sender into the task name), ✕ Not a task clears
GET|POST /api/gmail/techam      → TechAM delegation (Ray's way of working, Aug 2026): the TechAM team answers common client-email requests A2Z, so the triage panel's → TechAM button queues the ORIGINAL message for a real Gmail FORWARD from Ray's mailbox (KV techamq drained by gmail_push.gs message.forward; address asked once, remembered in techamcfg), files the task into the brand's plan via adoptIntoPlan BORN DONE with owner TechAM (their lane isn't progress-tracked; TechAM is in the page's FS_PEOPLE Owner-filter roster), and records dismissal reason techam
GET  /feedlab (+/feedlab/engine.js) → Feed Lab module: animated live-feed dissection (audit, AI-readiness score, recs). AI-READINESS MODEL REWEIGHTED (Ray, 16 Sep 2026: 'label architecture that involves custom labels is not necessarily usable for AI, so I don't know what's in there … include the most important factor for AI readiness, probably the conversational attribute that Google mentioned — when you fix this, obviously fix Feed Lab as well'; ONE engine, so /feedlab and /golden move together): CUSTOM LABELS LEFT THE SCORE — Google's spec for [custom_label_0-4] (answer 6324473) says they make 'filters to use in your Performance Max, Shopping, or Demand Gen campaigns … for reporting and bidding' and that 'the information you include in this attribute won't be shown to customers', so a field no surface reads is not evidence of readiness for those surfaces; the reading still comes back as `labelArchitecture` {score, scored:false, why} and BOTH pages print it as 'measured, not scored' with the reason + a link to Label Guard, which owns the detail. CONVERSATIONAL ATTRIBUTES ARE NOW THE HEAVIEST PILLAR (x2.4, four weight dots) — the only fields in the spec whose STATED purpose is AI comprehension (answer 17085370: so 'customers discover information about your products across AI-driven surfaces, like AI Mode in Search'; optional, never affect approval, supplemental data source or Merchant API); scored on COVERAGE not presence (q&a on 3% is a pilot, not a capability), the two variant-only fields (item_group_title, variant_option) dropped from the denominator on a feed with no item_group_id, and the weight is set so everything else tops out at 79.3 — a feed with NONE of them cannot read as Agentic-ready. The old 'Agentic readiness' pillar folded the six in beside identity + MASK (diluting them AND double-counting other pillars) → renamed 'Structured detail' = .55 structured richness (highlights per item, product_type depth) + .45 description depth, x1.2; identity 1.2→1.4 and taxonomy 1.0→1.2 (Search Engine Land's non-negotiables: valid GTIN, accurate title, price + availability matching the live site, clean image, brand, correct category). Still EIGHT pillars so every stored airP map, estate heatmap and the PDF tripwire fit; PILL_COLS on both pages swap Labels→Conv and Agentic→Detail (pre-change stored maps read '—' for Conv until a re-scan). LIVE IMPACT the day it shipped: Superdry GB 73→59 (Tier 3→2), Monsoon GB 68→56, both at 0/6 — the drop is the finding. Harness tools/test_feedlab.mjs (40 assertions: the weighting, coverage-not-presence, the variant exclusion, the ceiling, no double counting, and that emptying all five custom labels moves the headline by exactly nothing) in qa_gate/presync/validate. Docs: docs/FEEDLAB.md §5. Worker only STREAMS the sheet CSV (/api/feed/proxy?client=); the browser runs feedlab_engine.js and PUTs the audit to /api/feed/audit (KV feedaudit:<client> + :hist). Feed wiring: worker `DEFAULT_FEEDS` = the committed master feed-market map (imported from Ray's sheet `1eiqTbLC0fpJfjVyeJaf72kYfLPgGLDWUfXB38bRDfak` — ESTATE XML MIGRATION (Ray, 9 Sep 2026, from his uploaded FeedHero export — ops/feeds/feedhero_selection_2026-09-09.json is the committed record): EVERY Google-channel feed except House of Bruar gb/us/eu now imports from its realtime FeedHero XML — 46 {xml} feeds: Schuh gb/de/ie, YuMOVE, Monsoon, Accessorize, Hobbycraft, American Golf, Superdry gb/ie/de/fr/nl + NEW us/es/dk/benl/befr, Reiss all 28 markets (gb/us/ie/de/nl/au/ca/eu/fr/uae + NEW at/be/ch/cz/dk/es/fi/gr/hk/it/kw/pl/pt/ro/sa/se/sg/sk); per market the export's exact-'Google Shopping' row wins over localized variants; xmlRef also allows bare feedhero.net (reiss_us/eu); old sheets stay ONLY for cross-check — keep {xml} entries through any re-import. Sheets-only guards (Label/PT/Golden, gviz) now cover just the -fb Meta feeds + HoB until the XML scan lane is built; the cron sweep filters src.id so XML never burns rotation slots; Meta `<mkt>-fb` — META XML MIGRATION (Ray, 9 Sep 2026, second FeedHero export; record ops/feeds/feedhero_meta_selection_2026-09-09.json): 16 -fb feeds now realtime FeedHero XML (Schuh gb/de/ie, Monsoon gb, Accessorize gb, Superdry gb, Reiss gb/ca/de/ie/us + NEW au/eu/nl/sa/uae — Reiss gb-fb's Florere sub-brand variant feed deliberately not wired); still sheet-backed from the Meta tab gid 908873996: Hobbycraft gb-fb, Superdry ie/de/fr/nl-fb, HoB gb/us-fb); a source is a sheet {id,gid} OR a FeedHero-hosted XML feed {xml} (host-allowlisted `*.feedhero.net`, realtime, browser engine sniffs XML vs CSV from first bytes; Label Guard skips XML — gviz is sheets-only); `/api/feed/clients` bootstraps the Feed Lab selector + CC dossier (FEEDWIRED counts); ad-hoc attach in the CC dossier (⚡, link-shared Google Sheet or FeedHero XML URL) OVERRIDES the wired entry per market. New sheet rows → re-import into DEFAULT_FEEDS. COLUMN-E MIGRATION (Ray's workflow, 9 Sep 2026): the master sheet's column E "Google Shopping URL" holds each feed's FeedHero XML URL — Ray fills it at his own pace; `node tools/sync_feedmap.mjs` migrates DEFAULT_FEEDS to {xml} for every filled row (joins rows to the map BY SHEET ID, live-verifies each URL serves XML, line-level splice keyed on the sheet id so {xml} entries can never be reverted by a re-run; DRY=1 = report only; a wired XML gone from column E is a warning, never an auto-revert). Each migrated feed leaves the sheets-only guards (Label/PT/Golden — gviz) until the XML scan lane is built. Docs: docs/FEEDLAB.md
GET  /labels (+/api/labels/*)   → Label Guard module: g:custom_label_0..4 capture (value/volume pivots per client×market) + drop-off monitoring vs a known-good BASELINE — PMAX listing groups key on these exact values, so drops are flagged (crit/warn) on /labels AND as a badge on every app page's nav; cron sweeps 4 feeds at :00 + 4 more at :30 when the watch pass leaves budget (42-feed estate ≈5–6h) via Google gviz group-by queries (the worker never parses raw feed CSVs); "Expected — accept as known-good" (POST /api/labels/ack) accepts intentional changes; VERIFY-VANISHED (Ray, Sep 2026): vanished-column alerts (label-gone/cov-zero here + /ptypes, attr-gone on /golden) carry a "⟳ Verify — fresh scan" button — one manual re-scan confirms the wipe or clears a bad read (recovered column self-clears via the baseline roll; unstable-read skips surfaced honestly); ASK-THE-CLIENT (Ray's rule: confirm a drop is expected BEFORE acting): every alert row + per-client bundle button composes a pre-filled professional client email with a canvas-rendered before/after breakdown PNG per alert — "Create Gmail draft" queues it via POST /api/labels/askdraft (KV labeldrafts, drained by gmail_push.gs into GMAIL DRAFTS — never auto-sends; mailto + image-download fallback), contact remembered per client (labelaskcfg), alerts stamped "✉ asked" (labelasked; cleared by ack/recovery); Δ columns read vs YESTERDAY by default (labelday auto-capture) with a compact header toggle to vs LAST KNOWN-GOOD (estate alerts always fire vs last known-good; watches fire vs their own pinned reference, moved only by Re-arm); click any pivot value → LIVE cross-label dissection (GET /api/labels/cross: CL0 "Best Sellers" → CL2 women-fp/women-sale volumes), CL0–4 panes on one seamless row, sortable headers (value/skus/Δ) + 🔍 value filter across panes; CUSTOM WATCHES (🔔 alert builder, /api/labels/watch|dest kvmerge stores): pin exact values/cross breakdowns per feed → per-rule schedule: cron (:30) live-checks hourly or 0 7,17 * * * checks twice daily (07:00/17:00 GMT) and fires HIGH-PRIO pings to Google Chat/Slack webhooks direct or email via the Gmail bridge (KV labeloutbox drained by gmail_push.gs drainAlertOutbox); fire on transition, re-ping 24h while broken, ✅ on recovery, "Re-arm" accepts planned changes; daily 07:00 GMT status-report email (labelreportcfg, /api/labels/report[/send]); Facebook/Meta catalogue feeds ride the same rails via market suffix -fb — two colour-coded sets, Google green vs Facebook blue (XML SCAN LANE, Ray 9 Sep 2026 'fetch four times a day for every URL': .github/workflows/xml-scan.yml (cron 4x/day UTC + dispatch, secret FCC_PUSH_KEY = the GMAIL_PUSH_KEY value) runs tools/xml_scan.mjs — fetches every wired {xml} feed, aggregates per-row INSIDE the parser callback (a 125MB feed costs MBs), assembles the scanFeed-shape snapshot with labelguard.js's OWN exports (findCols/snapshotFromParts/attrsFromCounts), and POSTs batches of 8 to /api/gmail/push {xmlscan} (existing bypass + push key; worker validates the feed is a WIRED {xml} source and forces identity fields) → the shared processScanSnapshot runs the SAME baseline/day-ref/alert/two-strike processing as the gviz sweep (worker refactor: runLabelScan = fetch + processScanSnapshot; markScanUnreachable shared; push-lane catastrophic readings are HELD via labelpend: until a confirming re-push agrees on rows — the agent retries immediately on retry:true). Estate grids on /labels /ptypes /golden list XML feeds first-class again; the gviz cron sweep still covers the sheet-backed rest (HoB google gb/us/eu + HoB gb/us-fb + Hobbycraft gb-fb + Superdry ie/de/fr/nl-fb); custom 🔔 watches remain gviz-live = sheet-only; MANUAL LIVE RESCAN (Ray, 10 Sep 2026: the per-feed scan button must not depend on the 4x-daily run): on the sheets-only refusal the guard pages (/labels /ptypes /golden) stream the wired FeedHero XML IN-BROWSER — Feed Lab parser + labelguard's shared xmlCollector, served at /labels/engine.js from docs/labelguard_engine.js, a committed byte-identical copy of src/labelguard.js enforced by tools/check_lgcopy.js in qa_gate/presync/validate — and POST the computed snapshot {snap, vol} to /api/labels/scanpush (Access-gated, ACT 'label-scan-live'): applyPushedSnapshot forces identity, must resolve an {xml} source (dossier-attached XML included), runs the SAME processScanSnapshot + volTrack as the agent lane, and a catastrophic reading is HELD (202 retry) until the page's automatic second stream agrees; scan-all stays cron-only); LABEL POPULATION + CARDS THAT FOLD (Ray, 18 Sep 2026, sending the PT depth granularity card: 'I want this feature to be the table breakdown for highlight population (1, 2, 3, 4, 5, >5) applied to label cards and product types as well, and the [Golden Record scorecard's expand and collapse — all and individual] too'): the feed dissection opens with 'Label population — labels carried per SKU · SKU-weighted' — the depth card's OWN row format (popCard: one row per bucket 1…5 labels, share as a bar + number, footer 'avg N labels per SKU · N SKUs profiled · N carry none (x%)' + the share carrying 3+ of the five) read per SKU by xmlCollector (labelguard.js popProfile → snap.labelPop {pct,avg,skus,zero,max}) on the XML lanes ONLY (agent + ↻ Scan live feed) — the gviz lane counts columns, never rows, so a sheet-backed feed's card SAYS SO ('this snapshot came from column counts … wire the FeedHero XML') rather than drawing an empty table; the worker's splitRaw keeps labelPop on the labels: store and ptPop on the ptype: store, each through cleanPop (fixed keys, clamped — the push lanes hand over what a browser computed); EVERY BRAND CARD FOLDS ON ITS OWN (header row = chevron + brand + a summary that survives the fold: feeds, scanned, crit/warn — closing a brand can never hide a drop-off; ⊖ Collapse all / ⊕ Expand all in the section header, the button always naming the action still available; localStorage lg-collapse, a device preference never shared state; the ?client= deep link opens a folded card before scrolling to it). Harness tools/test_guardcards.mjs (Playwright, presync: both guard pages' tables, notes, folds, reload, deep link + /golden's highlight row) and tools/test_labelguard.mjs (popProfile/cleanPop/slotCols, the collector on a real XML stream, the worker split). Docs: docs/LABELGUARD.md §5a; ⬇ HTML · ALL MARKETS — ONE FILE PER BRAND (Ray, 21 Sep 2026: "Is there a way to get data shown for clients with multi-market such as Schuh, Superdry, or Reiss? A downloaded CSV or HTML would be fine, but not too techy. I like the current filter section view … HTML would be best because it can be interactive"): the dissection reads ONE feed, so two doors — ⬇ HTML on every brand card + ⬇ HTML · all markets beside ⤓ CSV in the dissection header — open brandExport(client): every market's snapshot (held, or fetched per scanned market; a never-scanned market is listed as such), lgxRecord() reduces them to exactly what the dissection draws, lgxHtml() writes ONE self-contained file (data embedded, LGX_APP serialised from the page with .toString() so it is parsed code, no fetch, nothing of the FCC, CL0–CL4 never custom_label_n): Across markets (coverage per label per market side by side, row click opens the market) · Values across markets (any label's values as a column per market, keyed by POSITION since GB and GB-FB share a code) · market tabs in the estate's order (never-scanned present but disabled) each carrying the dissection as on the page — population card, five panes value · SKUs · share · Δ, GONE rows, the market's alerts, the reference the Δ reads NAMED per market (yesterday where a daily capture exists, else last known-good) · the same 🔍 filter across every pane AND the matrix, sortable headers, show-all · ⤓ CSV INSIDE the file (this market / all markets, the page's CSV columns + market + channel) so one download gives both formats · 🖨 Print; demo mode masks the file as it masks the screen. Harness tools/check_lgexport.js (Playwright, presync: drives the real page, opens the downloaded file in a second page and uses it as a client would; LGX_KEEP=1 keeps it). Docs: docs/LABELGUARD.md §5c. THE ONE-HOUR OUTAGE IT SHIPPED WITH (Ray, 21 Sep 2026: "/labels have issue"): worker.js › inject() puts every widget + the live editor at the FIRST `</body>` in the served page, and the file builder's string carried a literal `</body></html>` — so on the live site the injection landed INSIDE that string and the page's whole script died, while every block parsed on its own (check_inline_scripts green, validate green, file:// harness green). Every closing tag in a page's script is written escaped (`<\/script><\/body><\/html>`), and tools/check_inline_scripts.js now parses every app page AS SERVED (the worker's injection applied) and refuses a `</body>` that is not the document's own closing tag
GET  /ptypes (+/api/ptypes/*)   → Product Type Guard module: primary g:product_type capture (category-tree pivot per client×market, top 250 paths) + drop-off monitoring vs last known-good, Google Shopping feeds ONLY (-fb excluded; primary column resolves bare g:product_type OR slot-1 g:product_type(1); numbered keyword slots 2..10 excluded by design) — PT drives PMAX listing-group splits, so drops flag on /ptypes AND the nav badge (pt counts ride the same /api/labels/alerts call); ZERO extra scan slots: runLabelScan captures PT in the same pass (+1 gviz group-by) into ptype:/ptypebase:/ptypeday:/ptypeidx/ptypealerts; Δ vs yesterday toggle ↔ vs known-good (sessionStorage pt-ref), "Expected — accept as known-good" (POST /api/ptypes/ack), live cross-dissection PT value → CL0-4 (GET /api/ptypes/cross), sortable headers + 🔍 path filter; DEPTH GRANULARITY KPI: SKU-weighted % of the catalogue at 3/4/5-level chevron paths (depthProfile, / fallback; avg levels; stored on ptypeidx at scan time) — colour-ramped stacked depth bar + chips on the PT card, mini bars on estate rows, styled hover card breakdowns on estate market rows, the scanned date, the brand h3 and the PT name; ✉ EMAIL on confirmed warning (KV ptypealertcfg via /api/ptypes/alertcfg — OPT-IN, OFF by default since Ray dropped the automated alert emails Aug 2026; page re-enable stamps v2, legacy configs never email; same rule on goldenalertcfg): estate warn/crit emails a per-feed digest through the Gmail-bridge outbox only on its 2nd consecutive sighting (estateMailPlan — one garbage read never emails), once per incident, ✅ on recovery; daily report gains a PRODUCT TYPE ALERTS section; 5-DEPTH STANDARD (Ray: 30–40% of volume at 5-level paths is the industry standard): depthStandard verdict pill (✓/⚠ below/🔻 shallow) on the PT card + hover-card footer, below-standard feeds get ✉ Propose depth optimisation — depthAskEmail composer on Label Guard's shared ask rails (askdraft → Gmail Drafts, contact memory labelaskcfg, ✉ asked stamp ptdepth|client|mkt) + default-on checkbox files "PT Depth Optimisation" into the client's Project Plan via POST /api/ptypes/plantask (worker PLAN_SHEETS → appendPlanRows) so it rides Intake → Project Plan → pipeline like the Gmail triage; PRODUCT_TYPE DEPTH CARD + CARDS THAT FOLD (Ray, 18 Sep 2026, the same ask as /labels; card SWAPPED 22 Sep 2026 — Ray, screenshotting the population table: "PT guard does not need this breakdown. It doesn't make sense anyway. Instead, replace it with the PT depth granularity chart"): the per-SKU 'product_type population — values carried per SKU' table (1…5, 6+ VALUES = category tree + keyword slots, the same shape as Label Guard's label-population card) never told a PT audit anything it needed, so the SAME card slot (popCard's shared row format) now draws ptDepthCard(dp) — the depthProfile SKU-weighted read (1…5, 6+ LEVELS) already computed for the compact header bar/chips, full-width, footer stating the 5-level share against the 30–40% industry standard and the 1–2-level shallow share; hi-lit buckets 3/4/5 (dpChips' own emphasis), no 'carry none' line (a present product_type column always resolves to a depth ≥1). BECAUSE depth reads the value/count PIVOT every scan captures rather than a per-SKU-only field, a sheet-backed (gviz) feed gets the FULL card too — no 'wire the FeedHero XML' fallback, unlike Label Guard's population card which stays XML-only. The retired ptPopCard() is gone from the page; the underlying per-SKU ptPop capture (xmlCollector → snap.ptPop, worker splitRaw) is UNCHANGED and still lands on the ptype: store — Ray named the card, not the data pipeline, so nothing downstream of it was touched. The estate cards still fold as on /labels (localStorage pt-collapse), the folded summary carrying the brand's average tree depth beside markets and flags. Docs: docs/LABELGUARD.md §8a
GET  /golden (+/api/golden/*)   → Golden Record module: attribute coverage vs GOOGLE'S PRODUCT DATA SPEC (answer 7052112, roster vetted Aug 2026) — ATTR_SPEC three tiers: REQUIRED (id/title/description/link/image_link/availability/price), REQUIRED-IN-CASES (brand new-products, gtin/mpn identifier pair, condition used-only, item_group_id variants, color/size/gender/age_group = the apparel five, flagged hard) and RECOMMENDED (gpc, product_type, sale_price, additional_image_link, product_highlight, product_detail, material, pattern, size_type, size_system = the optimisation surface); captured in runLabelScan on the SAME multi-count gviz query (ZERO extra subrequests, Google feeds only, count() formula-blank caveat accepted); stores golden:/goldenbase:/goldenday:/goldenidx/goldenalerts, three-reference model + ack (POST /api/golden/ack) like the sibling guards; goldenScore = weighted completeness (required ×3 missing-column=0, cond ×2 present-only with gtin+mpn merged best-of-two, rec ×1) vs the 99.9% Golden Record target; diffCoverage drop alerts (req/cond crit ≥10pp or column VANISHED — products disapprove; warn ≥3pp; rec warn-only ≥10pp), two-strike email via goldenalertcfg, GOLDEN RECORD ALERTS section in the daily report, gr counts + grClients on /api/labels/alerts (nav badge dots /golden); page = score DIAL + plain-English verdict (the header block is PINNED while you scroll the rows — Ray 16 Sep 2026 'when using the Golden Score card and scrolling down to review the feed scorecard, make sure this section stays frozen when scrolling down': sticky under a MEASURED topbar offset since the topbar is sticky itself and grows with the phone layer / view-as pill, CONDENSING once pinned — dial 150→74px, verdict away, 206→~120px, because a full-height frozen header is a wall across the viewport — while the feed, score, Δ reference and actions stay; the stuck state is read off the element's own position per animation frame, never a sentinel, since renderDetail rewrites the panel and an observer bound to the old nodes watches a detached element; static under 760px; print un-pins it AND the condensing pass stands down so paper gets the page at rest), three tier sections (spec badges, fill bars, Δ vs yesterday ↔ known-good toggle gr-ref, spec-condition notes, "not in feed" soft vs hard-red on required+apparel), estate scorecard with per-market scores + "N req missing" pills (EACH CARD COLLAPSES ON ITS OWN — Ray, 17 Sep 2026: 'Golden record in the dossier scorecard; allow button to expand or collapse "all" or individual brand option' — a card's header is a 36px clickable row (chevron + brand + a summary that survives the fold: avg score, market count, N req/crit/warn, so closing a brand can never hide a live alert), ⊖ Collapse all / ⊕ Expand all in the section header does every card at once with the button always naming the action still available; localStorage gr-collapse like gr-ref/gr-demo — a device preference, never shared state; the ?client= deep-link opens a folded card before scrolling to and outlining it), TWO SCORES PER MARKET, CAREFULLY TOLD APART (Ray, 17 Sep 2026: 'Surface content quality score directly in the dossier scorecard as well, next to the normal score. So there should be two scores appearing for each market, each brand. Obviously, carefully label them so we don't mistake. Feed scorecard and content quality score') — each est-mkt row previously showed only f.score; the SAME /api/golden/estate payload already carries f.q (content quality, written onto goldenidx by the /api/golden/quality PUT handler), so both now render as a captioned pair (bold number over a short uppercase label, feed / content, full-sentence tooltip on hover) using the SAME scoreCol() bands rather than two bare numbers a reader could swap; AND THE CONTENT SCORE SURVIVES THE NEXT SCAN (Ray, 21 Sep 2026: "why content quality score isn't saved?" — Schuh's three markets read FEED only an hour after the agent's pass): processScanSnapshot and the ack REBUILT the goldenidx entry from the scan alone, so every cron / agent / estate pass wiped q/qFails/qT/air/airTier/airP — the headlines only the /api/golden/quality PUT writes; keepQual carries them forward on both writers (the reading stays until the next analysis, qT dates it), pinned in tools/test_labelguard.mjs; a market not yet analysed for content quality shows only the feed score, never scanned shows neither, ⚡ estate rescan, CSV, ⬇ PDF = ONE CONTINUOUS VERTICAL PAGE **AND THE PAGE ITSELF, SCALED** (Ray 16 Sep 2026 'should all fit in 1 vertical page', then 'PDF export still doesn't reflect exact same visual as FCC'): body.pdf lays the scorecard out at its DESKTOP width (960px) and zooms the whole document onto the 186mm printable column — zoom NOT transform:scale, which is paint-time so Chrome paginates on the unscaled height — so every size, space, colour and column ratio survives and only the ruler changes; exportPdf measures the zoomed document and injects @page{size:210mm <content>mm}; the only print rules hide interactive chrome and restore the page's own <=900px rules to their DESKTOP form (the print viewport is 703px, else the client gets a phone layout on paper); check_grpdf reads type scale/padding/dial on screen AND in print and fails on any difference beyond zoom rounding, so a compact variant can never creep back; beforeprint covers a bare Ctrl+P; the CONTENT QUALITY section prints with it — score, verdict, every attribute row with its worst rule + hit rate + req/bp counts, plus any expanded rules, Ray 16 Sep 2026 'make sure it is one or two pages' — with print compacting chrome not content (~1.5 A4 lengths incl. quality); the 703px printable column trips the page's own <=900px rules, so body.pdf re-shows the spec note, worst-rule summary and hit counts; >3 A4 lengths paginates as ordinary A4; tripwire tools/check_grpdf.js in presync renders the REAL PDF and measures AT THE PRINTABLE WIDTH), ⬇ PDF IS ONE CLICK, NO DIALOG (Ray, 17 Sep 2026: 'PDF download for Golden Record to shre to client still not seamless btw — shall we also add html download while you fix pdf download?') — window.print() always opens the browser's own dialog, no way for page JS to skip it; exportPdf now rasterises the SAME body.pdf layout with html2canvas + jsPDF (cdnjs, loaded on first use) and calls .save() directly, captured at the page's natural 960px width (a new body.pdf.pdfshot{zoom:1} override, since screenshot-library zoom support is inconsistent) then sized to the image's own aspect ratio so it's still the one continuous sheet; document.fonts.load() force-loads every Lato weight first — capturing before the bold 900-weight (used only by headings the print layout had just revealed) finished loading ran every word together with no space, a real bug QA caught visually; the old print dialog is the fallback if the CDN can't be reached, never remembered as permanently failed so the next click retries. ⬇ HTML sits beside it — clones the whole page verbatim (same <style> block, so it can never drift from /golden itself), strips every <script>, same natural-size layout: a genuinely zero-dialog, self-contained file. THE CLIENT DOCUMENTS ARE SIMPLER THAN THE AM'S SCREEN (Ray, 18 Sep 2026, reading the ⬇ HTML before sending it: 'Keep the content quality score description, but remove the weight. The 1.4 × 1.6 factor is not necessary … show the scoring bars in green, yellow, and red, because now they're empty. Remove the pillar heat map for now; it's too complicated for multi-market clients … remove the "two scores" question from every HTML, and remove the element that sends code or feedback to Claude'): PDF and HTML share body.pdf, so ONE rule drops the pillar weight dots, the pillar heatmap and the 'Two scores, two questions' band from both (the live page keeps all three — AM tools); the HTML export REMOVES those nodes rather than hiding them; clientCopy() (true only while the export records the pop-ups) makes every scoring builder write its description WITHOUT the ×factors (weights as an ordering in words, ×1.0/×0.4 as 'full cost / a fraction of it'); the pillar and MASK bars carry their width INLINE with the pillar bar coloured by the number's own airCol band (green ≥80 / amber 60–79 / deep orange 40–59 / red <40) and a CSS @keyframes grow-in, so a file with no scripts draws them (body.pdf sets animation:none because html2canvas renders a fresh clone); the live editor's ✎ handle + 'Send an element to Claude Code' panel + 💬 Feedback panel, Tachyon, Feed Chat, view-as, app switcher, nav customiser, ⓘ toggles and every HTML comment are REMOVED from the file. tools/check_grpdf.js renders the export from the page AS SERVED (widgets injected) and asserts all of it, incl. that the file never mentions Claude; GRPDF_KEEP=/path.html keeps the export for a visual pass. THE INLINED METHOD READS AT THE POP-UP'S OWN SIZE (Ray, 21 Sep 2026: 'the text on the downloaded html … is strangely bigger than rest — can you mirror the same text size/font as rest of audit'): the pop-up's type scale lives on .sc-box and the body element sets no font-size, so the same prose inlined by popDet fell back to the browser's 16px beside a 12px audit — popDet stamps the body `xd-pop` (12.5px / 1.55 + the shared ul/li rules), and check_grpdf RENDERS the exported file and asserts the computed size at section, pillar and attribute is 12.5px and no larger than the row beside it. THE DOWNLOAD FOLLOWS THE INTERFACE EXACTLY (Ray, 21 Sep 2026, sending the on-screen scoring pop-up: 'just follow the audit golden score interface exactly for the download (i like this)'): body.xhtml keeps the screen's own look — wash background, the 1180px .wrap column, the header band flush in the panel card with its dial and chips (the active Δ reference as a static chip, the other toggle button removed; #pdp-last and #prof-edit removed) — and every inlined scoring box IS the pop-up card: popDet writes the pop-up's OWN header into the summary (its own title — 'Content quality — the headline' / 'AI-readiness — the headline' / 'g:title — content quality' — the 22px score, the tier/verdict chip) over the pop-up's body on a white 14px card; a pillar tile folds its card behind the tile's own 10.5px 'how it's scored' chip (mode 'chip', header inside; .pillars{align-items:start} so an opened card grows its own tile only). check_grpdf's rendered-export block asserts header sizes, the white card on the wash page, the attribute card's title, the chip, the single Δ chip and the absent buttons; check_grhtml looks for the pop-up titles. AND IT IS FOLDABLE, WITH THE SCORING LOGIC IN IT (Ray, 18 Sep 2026: 'allow the HTML file downloaded to be expandable and collapsible on every sections (content quality and AI-Readiness) so my client can read the scoring logic and the detail of each content quality scoring. Please remove the Scoring section bottom from every HTML download') — stripping every script is what makes the file safe AND what made it dead: every ▸ chevron and every ⓘ Scoring logic button was wired to a handler that no longer existed, so the method a client is asked to trust was unreachable and a folded attribute's findings were not even in the file. htmlifyExport now rewrites the clone: native <details>/<summary> (no JS), the two section methods OPEN by default (a client should meet the method, not hunt it) and every attribute and pillar folded (eight attributes of findings expanded at once is a wall; the summary row already carries the score and the worst-breaking rule). The prose is NOT re-written for the export — SCCAP turns the ONE openScPop into a recorder, so the export calls the SAME five builders and inlines exactly what the AM reads; a rule change can never drift the client's copy. THREE traps, each pinned: every analysed attribute is force-opened BEFORE the clone (a closed row's detail is never rendered, so the download would carry a chevron over nothing) and the page is put back exactly as it was afterwards; .qz-det is a CHILD of .qz-row, not its sibling, so it is lifted out BEFORE the row's children move into the <summary> — a naive move buries the whole findings block inside the summary and the row expands to nothing; and the export gets its own body class ('pdf pdfshot xhtml') so the PDF rasteriser never picks the disclosure layout up. The dead chrome goes too, on BOTH exits (body.pdf hide list + node removal in the clone): the ⓘ Scoring logic buttons (now the disclosure itself), the injected FCC layers the clone drags in with their scripts left behind — the live editor toolbar (✎ Edit / 🎨 Design / 🧹 Reset page), the Shipped panel's edge handle, presence avatars, the EN/VI pill, the hours dots, the phone bar — and the rows' internal ✉ Ask client / → Brief actions. Tripwire tools/check_grhtml.js in presync renders the REAL exported file in a browser and TOGGLES it, rather than trusting the string (the string cannot be trusted here: the page's own stylesheet names every stripped selector in its hide list). NOTE the hide list is TWO rules, deliberately — tools/test_labelguard.mjs reads the page's own body.pdf list BY SHAPE (.qz-scan immediately before the brace), so appending the injected-chrome selectors to it breaks a tripwire with nothing to do with this; and that suite (380 assertions over /labels /ptypes /golden) is NOW in qa_gate + presync as this file has long claimed — it was validate-only, so every session touching those three pages got its first signal from a red CI run, which is how PR #458 found one stray selector. Harness: the one-click-download + CDN-fallback + HTML-export blocks in tools/check_grpdf.js; ⚡ SCAN WHOLE ESTATE NOW ALSO SCANS CONTENT QUALITY (Ray, 17 Sep 2026: 'scan whole estate > will also scan content quality too for all estate') — the estate rescan forced every feed's score/labels/product types/attribute coverage but left content quality to per-feed manual 'Analyse Content Quality' clicks, so a freshly force-rescanned estate could still show stale or missing quality readings; each feed in the loop now gets BOTH passes back to back (the existing /api/golden/scan POST, then qualityRun() — the SAME in-browser stream + PUT the per-feed button uses, so a feed scanned from here reads identically to one scanned by hand), one feed's content-quality failure never halts the estate run. Harness tools/test_grscanall.mjs, and 🎭 demo mode (industry aliases, gr-demo) for client screen shares — policy page cited in hero + footer; CONVERSATIONAL AI SIX as a fourth tier (req:'ai': question_and_answer, document_link, related_product, item_group_title, variant_option, popularity_rank — Optional on the same spec page, usually supplemental-source, warn-only alerts): own AI-readiness KPI "N of 6 live" (verdict + tier header + goldenidx.ai), NEVER counted into goldenScore; PER-ATTRIBUTE ACTIONS on every row: ✉ Ask client (attrAskEmail composer, missing vs low-coverage variants, shared askdraft rails, stamps grattr|client|mkt|attr, default-on checkbox files "Golden Record Fix - g:<attr>" via POST /api/golden/plantask → PLAN_SHEETS/appendPlanRows) and → Brief (/workflow?brief= deep-link cat technical + the same plantask write keepalive) — both paths ride Intake → Project Plan → pipeline like the Gmail triage; actions hidden in demo; INDUSTRY SCORING PROFILES (Ray: certain attributes incorporated into scoring per brand/industry = the industry best practice): engine INDUSTRY (client→industry) + INDUSTRY_PROFILES defaults (apparel five expected for Fashion/Footwear, apparel-only recs waived for Pet Care etc.), profileFor merges defaults ← KV industry override ← KV brand override (goldenprofiles, GET/PUT /api/golden/profile; required seven + gtin/mpn untouchable), goldenScore(attrs, profile): expected counts-when-absent at tier weight (AI six opt-in-able at ×1, ★ on a REC attr lifts it to ×2 — rec always scores, so without the weight lift starring one would be a no-op, Ray 16 Sep 2026), waived excluded; scan stores industry + per-attr cov map on goldenidx → the page computes INDUSTRY BENCHMARKS from the estate itself (verdict line "avg X · best Y (Brand)" + best-practice tick on every fill bar at industry-best coverage) AND live-derives every estate score from that cov map + the CURRENT profile (rescoreEstate), so a profile save re-scores the WHOLE brand instantly — dial, every market, benchmark; industry scope re-scores every brand in it, zero rescans (Ray 16 Sep 2026); ⚖ "<Industry> best practice" chip + ⚙ tri-state editor (default → ★ scored → waived, brand vs whole-industry scope, reset), ★ marks scored attrs, hard not-in-feed flags follow the profile; CONTENT QUALITY (Ray, 16 Sep 2026: 'reviewing the data quality of each attribute especially if they contain free content … each has its own standard of what good quality must look like — benchmark rating against google support page'): a fifth section on the scorecard reading what is IN the free-text fields, not whether they are filled — engine QSPEC = 8 attributes x 46 rules quoted from each attribute's OWN Google spec page (title 6324415, description 6324468, product_highlight 9216100, gpc 6324436, product_type 6324406, color 6324487, material 6324410, pattern 6324483), two severities never blurred (fail = stated REQUIREMENT -> disapproval/truncation risk; warn = stated BEST PRACTICE); careful judgement calls (stripBrand+shoutyCaps so HUGO BOSS is a logo and 'SALE NOW ON' is a shout; a bare top-level GPC name is shallow not invalid since the rule tests the SHAPE Google specifies; 'n/a' caught once by the placeholder rule, never double-counted); qualityCollector streams IN-BROWSER over the live feed (worker never parses rows; bounded dupe map, 4 example offenders per rule) -> PUT /api/golden/quality (KV goldenqual:<c>:<m>, worker keeps only spec-known attrs/rule ids, annotates goldenidx q/qFails/qT); qualityScore = rule costs the share of FILLED products breaking it x severity (QW fail 1.0 / warn 0.4), attributes weighted 3/2/1 like goldenScore, bands >=90 meets spec / 75-89 below best practice / <75 spec violations; page = headline score + verdict, per-attribute rows expanding to every broken rule with Google's own wording + the offending values + the spec link, per-attribute Ask client (qualityAskEmail) + Brief ('Content Quality - g:<attr>' into the Project Plan), hidden in demo + PDF; a column debuting deeper than the first product is named 'not measured this run', never scored partially; DOES THIS ISSUE APPLY TO THE BRAND? (Ray, 21 Sep 2026: 'for each problem, for example under Title, allow a button to indicate whether the issue actually applies for the brand … Reiss, brand not in title — it should be possible to remove the issue and have the overall score reanalyzed'): every rule row in an expanded attribute carries '✕ Doesn't apply to <Brand>' — one click writes ONE '<attr>:<ruleId>' token onto the brand's scoring profile (`qwaived` on the SAME goldenprofiles record the ⚙ editor keeps, brand scope, team-shared; PUT /api/golden/profile {qrule, waive}; the editor's whole-record save and its reset both PRESERVE qwaived since they never see it; industry-level qwaived unions in; every token validated by qruleKnown against QSPEC) and the engine's attrQuality(key, a, qwaived) costs the rule nothing, drops it from `broken`/fails/warns and reports it APART under `waived` with the share it would have cost — so the row, the headline, the verdict, the Ask-client email and the brief re-analyse without it, while the rule stays LISTED, struck through, under 'Set aside for <Brand> as not applicable' with '↩ Applies again' (visible and reversible, never silently gone; the pop-up reads 'set aside for <Brand> · x% not counted', the headline pop-up says how many). THE STORED HEADLINE FOLLOWS: the worker re-scores every stored goldenqual reading of the affected brand(s) against the new profile on the same PUT and rewrites goldenidx q/qFails (returned as `requal`, mirrored onto the estate in-page), and the /api/golden/quality PUT now scores with profileFor too — before this the index carried the unprofiled score while the page showed the profiled one. The button is an AM action (hidden in demo, removed from the ⬇ HTML / hidden in the PDF); the set-aside block itself stays in the client copy. Harness: tools/test_labelguard.mjs (engine + page/worker wiring); AN ATTRIBUTE SCORES ONLY WHERE GOOGLE ASKS FOR IT — GPC CATEGORY SCOPE (Ray, 23 Sep 2026, on Hobbycraft's material 47.9% / pattern 43.9%: "for standard GPC material, that attribute is not required. So by that logic, it should not be cumulated or counted toward the score"): every attribute was scored over EVERY product, so a craft catalogue was marked down for yarn without a gender and a golf retailer for clubs without a size. Google's spec has NO per-category attribute list across the ~5,600 GPCs — six attributes carry a category condition, each read off its own help page: color (166 Clothing & Accessories, 6324487), gender (166 minus 16 named sub-categories, 6324479), age_group (the same + cufflinks 193, 6324463), size (1604 Clothing + 187 Shoes, 6324492), material + pattern (166 per the clothing best-practice page 7348545; elsewhere only 'for products that vary by' them). labelguard.js GPC_SCOPE: each is scored over the products in its category (attrs[k].cov = that coverage, .all = the whole-catalogue reading, .scope {n,f,u,t}); NONE in scope = .na — out of the score even when ★ starred, never listed missing, never alerted, the row reads 'no clothing & accessories products — not scored' with no actions; the weight is × n/t so 2 clothing products in 24,000 cannot move the score (an all-apparel feed scores exactly as before); a cond attribute ABSENT on products Google requires it for counts at 0 with or without a profile; a product with NO readable category stays IN scope. The category is read in every language the estate ships (en-GB 'Clothing & Accessories', en-US, de, fr, nl, da, es; numeric IDs; a stray leading '{' as on 8 Reiss markets) from a table generated off Google's own taxonomy-with-ids files by tools/build_gpc_scope.py (26 locales). Measured per product, so the XML lanes only (xmlCollector → gpcScopeCounter → applyGpcScope); a gviz-read feed keeps the whole-catalogue reading. goldenCovIndex writes the scoped cov + sc (in-scope counts) onto goldenidx at both writers, the page's twin goldenScore / attrsFromCov re-score from them identically, the Playbook skips a null cov, and diffCoverage never compares a scoped reading with a whole-catalogue baseline. Live 23 Sep 2026: Hobbycraft GB 82.7 → 90.9 (2 clothing products in 24,413), American Golf 78.7 → 80.4, Accessorize 90.6 → 90.1, YuMOVE 87.6 → 89.4, the apparel brands ±0.2. Docs: docs/LABELGUARD.md §9.9; ONE AUDIT COLOUR LEGEND (Ray, 23 Sep 2026, on the Playbook rail painting 76.7% completeness red: "golden record and bar color should be a bit more forgiving (< 70 red, 70-85 orange, 85 - 95 yellow, 95-100 green) - apply across all audit"): the /golden dial already used those four bands, but every other audit surface carried its own — the Playbook bar had no yellow and painted everything under 85 red, the attribute fill bars were green only at 99.5 and red under 50, the content-quality bars and the dossier card cut at 90/75, Feed Lab's attribute rows went red only under 40. ONE function, auditBand(v) (>=95 green, >=85 yellow, >=70 orange, else red), sits on /golden (scoreCol = dial, estate cards, content-quality score; the attribute and content-quality fill bars as b-green/b-yellow/b-orange/b-red), /feedlab (attribute coverage bar AND percentage), the dossier Golden Record card (grBand keeps its class names good/mid/warn/bad) and the Playbook rail (pbBand → good/mid/warn/crit); tools/test_bands.mjs LIFTS every copy and runs one boundary table through them (qa_gate/presync/validate). AI-READINESS STAYS ON ITS OWN LADDER, deliberately: its colours ARE its tiers (40/60/80 = Structured/Enriched/Agentic-ready) and a feed without the conversational six tops out at 79.3, so the 95-green legend would paint the whole estate red — the opposite of more forgiving; the harness pins airCol / scoreColor unchanged. THE SAME PANEL'S LIST WAS BROKEN: the Playbook's floors were written as fractions (0.99/0.9/0.6) while goldenidx stores percentages, so every real reading cleared them and 'Weakest first' only ever listed an attribute under 1%, printed as cov×100; the floors, bars and the 'worst N%' figure now read percentages and the harness fixtures use the real unit; A REPEATABLE ATTRIBUTE IS READ ACROSS EVERY COLUMN IT OCCUPIES (Ray, 16 Sep 2026: 'each product has at least four to five highlights, so why is it now showing as zero point?') — Monsoon GB ships FOUR repeated <g:product_highlight> elements per item, the Feed Lab XML parser expands them to g:product_highlight/(2)/(3)/(4), and the quality read resolved ONE column, saw one value and fired 'fewer than 2 highlights' on 100% of the catalogue, scoring 0/100 off a feed that meets the spec; findMultiCols resolves the whole slot set, the collector joins them (plus any ||| form) into the list every rule already splits on so NO rule changed, length is measured PER VALUE not across four glued together, the snapshot carries cols + perProduct and the row prints 'read across 10 columns — 4.1 values per product' (a count nobody can check is a count nobody should trust); live Monsoon GB g:product_highlight 0 → 91.1, content quality 79.2 → 91.4; the page passes the header to the collector (without one the old single-column read is preserved exactly) and tools/test_labelguard.mjs pins both paths; TAXONOMY DEPTH FOR GPC AND PRODUCT_TYPE (Ray, 17 Sep 2026: 'bringing PT depth (seperated by >) in the Content Quality for product type as well pls and GPC') — the existing shallow/single-level warn rules only said whether a path cleared a threshold, not how deep the catalogue actually goes; both attributes now carry depth:true on their QSPEC entry and the collector accumulates each value's chevron/slash depth via the SAME pathDepth read Product Type Guard's depthProfile and Feed Lab's gpcDepthAvg already use, so the number can never disagree with either sibling surface — avgDepth rides through attrQuality/qualityScore alongside avgLen, is allow-listed through the /api/golden/quality PUT sanitizer the same way cols/perProduct are, and prints on the row detail in both the broken-rule and the "every rule passes" states ('averaging 4 levels deep'); a bare numeric GPC id or an unseparated product_type value reads as depth 1, matching how Feed Lab already treats them; GPC'S "TOO BROAD" RULE KNOWS WHICH BRANCHES END (Ray, 17 Sep 2026, uploading Google's official taxonomy-with-ids.en-US export after a live scorecard flagged 'Apparel & Accessories > Shoes' too broad: 'if the final GPC (Shoes) which has no further clarification from Google, then that's already optimal') — GPC_LEAF2 (52 second-level branches Google ships with zero children, derived once from the export) is consulted before the shallow rule fires, so a genuine dead end scores as optimal while a one-level value (every top-level category branches further) and any two-level value that DOES have a deeper option still warn. Docs: docs/LABELGUARD.md §9.6; HOW MANY HIGHLIGHTS SPOTTED, OUT OF 100 (Ray, 17 Sep 2026: 'showcase what you have done ... how many highlights you have spotted within 100. So recommendation is from 6 to 10') — Google's spec is a 2-minimum with 4-6 recommended up to a ceiling of 100 (already the count-min/count-low rules above); this is a separate, purely descriptive read of the catalogue's SHAPE against Ray's own house target of 6-10, layered on top the same way PT Guard layers its 5-depth standard on Google's base spec — never replacing the base rule, never inventing a pass/fail threshold Ray didn't give (he named a range, not a target coverage %, so the card states the observed share rather than judging it). qualityCollector buckets every product's highlight COUNT — EXACT since 18 Sep 2026 (1/2/3/4/5/6+, the POPULATION keys) — and reports the SHARE of the catalogue in each (hlDist); rendered on the product_highlight row as the POPULATION TABLE (popCard: one row per bucket, label · bar · %, the PT depth card's own format — Ray, 18 Sep 2026: 'the table breakdown for highlight population (1, 2, 3, 4, 5, >5)') with 6+ marked as 'our target' and a footer separating Google's 4–6 (share at 4+) from the house 6–10 (share at 6+) — deliberately NOT the page's existing vertical .thist/.thb title-length histogram, which turned out to render every bar at 0px height (a pre-existing, unrelated CSS bug — .thb never gets an explicit height so its children's percentage-height styles can't resolve; left alone since fixing it properly is a separate task); hlDist allow-listed through the /api/golden/quality PUT sanitizer with five fixed bucket keys, each independently clamped, same discipline as cols/perProduct/avgDepth. Docs: docs/LABELGUARD.md §9.6; A DUPLICATE IS A GROUP (Ray, 16 Sep 2026, reading Monsoon GB: 'the issue you highlighted — titles duplicated across products — is not correct') — the COUNT was right (76 of 9,664 products across 35 shared titles, verified against the live feed: real products with different PDPs carrying an identical title) but the EVIDENCE was unreadable, four different-looking titles under one finding with nothing saying each appears twice, which reads as a false positive; the collector now keeps each repeated value with its repeat count and the ids carrying it (groups [{v,n,ids}] biggest-first, 4 groups x 4 ids, vals = distinct values shared) and names a value ONCE instead of re-pushing it per repeat, the page renders '×4 <title>' over its product ids, the hit column reads '76 products / 35 values' and a caption states the test (one whole value, case-insensitive); THE SCAN SHOWS ITS PROGRESS (Ray, same day: 'ensure this scanning is high quality and takes as long as needed … allow the user to see a progress bar indicating how long it will take'): nothing sampled or cut short, every product scored — qualityRun hands its caller a progress RECORD (phase · bytes read · bytes expected · products scored) not a sentence, the band draws % · MB of MB · products scored + an ETA off the rate measured so far (no guess under ~1.5s), the size comes from x-feed-bytes which the feed proxy forwards from the upstream XML feed (informational — NEVER content-length, which truncates a decoded stream), and a sheet export or a feed outrunning its declared size falls back to an indeterminate bar saying 'size not declared' rather than a fabricated percentage; VARIANT-AWARE WHERE GOOGLE IS — the title rule counts every repeat (Google asks titles to carry 'distinguishing details of each variant') while the description rule counts only copy reused across DIFFERENT item_group_ids (boilerplate: 'describe only the product itself'; variants sharing copy is expected and reported as context), the same call the Feed Lab pillar makes — live Superdry GB 99.7% → 81.0%, 4,858 products across 1,136 values correctly set aside, every group labelled 'different products' or 'variants of one product'. Docs: docs/LABELGUARD.md §9.6; TWO SCORES, TWO QUESTIONS (Ray, 16 Sep 2026: 'definitely a discrepancy between the scoring of content quality versus AI readiness … description for Superdry GB 60.1 but AI readiness 89 … taxonomy depth 49 whereas product type / GPC is almost 100 … is there a different scoring matrix for each? How do we bring this together and flag IF there is different scoring logic between the two sections'): a reconciliation band under the AI card — content quality scores PRODUCTS against rules Google STATES (a floor: disapproval risk / stated guidance unused), AI-Readiness scores the feed on the agentic ladder (a ceiling, blending coverage+depth+uniqueness per pillar), both off the SAME single read of the SAME products at the SAME moment, so a gap is a finding not a bug; RECON maps five field pairs (title↔titles, description↔descriptions, gpc+product_type↔taxonomy, colour/material/pattern↔attributes, product_highlight↔ai), each row carrying both live numbers (worst-breaking rule vs the pillar's own summary), the Δ, what each side measures and — at Δ≥15 — the plain-English cause, sorted widest-gap-first with a header count of the fields that read differently; footer states which score governs which decision (quality = what to fix and brief, AI = what to sell); prints with the scorecard (body.pdf restores the ≤900px grid-column stacking or the measured sheet is shorter than the printed document). Docs: docs/LABELGUARD.md §9.8; AI-READINESS ON THE SCORECARD (Ray, 16 Sep 2026: 'bring in the AI readiness score on the feed lab section … the most important item is the AI readiness score'): the SAME feedlab_engine audit() /feedlab calls, run on the SAME stream as the content-quality read — one fetch, two readings, identical numbers to /feedlab; rows sampled to AUD_CAP 30k with rowTotalEstimate so counts scale and memory stays bounded; packAudit trims to what the card renders and the worker re-validates the shape (pillars <=8, numbers clamped) + puts air/airTier on goldenidx; card = the Feed Lab visual language (conic ring coloured by band, Tier N pill + '+N points to Tier N+1', four-rung ladder with YOU ARE HERE, eight weighted pillar cards with dots + rationale, weighted-not-average footnote); TITLE ANATOMY (MASK spectrum + length histogram, 80-120 window marked) nests under the g:title content-quality row; PILLAR HEATMAP under the pillar cards (Ray: 'the pillar heatmap is great too') — this brand's analysed markets x the eight pillars, Feed Lab's colour scale, weakest cell across the brand ringed + named in the subtitle, row click opens that market; reads goldenidx airP (per-pillar scores written by the worker), an unanalysed market shows '—', appears from the second analysed market, and a finished analysis mirrors onto its own estate row what the worker writes so the feed joins the grid immediately; prints with everything else — body.pdf pins the card's desktop geometry since the <=900px rules would stack the ring above the ladder and make the measured sheet shorter than the printed document; whole document ~2 A4 lengths on one sheet, check_grpdf asserts score+ladder+pillars; SAME INDUSTRY PROFILE AS GOLDENSCORE (Ray, 17 Sep 2026, YuMOVE/Pet Care screenshot, Attribute completeness scoring 30/100 on empty apparel fields: 'make sure all scoring (AI readiness, content quality) always refer bakc to the industry best practice that had been set') — the nested audit() call and every LGQ.qualityScore(...) call site on this page now pass profileForC(client)'s expected/waived through (feedlab_engine.js's attrCounted: cond-tier color/size/gender/age_group/item_group_id excluded unless expected, rec-tier material/pattern excluded only when waived; qualityScore(snap, profile) drops a waived QSPEC attribute entirely), and /feedlab's own front-door reading fetches /api/golden/profile directly (profileForFL) so the two can never disagree. Docs: docs/LABELGUARD.md §9.7; PDP RECOVERY SCAN (Ray, 14 Sep 2026: 'missing data can be sourced from the PDP … take one URL linked to a product PDP to scan for any information for missing attributes … a button to scan, a pop-up module scanner'): 🔎 PDP on every non-required row + 🔎 Scan PDPs on the scorecard open a pop-up that samples the live feed (docs/pdp_engine.js sampler — rows BLANK in the focus attribute first, 10/25/50), fetches each product page through GET /api/golden/pdp/fetch (NO open proxy: the URL host must equal the feed's own product host learned from the feed head, cached in pdphost:; honest UA FeedSparkPDPScan; 9s; 403/429/503 = blocked → 'ask the client to allowlist the scanner') and runs the engine IN-BROWSER (the worker never parses a page): JSON-LD → OpenGraph product: meta → microdata → dataLayer → the visible 'Fabric & Details / Composition & Care / Key features' text (composition → dominant fibre, pattern vocab, care/fastening/neckline rows → section:attribute:value product_detail, feature bullets → product_highlight, size-guide/ingredients links → document_link, FAQPage → question_and_answer) — every value carries its source + the page line; identifiers (gtin/mpn/size) ONLY from the variant that IS the feed row or a single-SKU page (a page-level barcode on a multi-variant page is another SKU's), retailer sku = mpn only for own-brand, recommendation blocks = a related_product SIGNAL never a value; optional Tachyon pass over the details excerpt (5 pages per /api/claude call, strict JSON, a value without a VERBATIM page quote is dropped, vocab enforced, never identifiers, rules win); result = recovery matrix (blank in feed · found on PDP · rate · example + evidence · signals apart) + estimated Golden Record uplift + exits ⬇ supplemental CSV (id + g: columns), → Brief (plantask attr=pdp files 'Golden Record PDP Recovery - <Brand> <MKT> - MMYY'), ✉ Ask client (evidence-led proposal on the askdraft rails); sample stored per feed (PUT /api/golden/pdp → goldenpdp:<c>:<m>) and reopens from the scorecard chip. Probe truth 14 Sep 2026: Accessorize/Monsoon/Superdry/YuMOVE/American Golf fetchable; Reiss (Akamai), Schuh, Hobbycraft (Cloudflare) block server fetches. Harness tools/test_pdpharvest.mjs (fixtures shaped on the live pages + worker allowlist parity) in qa_gate/presync/validate. Docs: docs/LABELGUARD.md §9.5; EVERY AI-READINESS TILE VETTED, ONE DEPTH, NO SUBTEXT (Ray, 17 Sep 2026: "taking Schuh GB. product type is after scan for content quality; it says average 3.3 level depth while AI readiness in structured detail tiles only says product type 2.2 level depth … vet every single tile of AI readiness against what the current industry or high‑authoritative platforms such as Google, ChatGPT, or Claude recommend, and remove all the subtext underneath … Review every single attribute scoring logic and pop up there in these 2 sections"): the tile's "levels deep" was a COUNT OF FILLED KEYWORD SLOTS (product_type(2..10)) with the category column itself dropped — feedlab_engine's slots() started at (1) while the XML parser names a tag's first repeat BARE, so on every FeedHero XML feed the primary g:product_type, the first product_highlight and the first additional_image_link were all missing from the audit (Monsoon's four highlights read as three); slot one is now the bare column (bare and (1) are ONE slot), the primary product_type resolves exactly as labelguard's KEY_ALIASES and its chevron depth comes from pathDepth ported verbatim, and tools/test_feedlab.mjs pushes the same rows through qualityCollector and audit() and asserts the two avgDepths are equal (78 assertions). Every pillar vetted against the published specs (docs/FEEDLAB.md §5): GPC credit knows a numeric ID and Google's 52 terminal two-level branches (GPC_LEAF2 duplicated into the engine, parity asserted), condition (optional for new goods on Google + OpenAI) left Identity & trust and link joined it, https left Media richness (Google accepts either; img-http finding gone), product_detail (Google names it beside highlights for AI surfaces, answer 9218260) joined Structured detail and product_type depth left it (Taxonomy owns it — nothing counted twice), description length is Google's 160–500 not a house 300, title length Google's 30/70/150 with MASK 80–120 kept as the labelled house target; weights unchanged so the 79.3 ceiling holds. THE SUBTEXT IS GONE: .psum under every tile, the AI section's explainer + footnotes and the content-quality explainer on /golden, .psum on /feedlab — a tile is a name, a number, a bar and its weight, and CLICKING IT (or the row's ⓘ, or either section's ⓘ Scoring logic) opens the scoring pop-up: the formula (FeedAudit.BASIS — ONE object in the engine so /feedlab and /golden can never explain a number two ways), this feed's live components (pillar.reads, packed by packAudit and allow-listed by the worker's /api/golden/quality PUT), every rule for a content-quality attribute with severity + hit rate + cost (LGQ.qspecOf), the Google / OpenAI / Anthropic page each threshold comes from, and a box naming anything that is FeedSpark's own standard rather than a platform's. Docs: docs/LABELGUARD.md §9
GET  /kwcal (+ GET|PUT /api/kwcal) → Keyword Optimisation Calendar: each brand's shared marketing calendar (lanes: campaign / moments-location / moments-studio / sale) drives a month-by-month AI keyword-optimisation schedule — every moment derives a KW task ~3 weeks ahead (LEAD_DAYS=21) with draft keyword themes; ⚡ Measure impact streams /api/feed/proxy through the Feed Lab parser IN-BROWSER and counts SKUs matching each moment's terms (title+product_type substring; sale windows are brand-wide = whole catalogue; per-event mkt honoured, e.g. Black Friday USA→us); → Brief deep-links /workflow?brief= (cat keyword); status chips planned→intake→briefed→live→done; store = kvmerge per client key (events + measured), Reiss seeded in-page from the brand's shared Jul–Dec 2026 calendar (SEED applied only when KV lacks the client); v3 REDESIGN (Ray): FS-STANDARD roadmap layout (month sections + uniform cards — never mirror the client's slide; their calendar links per brand via calImg and opens SIDE-BY-SIDE 🗂 for transcription cross-check); v4 DECLUTTER (Ray, 11 Sep 2026: 'it looks quite cluttered … the calendar and the hours resourcing shouldn't be in the same zone … the rolling bar for the calendar doesn't look good … hours and resourcing could be displayed more intelligently') — three STRUCTURAL changes, not styling: (1) the months WRAP — .road is repeat(auto-fill,minmax(250px,1fr)) with align-items:start, so the whole roadmap is on screen, the horizontal scrollbar is gone and each month card sizes to its own content (the old grid-auto-flow:column timeline is retired; its marker now pins the wrapping grid); (2) RESOURCING LEFT THE SIDE RAIL — it was stealing a quarter of the calendar's width to list every optimisation AGAIN (name + date + a dropdown) next to the cards that already said it. It is now its own collapsed band BELOW the board (#hoursrail, .hband, state in localStorage fcc-kw-band) answering the two questions the list never did — 'who is carrying it' and 'when it lands' as per-person and per-month HOUR BARS, with the rule-of-thumb line as a footnote; the side rail now holds only the 🗂 client-calendar pane, so .split.two is gone; (3) the CARD carries four facts, not eight — name, ONE muted meta line (date · scope · hours · ✦themes, all inline and clickable), the state chip and an OWNER INITIALS CHIP (ownChip → ownOpen menu, so assignment lives on the board and the band never duplicates the task list). The lane became the card's coloured LEFT EDGE plus one legend line listing only the lanes in use (laneKey/renderLegend: campaign=orange, location=navy, studio=grey, sale=green) instead of a lane word on every card; the ⠿ drag grip and ✦ glyph are gone (the whole card drags); scopeBtn shows the product COUNT with the PT/text/condition breakdown in its tooltip; the WF chip is 'STAGE · COURT' on one line (the 'WF · ' prefix moved into the tooltip) so it stops wrapping in a 250px card; and the two explanatory paragraphs were cut to one line each; v5 ROLLING WINDOW (Ray, 14 Sep 2026: "where is December and onwards? Please show six months from the current timeline so I can track, drop, and plan ahead"): the board is a PLAN, not the span of whatever moments exist — monthRange used to run earliest-moment → latest-moment, so Accessorize stopped dead at Nov, the month its source calendar ran out, and there was nowhere to drop December. It now starts at THIS month and runs winLen() months whether or not they carry work (an empty month renders as a drop target); the window only ever GROWS — a moment scheduled before this month, or dragged past the horizon, is never hidden, and the span line says when and why it widened. Plan-length chips 6/12/18 months (WIN_OPTS, default 6, localStorage fcc-kw-win) sit under MARKETS, hidden on the '*' overview; the current month carries a "now" pill + orange border (.mo-now) and months already gone recede (.mo-past); an empty brand still gets its six months, with the ＋ Add event hint ABOVE the board rather than replacing it. WIN_MAX=36 guards a mistyped year — a moment further out than that is NOT rendered (400 sections would hang the page) but named in a red line above the board, each name a button that opens its date editor (outsideWin → data-fix), so the typo is fixable from where it is reported. The client PDF and the '*' overview still print only months that carry moments — empty planning months are internal. scope = real product_type values via searchable MULTISELECT picker (⚙ Set scope — TWO-COLUMN WORKBENCH since Ray's 11 Sep 2026 note 'this selection is too narrow to visually see': 920×680, browse/search fills the LEFT column at full height (~424px, nine rows — it used to be flex-squeezed to three) while the right rail holds everything that DESCRIBES the scope (selected, text filters, attribute conditions) so those can never steal the list's height again; a row is two lines — leaf in bold, ancestor path as a muted breadcrumb, count in its own fixed column that can never sit on the name; browsing is grouped by top-level category (collapsible, per-group type count + SKU volume + select-all/clear-all, siblings clustered by branch), searching switches to a flat ranked list with matched words highlighted and an empty state that points at text filters; ↑↓ move a cursor, Enter ticks, Esc closes; ✨ Suggest reports what it added — word-search over the ptidx built by ⟳ Sync product types, one stream per market, 400+-PT-friendly, ✨ Suggest-from-themes preselect matching PT NAME or per-PT word-bags crawled from titles+descriptions during sync (idx.bags); MARKET-BASED: market chips from /api/feed/markets, scopes stored per market (e.ptsm{mkt:[pts]}, legacy pts=gb), volumes/sync/picker/brief all follow the board market, pinned e.mkt wins); volumes are EXACT PT counts (sale windows brand-wide); scope has TWO combinable methods — PT multiselect AND free-text filters matched against title+description (picker text section: Enter adds, themes as one-click +chips, per-term SKU counts, e.txsm{mkt:{qs:[{q,n}],uni}} where uni = UNIQUE SKUs matching PTs ∪ terms so combined totals never double-count; counts run off an in-memory per-SKU cache window.__skucache built during ⟳ Sync and auto-streamed once on demand via ensureCache — never persisted to KV); KEYWORD SATURATION audit: optimised keywords land in numbered product_type fields (g:product_type2..10 — ALL FeedSpark feeds use this; normKey forms product_type2 / _2 / |||N all detected, EXCLUDED from scope candidates since they are keywords not the category tree), saturated = ≥1 populated → always-on per-brand/market KPI '% Keyword saturation' (ptidx[mkt].sat{n,cols}, refreshed by both sync and ensureCache streams), overview rows badge '% keyworded', picker foot shows 'N not yet keyworded' for the scope, per-event lift stored (e.satm{mkt:{unsat}}) and the brief gains 'Keyword saturation (MKT): x% — N in-scope SKUs still to keyword; landing this brief lifts the brand to ~y%' (each event = a step toward 100%); the → Brief auto-fills everything (moment, land + brief-by dates, themes, PTs + SKUs + % of feed, text filters + unique total); client calendar side-by-side defaults to a git-bundled slide when KV has no calImg (CALSEED map ↔ worker /kwcal/cal/* Data modules from docs/calseed/ — Reiss planner seeded; in-page URL/upload overrides); SEEDED BRANDS: Reiss (shared marketing planner) + YUMOVE (Ray, 21 Sep 2026, sending the brand's own promotional calendar, 14 Nov – 2 Dec 2026) — unlike the other two this is a PRICING calendar, not a product one: it schedules discount mechanics per customer cohort, SUB (subscription) vs OTP/PAYG (one-time purchase, pay-as-you-go), so all seven moments are sale-lane and brand-wide by nature and the board asks for no product scope on any of them. THE FEED FACT, not just site wording: the DEFAULT PDP OPTION flips Subscription → Single Purchase on 27 Nov and back on 1 Dec, which changes the price the landing page leads with — a feed still advertising the subscription price across Black Friday is a price-mismatch DISAPPROVAL, so both flips carry that on the card, and the 1 Dec revert is named as the step most likely to be forgotten. Every column of the source matches the real 2026 calendar except a duplicated "30th Nov" (the sheet prints 30th/Sun then 30th/Mon; 30 Nov 2026 is the Monday, so the Sunday column is the 29th) — corrected, with the reason on the card, and the band starts read off the chart's coloured runs rather than printed dates say so, so an inferred date is never briefed as a confirmed one. + ACCESSORIZE GB (Ray, 11 Sep 2026: "still not seeing accessorize GB feed events/themes being populated") — the brand's own master calendar (ACC_ marketing_calendar.xlsx, retail Periods 1–3, 30 Aug–28 Nov 2026) is seeded IN THE PAGE, 15 moments at 5 per retail period, since an ingest that only exists as a downloadable JSON is an ingest nobody can see; row 48 "Key Focus" only runs to W4 so P2/P3 read off the weekly messaging hierarchy (R15 Overarching / R16 Primary / R17 Secondary / R18 Tertiary + web promos + homepage tiles), and the workbook's key-dates row is CARRIED OVER FROM 2024 (4/4 deterministic dates match 2024) so the seeded dates are corrected — the four moments that turn on a correction or on a date the client has not confirmed carry a `note`, now rendered into the card's tooltip (the Reiss seed had carried a note since v1 that nothing ever displayed). IMPORT TARGETING FIX (same ask, the root cause): importEvents honoured the JSON's own `client` only when `STORE[obj.client]` ALREADY existed, so a calendar for any brand without a KV record — every brand but Reiss — silently filed itself into whatever was ON SCREEN; it now resolves the named brand against the real roster (resolveBrand, case/space-insensitive) and REFUSES rather than guessing when the name does not match, pins a top-level `market` onto every event that names none, and lands the board on the brand it imported into with the status line naming it (IMPTGT) — "imported 15 events" over an untouched board was the same silence as importing nothing. rehomeImports repairs what the old guard mis-filed: import blocks carry brand-prefixed ids (IMPORT_HOMES), so an `acc_` event sitting on another brand's board can only have got there that way — moved home on boot BEFORE applySeeds (so a repaired brand is not double-seeded), saved once, idempotent; ▦ ALL-BRANDS OVERVIEW (selector option '*'): one view of every brand's schedule — month strips of task chips, global KPIs, per-task popover with → Brief / ⏰ Chase (Workflow deep-link, cat account) / ✉ Ask client (drafted mailto) / advance-status; stages auto-badge LIVE from /api/briefs when a pipeline brief's task carries 'AI Keyword Optimisation — <name>' (WF wins over the manual chip); ⬇ CLIENT PDF (Ray, 11 Sep 2026: 'a button to export into PDF so we can share with clients', then 'downloading a client PDF is actually not seamless, so it should look like a calendar in Google Gmail and fit on one page only'): the roadmap exports as a ONE-PAGE CALENDAR — browser print-to-PDF, no library, A4 landscape. The live board can't be printed (it's a horizontally-scrolling month grid carrying internal-only detail), so renderPdfDoc() builds a SEPARATE client-facing document into #pdf-doc from the same data and the @media print block shows only that: a compact header band (wordmark, brand · market · period, inline KPIs), then a GRID OF MONTH BLOCKS — each a real Monday-first day grid with every optimisation marked on the day it goes LIVE (the day takes the colour of its furthest-along optimisation), and a short agenda under the grid so the names stay readable at calendar scale (day · status dot · name + up to 3 themes · products · result pip), plus a status legend, the one line that replaces the old methodology card, and the Private & Confidential footer. FIT ON ONE PAGE is enforced, not hoped for: fitPdf() binary-searches the largest scale (0.45–1.8, so a sparse calendar GROWS to fill the sheet rather than sitting at 8px in a third of a page) whose laid-out height fits the page box and whose width does not overflow, then applies it as `zoom` — NOT transform:scale, which is paint-time so Chrome kept paginating on the untransformed height and silently produced two pages (a 44-moment stress case pins this). The .pd-* layout rules therefore live OUTSIDE @media print, scoped to #pdf-doc, so fitPdf can un-hide the element for a moment and measure the REAL layout; when they were print-only the measurement saw unstyled content and shrank the page to two-thirds. The month grid is repeat(auto-fit,minmax(330px,1fr)) and the agenda rows carry overflow:hidden — a nowrap name used to push each block's min-content to ~330px, which a 1fr column could not shrink below, clipping the last month off the sheet. CLIENT-SAFE BY CONSTRUCTION — the hours & resourcing rail (owners/effort), → Brief links, edit controls and the PT/text-filter jargon are never written into it, and the '*' all-brands overview cannot export (one client must never see another's roadmap). Internal stages collapse to Scheduled / In progress / Live / Complete — EVERY stage the pipeline can produce, since 22 Sep 2026 (Ray, reading his own Reiss PDF: "isn't cashmere/merino and leather & suede also 'Live' - so shouldn't the box be orange?"): they were, and CST simply had no `running` key — Workflow's "Test running ⏱", where an optimisation IS live in the feed being measured — so the lookup came back undefined and a ||'Scheduled' fallback painted both of his moments grey and left them out of the Live / complete KPI. Nothing failed; the document quietly told a client that live work had not started. The same shape as the KPI band's missing buckets (#474). `running` → Live beside `analysis`; the `live` key is the calendar's OWN legacy manual word that stOf can never return from a ticket, and an unreachable key is exactly what made the map look complete. cstOf(e) is the one reader and its fallback can no longer under-state: an unknown stage came from a TICKET, so somebody is working it and it reads In progress — only a moment with no ticket at all reads Scheduled. Harness tools/test_kwcal_stages.mjs (24 assertions) lifts STAGES out of FeedSpark_Workflow.html AND CST out of FeedSpark_KWCal.html — neither file can see the other, so it fails the moment they drift, including on a stage Workflow adds later — then renders the PDF with a ticket at all eight stages and asserts each day's colour and the KPI; in qa_gate/presync/validate. Colours are hard-coded so the PDF is on-brand from either theme; renderPdfDoc() runs on every render so a native Cmd/Ctrl+P prints the client doc too; CLOSED LOOP — moment ⇄ ticket ⇄ result (Ray, 11 Sep 2026: 'the status of each event or team is connected, changed and updated, and aligns with the brief inside Workflow … all of these events will be briefed specifically using the keyword calendar only … refreshed throughout the weeks and the month — including result'): every optimisation is briefed from THIS calendar and nowhere else, so each moment owns exactly ONE ticket and the board never guesses which — → Brief puts the moment's id in the deep-link payload (kw:e.id) and the Workflow composer stamps it on the saved brief (b.kw, threaded via window.__kwId: cleared in prefill, set by the deep link, consumed on save, kept through later edits), so the tie survives a rename, a market change and a re-brief; wfFor resolves strongest-evidence-first (stamped id → regenerated house task name → the moment name ENDING its task segment, never bare containment, which let 'Coats' capture 'Coats & Jackets'), newest wins on a tie and a foreign client's brief can never leak in. TICKETS RAISED OUTSIDE THE CALENDAR (Ray, 15 Sep 2026: "you're still not able to add any activities that Steven has actioned from his end — Cashmere/Merino and Gifting have been briefed successfully from his end and ASPL already picked up"): every rung above matches the CALENDAR'S OWN wording, so a ticket a colleague raised by hand in Workflow was invisible — the moment read "planned" while ASPL was already working it, and nothing anywhere said so. The tie is now resolved for the WHOLE brand in ONE pass (wfBuild → wfAll, cached per render by wfReset; wfFor/wfHow read it), rung by rung, so a brief is claimed by AT MOST ONE moment and an early moment can't greedily take a ticket a later one owns. A fourth rung reads the ticket's task as WORDS: sigWords strips filler, the brand's own name and bare date tokens, and nameScore is the harmonic mean of BOTH coverages (how much of the moment is in the ticket AND how much of the ticket the moment accounts for) — one-directional scoring let "Coats" steal the "Coats & Jackets" ticket, since the short name is 100% present in the long one. A keyword-shaped ticket needs ≥0.5 and a STRICT win; anything else (a Golden Record or Feed Fix ticket) must name the moment outright (1.0), so unrelated work can never drift onto the board. A ticket two moments fit EQUALLY ("Atelier refresh" against Atelier 1 and 2) is claimed by NEITHER — a confidently wrong tie is worse than an honest gap. Those, plus near misses, land in the UNLINKED STRIP above the board (looseStrip/looseKw: what the calendar could not place, with each ticket's stage and whose court), and one click (linkOpen) links a ticket to a moment — stored as e.wf, decisive above every rung, surviving a rename on either side, ⛓-unlinkable in edit mode. The stage chip's tooltip names the ticket, who briefed it and HOW it matched (HOWL). A briefed moment's chip is therefore LIVE FROM THE PIPELINE and renders as a LINK to /workflow carrying the stage plus WHOSE COURT it sits in (COURTM = Workflow's own stage→team map: AM / ASPL / Client) — the stage moves in Workflow, never on the calendar, so the old phantom click (which cycled a hidden e.st the render then ignored) is gone; only un-briefed moments keep a manual chip, and the overview popover swaps '↻ Advance status' for '🎫 Open in Workflow' once a ticket exists. RESULTS close the loop: /api/kwresults is Dino's scheduled result archive per brand × market × HALF-MONTH round, carrying no moment id — so periodWin parses the period token ('Aug II' + the email date, with the Dec-reported-in-Jan year roll) into its window and resFor ties a round to every optimisation whose live-by date falls inside it, which is the real relationship (one round reports a fortnight's work); the card gets a verdict chip → metric popover, the KPIs gain 'Results reported' + 'Not yet briefed', the overview badges 📈, and the client PDF prints the verdict per moment but the round's NUMBERS ONCE per round with a 'Covers: …' line, so a shared round never reads as several separate results — THE FIGURES ARRIVED 22 Sep 2026 (Ray, sending the print dialog: "downloaded clietn PDF is not print pls, download directly, also, add results in where test has recorded (right now silk & satin / denim are done and results are captured so ensure the download button always reflect the latest updated calendar)"): the agenda row carried a verdict pip and NOTHING ELSE, so a client read '▲' with no idea what moved or by how much — the one number the whole calendar exists to produce was the one number it did not print. pdfResults() adds a RESULTS REPORTED section, and the two kinds of result are never merged because they answer different questions: a read-out filed on a moment's OWN brief thread is that optimisation and nothing else, so it is named by its moment, while a fortnightly round reports everything that went live in its window, so its figures print ONCE under the fortnight's own label with 'Covers:' naming the moments that share them (the tie is on the LIVE-BY date, moment minus LEAD_DAYS, not the moment's own date). A result and its Covers line are ONE grid item — an auto-fit column break between the two put 'Covers: Denim' alongside a DIFFERENT round, which reads as that round's coverage and a client cannot tell it is wrong. Metric lines print as the report itself worded them, nothing re-derived or averaged, and a report with no parseable figure still appears, saying so rather than inventing one; dates use the document's own month names since en-GB's short form prints 'Sept' beside 'Aug'. AND IT DOWNLOADS IN ONE CLICK: window.print() always opens the browser's own dialog, so — the same route /golden took — exportPdf rasterises #pdf-doc with html2canvas and packages it with jsPDF (cdnjs, loaded on first use) and .save()s it, the page cut to the capture's aspect at A4-landscape width so it is one page BY CONSTRUCTION rather than by a paginator's opinion; printPdf() survives as the fallback for an unreachable CDN and is never remembered as permanently failed, so the next click retries. ALWAYS THE LATEST: the export re-pulls /api/briefs + /api/kwresults and rebuilds the tie (wfReset) BEFORE it builds the document — without it the file was only ever as fresh as the last 90-second poll, and the moment a client asks for the PDF is right after a result lands. AND IT IS LAID OUT ON A DESIGN SCALE (Ray, 22 Sep 2026: "redesign it so the headlines and the footer have a bit more margin and look more premium … Use an Apple design scale or design theory"): the margin was a BUG, not a preference — ⬇ Client PDF rasterises #pdf-doc ITSELF, so @page margin, the only margin this document had, never reached the downloaded file and it printed edge to edge; --pad gives the element its own page margin, which the capture therefore includes. On top of that the sheet is now TOKENS rather than nudged numbers: a 4pt SPACING GRID (--u, every margin/pad/gap a multiple, half-steps allowed) replacing the old 1/1.5/3/5/7/9/13px soup; a SIX-STEP MODULAR TYPE SCALE (20/13/10.5/9/7.5/6.5, every step ≥1.1×) replacing ELEVEN sizes several of which were a half-pixel apart and therefore no hierarchy at all; TRACKING set tight on large type and loose on small uppercase labels, which is most of why a headline reads as designed rather than merely bold; an ink ramp; and TWO WEIGHTS, 400 and 700 — not a compromise but the only honest option, since the page loads Lato 400/700/900 ONLY and a 500 or 600 would be SYNTHESISED, which html2canvas rasterises unpredictably, so the downloaded file would not match the screen. The month blocks lost their boxes (twelve bordered cards is twelve frames competing with what is inside them; the grid's own gap groups them) and the footer gets the most air in the document. THE TRAP THIS UNCOVERED: fitPdf's measurement override and exportPdf's capture override each RESTATED font-family and line-height inline, duplicating the stylesheet — so the moment the document's leading changed they measured and captured a document that no longer existed, the scale chosen was too large (735 against a 718 box) and the print fallback would silently paginate. Both now override position and width ONLY. Harness tools/test_kwcal_pdf.mjs (Playwright, 38 assertions: one click and no dialog, the page geometry, the re-pull, the own-ticket vs round split, the Covers membership recomputed independently of the page, the grid-item grouping, the CDN fallback + retry, then the grid, the scale, the two weights, the page margin, that neither override restates typography, and that the scale fitPdf chose REALLY fits the box it measures against — the assertion that fails on the pre-fix page) in qa_gate/presync/validate. THE BAND ADDS UP, AND THE MARKET LIST IS THE ONE THIS MODULE CAN WORK ON (Ray, 21 Sep 2026: "This section on keyword calendar is so messy … the numbers also don't make sense. Why the 100% keyword saturation? … the list of market should be cleaned up"): the stage tiles covered intake/briefed/progress and live/done/confirmed while COURTM's own vocabulary ALSO has running, analysis and blocked — so an optimisation at Test running or Analysis, which is where most of Reiss's sit, counted as briefed (leaving "not yet briefed") and appeared in NO tile: 14 scheduled over 1 + 1 + 9 = 11, three simply missing. The four buckets are now a PARTITION of every stage stOf can return, named by the COURT the work sits in (Not briefed / With ASPL / With the AM / Complete), they always sum to the total beside them, and a stage nobody has taught the band about surfaces as "Other stage" rather than being dropped — the exact failure this replaces. The band is TWO LABELLED ROWS (Plan · Measures), each filling its own width, instead of one nine-tile auto-fit wall that orphaned two tiles onto a second line; the all-brands overview shares the container so it puts the flat class back. MARKETS: /api/feed/markets serves every WIRED feed, right for Feed Lab and Overlays and wrong here — optimised keywords land in the numbered g:product_type slots, a field the Meta catalogue spec does not have, so on a `-fb` market ⚡ Measure impact, the scope picker and the saturation KPI all measure something that cannot exist (Reiss listed 42 chips of which 14 could never be keyword-optimised; same rule /ptypes and /volume already apply). They are filtered out and COUNTED on screen ("10 Meta feeds not shown"), never silently removed, and the remaining list folds at 12 behind one button that can never fold away the board's own market. SATURATION SHOWS ITS WORKING: the tile asserted a headline with its counts in a tooltip and no date, so a reading taken weeks ago read as today's and a suspicious one could not be told from a real one — it now prints n of total, names how many keyword columns it read, dates the reading (warning past 14 days) and, when every SKU carries exactly ONE extra product_type value (sp≥99% on a single column), says CHECK rather than asserting a finished job: keyword injection fills a varying number of slots per SKU, so that shape is far more likely a second CATEGORY path than a keyworded catalogue. Stated as something to look at, never as an error — only the feed can settle it. Harness tools/test_kwcal_kpis.mjs (the band's bucket arithmetic LIFTED from the page by name, so a stage can never fall out of the partition again; records the OLD sum verbatim) in qa_gate/presync/validate. LIVE REFRESH: liveTick re-pulls /api/briefs + /api/kwresults every 90s and on tab-visible, re-rendering only when the signature actually moved and never while a scope picker or popover is open. Harness: tools/test_kwcal_tie.mjs (pure node, lifts the functions out of the page by name) pins the precedence, the collision guard (including the hand-raised-ticket rung and its equal-fit refusal), the window maths, the rolling plan window (six from now, grow-only, the WIN_MAX guard), the Accessorize seed shape (15 moments, 5 per retail period, notes on the corrected dates), the SCHUH FY27 seed and the mis-file repair — wired into qa_gate, presync and validate.yml. + SCHUH (Ray, 22 Sep 2026: "Schuh's marketing calendar - import anyhting from Aug/26 onwards") — the brand's own Customer Marketing Calendar FY27, a DAY-COLUMN grid (col B = Sun 28 Dec 2025, one column per day to 30 Jan 2027) with each activity a merged block. 40 moments seeded in the page: everything whose block touches 1 Aug 2026, dated at its own start, so the five that opened earlier (both Back to School campaigns, the bundling offer, Nike Kids, Kickers) are KEPT and say so rather than dropped for starting in July — Back to School covers the whole of August. Unlike the Accessorize workbook nothing needed correcting: all 57 W/C Sunday dates and all 399 day letters agree with the column mapping and 22 of the 23 weekdays stated in words check out (the exception is an END date on the Jan-2027 student promo, flagged on that moment). WHERE A CELL NAMES ITS OWN DATE ('20.8', '7.10', '16th Sept.') that date wins over the merged block, which is a content window drawn to fit the row — the five that differ report both. FOUR LANES = FOUR ROW-GROUPS: sale = PROMOTIONS (9), campaign = SCHUH CAMPAIGNS · Full Price (4), studio = BRAND PARTNERSHIP CAMPAIGNS + NEW BRANDS (20), location = the calendar's own weekly demand forecast (BRAND SEARCHES + CATEGORY SEARCH TERMS) consolidated to ONE moment per week rather than one per brand word (7). Two sale moments are deliberately NOT brand-wide — 'up to 30% off selected lines' never says which lines, so it is left unscoped for the AM rather than counted as the catalogue — and Black Friday sits on Schuh's own 22 Nov start, five days before the cultural date, because the feed follows the retailer. THE FINDING: the forecast lane is mostly out of phase with the launches (Crocs peaks six weeks early, Dr. Martens twice with the campaign between, Puma peaks with no activity anywhere in FY27), which is briefable on its own since feed work does not need the campaign live. A UK & ROI calendar (£/€, ROI bank holidays) — nothing in it is German, so no moment pins a market. LANE NAMES ARE A BRAND'S OWN WORDS, DISPLAY ONLY (LANE_NAMES): 'Moments · Studio' is Reiss's vocabulary and the wrong phrase for a Nike drop, so Schuh's board reads Schuh campaign / Brand launch / Search peak / Promotion while the keys, colours, stored events and tie-matching never move, and a brand with no map keeps the generic words. Ingest record: ops/calendars/
GET  /volume (+/api/volume)     → Product Volume module (Ray, 9 Sep 2026: 'document the daily product volume … 300 new products coming in and 900 going out … like Merchant Center'): GMC-style daily in/out churn per brand×market for AMs — the xml-scan agent captures each feed's full SKU id set as 'id|category' lines (category = first chevron level of primary product_type; VOLCAP 40k, over = truncated → rows-only) alongside every snapshot; the worker's volTrack (rides only CONFIRMED scans — catastrophic holds never write) keeps the day's CLOSING set in KV volids:<client>:<mkt> (same-day scans replace it), pins the PREVIOUS day's close as volbase:<client>:<mkt> on the first scan of a new UTC day, and EVERY scan of the day set-diffs today's latest set against that baseline into volhist: entries {d, rows, in, out, cats top-12} (cap 60 days) — RUNNING close-to-close (fix 14 Sep 2026: the first-scan-only diff read 0/0 forever for feeds that regenerate in daytime, e.g. Accessorize; the day's bar now builds through the day and settles at the last scan), so intraday flapping never double-counts; GET /api/volume?client=&market= serves hist + the labelhist rows-per-scan series + tracked kind (xml|sheet). The page (docs/FeedSpark_Volume.html): mirrored daily bar chart (in above / out below one zero axis, validated palette light #2563EB/#ED6F0B · dark #4C82E0/#C67B28, hover tooltip, day-click scopes the Category-movers table), total-SKUs trend, ⊞ table view, and the 'How these volume movements are tracked' methodology card (Ray's ask). Sheet-backed feeds show rows-history only with an honest XML-needed note; history accrues from first scan (no backfill — id sets didn't exist before 9 Sep 2026); NEW-PRODUCT ARRIVALS by first-seen date (Ray, 15 Sep 2026: 'there's a field called fs:date_of_birth, which signals the timestamp when the product ID first appears … forecast and approximate how many products arrive per year per quarter per month for all my accounts … just use Shopping feed to access this only'): FeedHero stamps every product with <c:fs_date_of_birth> (custom c: namespace → key fs_date_of_birth after the prefix/type strip), so docs/arrivals_engine.js (UMD FeedArrivals, served at /volume/engine.js) reads new products per MONTH / calendar QUARTER / YEAR straight off the feed with no dependence on when monitoring started — SURVIVORS ONLY (a month counts what is still live today: recent months complete, older months lower bounds) and the forecast = the plain run-rate of A YEAR of COMPLETE months ÷ 12 (× 3, × 12), never a fitted curve — RE-BASED 18 Sep 2026 (Ray: "The logic for new products a month is not accurate. For example, Monsoon had 10,298 divided by 12. It's not 1,534. Let's use the logic of the current calendar year, looking back one year and divide by 12"): it was the mean of the last 3 complete months, which reads whatever the catalogue did most recently rather than what it does in a year — Monsoon GB's Jul and Aug (1,741 / 2,221) run three to five times its spring months, so × 12 forecast 18,408 arrivals a year onto a 10,298-product catalogue, the whole shop twice over, on a figure that goes to a client in a quote; twelve months average the seasonality out, and the same feed now reads 604. DIVIDED BY 12 whenever there IS a year to look back on — a feed monitored for five months has no year and dividing those five by 12 would under-read by more than half, so it divides by the months actually observed and the basis SAYS how many (short of a year is stated, never padded out with months of zeros); an empty month INSIDE the window is a real zero and is divided by. m3/m6 are still reported and /volume prints the 3-month average beside the run-rate, because a recent burst is worth SEEING — it just is not what the year is priced on. ONE engine, so /volume, the Monthly update card, the AI Quote and the Playbook can never quote two different rates; labelguard's shared xmlCollector histograms the field (DOB_KEY) on the SAME stream as the id set, so the 4x-daily xml-scan agent's confirmed snapshots reach dobStore via volTrack (voldob:<c>:<m> record {t,rows,n,bad,m{YYYY-MM:n},min,max} + ONE estate index voldobidx, last 36 months per feed); Google Shopping feeds ONLY — -fb Meta feeds are refused server-side and the page shows the rule; /api/volume carries `dob`, GET /api/volume/arrivals = every Shopping feed (roster ∪ index, never-scanned feeds listed honestly), POST /api/volume/dobpush = the page's ⚡ Scan now (streams /api/feed/proxy in-browser through the Feed Lab parser + FeedArrivals.dobCollector, ACT volume-arrivals-live) so nobody waits for the next pass. Page: 'New product arrivals — by first-seen date' card (coverage / last full month / 3-mo run-rate / last-12 KPIs, ≈ per month · quarter · year band, 24-month bars with the current month translucent, per-quarter + per-year tables, coverage <90% warning) + 'Arrivals across all accounts' estate table ranked by run-rate with sums (row click opens the feed) + methodology step 6. The AI Quote's new-product forecast now PREFERS this run-rate over a few days of churn (basis 'dob', churn stays the fallback). Harness tools/test_arrivals.mjs (engine, collector via real XML stream, dobStore lifted by name) in qa_gate/presync/validate
GET  /overlays (+/api/overlays, /api/overlays/scanpush, /overlays/engine.js) → Image Overlays module (Ray, 10 Sep 2026: 'when you scan the feed for each client that has overlay service on, especially the Meta feed, the image link starts with dashboard.feedspark.com — render the type of overlay live on each client and render the image; the clue is in the URL string'): docs/overlay_engine.js (UMD, shared by the xml-scan agent, the page's live scan and tools/test_overlays.mjs — wired in qa_gate + validate.yml) reads every image_link + additional_image_link; a FeedSpark image host (dashboard./lia. feedspark.com, feed5.com) = overlay service ON; classifyOverlay decodes the TYPE off the URL string — engine path (image-creator/<project>/<script>.php: image_process_products_lifestyle = 'Product × Lifestyle split', dynamic-overlay-engine/<brand>/image_process_engine = 'Dynamic overlay engine · frame <asset>', image_process_subscription_v1 + show_price = 'Subscription v1 · price tag', lia …/feedspark-meta-dynamic-v2/meta_catelog_call = 'Meta dynamic template v2'; unseen scripts get a DERIVED label, never hidden) + recipe readouts from the params (img_url_left/right = the two composed sources, img_ver/ver = version, tag colours/style, frame asset, template id/hash); overlayCollector counts per type + keeps 16 rendered samples {id,ti,link,url,src,ver}. The 4x-daily xml_scan.mjs captures `ovl` on the SAME stream as snap/vol and the worker's overlayTrack (confirmed scans only, via applyPushedSnapshot) stores overlay:<c>:<m> (whole capture) + overlayhist:<c>:<m> (counts per scan, ≤120) + the one-read estate index overlayidx; GET /api/overlays = full roster (feedRoster) ∪ index so never-scanned feeds show honestly, ?client=&market= = capture + hist; POST /api/overlays/scanpush = the page's ⚡ live in-browser scan (streams /api/feed/proxy through FeedAudit XML/CSV sniff + the collector, works on sheet feeds too, ACT 'overlay-scan-live'). Page docs/FeedSpark_Overlays.html: estate table (Meta first, coverage bars blue=Meta/orange=Google, type chips, ⚡ per row + 'Scan all Meta feeds' queue), KPIs, per-type cards (label, family chip, version chips, decoded recipe, URL template, lazy gallery of the LIVE overlay images), lightbox = composite vs the source inputs (left/right) + recipe + product link, coverage trend per scan, methodology card. Estate truth 10 Sep 2026: Monsoon gb-fb 329/1593 (products×lifestyle split) + YuMOVE gb 85/85 (subscription tag ×60, dynamic engine ×15, lia meta-dynamic ×10); every other wired feed serves the client's plain CDN image
GET  /workflow?pb=1 (+GET /api/playbook) → Playbook — THE ACCOUNT COCKPIT INSIDE WORKFLOW (Ray, 16 Sep 2026: "Let's elevate the workflow like a playbook … with an icon next to Build Log … Delete the separate playbook module and incorporate it into the workflow as the right-hand panel", then "only within the workflow, the left-hand panel will show the retainer pop-up … right now when you hover over the retainer pop-up, it covers all the tasks and brands in the workflow, making it less interactive and efficient … Make sure it isn't too text-heavy"): the standalone /playbook PAGE IS DELETED (301 → /workflow?pb=1), its nav icon now sits beside Activity/Build Log on every page and opens the rail IN PLACE, and it is no longer separately grantable — it rides the `workflow` grant (MODGATE now strips a query string before reading a link's slug). Workflow opens onto TWO RAILS that PUSH the page rather than sit on it (one at a time under 1360px; a full-width sheet under 900px; edge handles keep the controls on the phone): LEFT = the retainer read-out DOCKED — docs/hours_widget.html renders its usual body into #fcc-hrs-dock when a page mounts one and then HOVER OPENS NOTHING, a click loads that client into the panel (the popover's inner CSS became the shared .fh-body, so there is one read-out with two homes); RIGHT = the Playbook, three sections, every row ONE line — a glyph, a name, a bar, a number — with one muted sentence per section: (1) DOING WELL · NOT DOING = the brand's plan tasks through the strategy taxonomy, split Landing (3+ done, 80%+ through) / Stalling (2+ tasks under 60%) / Not on this account (a play ≥2 peers actually run, 2+ tasks each, that this one carries nothing of — one peer with one task is not a practice; BAU admin never cross-pollinates onto a client call), each gap carrying → Brief; (2) NEW PRODUCTS = /api/volume/arrivals' fs:date_of_birth histogram as a share of the live catalogue, 10–20% HIGHLIGHTED as a collection landing (Ray's rule verbatim; >20% is the same signal louder, highlighted as a major drop rather than falling off the top of the band), read off the LAST COMPLETE month since a part-month makes every account look like it fell off a cliff, and an unscanned feed says "not scanned" never 0%; (3) GOLDEN RECORD = /api/golden/estate (goldenidx) per market, attributes MISSING vs attributes PRESENT BUT THIN kept apart as the different failures they are, worst-first against Google's tiers (req <99%, cond <90%, rec <60%), the conversational AI six never scored as a failure. Both rails follow ONE account (one client selected in Intake moves them); refresh 120s + on tab-visible while open. ⚙ Tune keeps the taxonomy editable and TEAM-shared through /api/state ns pbtax. Harness tools/test_playbook_panel.mjs (lifts the engine between its PBENGINE markers) in qa_gate/presync/validate. Docs: docs/PLAYBOOK.md NEW-PRODUCT VOLUMES AS THEIR OWN PANEL (Ray, 17 Sep 2026: "the right-hand panel … is also considering adding product volumes, so we can raise when a certain amount of time passes and a new product comes into the feed and need to be looked at / briefed directly into Intake for optimisation such as (titles, keywords) - showcase product type breakdown", then "bring the same new product volume data into the Workflow module as well and appears on the right (almost like Retainer) which doesn't cover any text"): the right rail is now TWO TABS — 🧭 Review (the three sections) and 📦 NEW PRODUCTS, the volume data given the whole rail (fcc-ck-tab, per device). THE RAISE is a BACKLOG, not a calendar cohort: ageing "the last complete month" is an artefact (that cohort is 0–30 days old BY DEFINITION, so a 30-day threshold fires one day a month), so pbRaise counts every arrival month AFTER the month of the brand's last title/keyword plan task and ages from the OLDEST of them — default 21 days = the FCC's own keyword lead time (KWCal LEAD_DAYS), chips 14/21/30 (fcc-pb-raise); the RUNNING month is never counted, only the last 6 months count (older is a catalogue rewrite, not new-product work), a brand with NO plan read raises with "none on record" rather than being assumed covered, and a market can be BOTH a collection landing and a backlog (different statements, both marks kept). WHERE THEY LANDED = pbCatMix over volhist.cats (first chevron of primary g:product_type), top five + Other, ONE /api/volume call per market — on demand in Review, filled automatically in the New products panel — labelled as products ENTERING THE FEED, a wider measure than the first-seen cohort since a product returning to stock arrives too. Each raised market carries → Titles / → Keywords which prefill the composer with the cohort, its age and the breakdown. A CHART PER MARKET, NOT A HIGHLIGHT ON EVERY ROW (Ray, 17 Sep 2026, sending the Volume module's own estate sparkline: "these new products alert on the Playbook banner - can you replace with these bar chart instead and no need red highlights"): the row carried an amber wash, a red one over it for a backlog, a filled NEW COLLECTION pill and a red ⚠ banner — and on a 28-market brand every market sat at 12–13.4%, so EVERY row was highlighted in the same colour saying the same word, which discriminates nothing and reads as alarm. The row is now the rail's own stated shape (a name, a bar, a number): pbSpark walks the LAST 12 MONTHS of arrivals FROM THE CALENDAR, never from dob.m's keys (that map holds only months that had arrivals, so slicing it draws twelve bars that LOOK consecutive and are not — a gap month closes up and every bar after it sits under the wrong label; a month with nothing is a real zero here and gets a minimum-height bar), with the RUNNING month flagged and drawn at .45 rather than plotted as finished. Marks, blue pair (#2563EB / #4C82E0 dark) and the .45 running month are the Volume estate table's own, so the rail and the page it links to can never draw the same series two different ways. The band verdict moved into the row tooltip; the cohort summary and per-market backlog line are ordinary muted copy; → Titles / → Keywords untouched. Harness assertions in tools/test_playbook_panel.mjs (the calendar walk incl. a year-boundary window, the absent-month slot, the running-month flag, and that no .ck-row.hot/.raise wash or per-row band pill survives). ONE ENTRY INTO THE COMPOSER: window.FCCBrief (briefFromModule) is what the rail AND every /workflow?brief= deep link call, so a brief raised in the rail is a brief raised in Workflow — same draft, same [ibfcode], same save into Intake + the Project Plan, no page reload that would throw away the rail (the wrapper is NOT named openBrief: that name is the composer's own opener, and a second declaration silently broke ＋ New brief until the harness caught it). ONE BRAND FILTER, BOTH WAYS (Ray, 17 Sep 2026: "when the playbook is surfaced, there's a brand filter on top. If the brand is selected, can the filter also be applied in the intake and vice versa?"): board → panel already existed (renderClientChips dispatches fcc-clients, the rail follows a single selected client); panel → board now writes the SAME itState.clients the chips and the ▾ menu write, through one named entry point window.FCCFilterClient (filterToClient) — one client filter on the page, never a second that can disagree. TWO rules keep it from surprising: only a REAL pick propagates (the rail choosing its own default on open, or following the board's own chips, never reaches round and re-filters behind the reader), and a brand the board does not carry is REFUSED AND NAMED — setupMulti self-cleans an absent option, so filtering to a brand with no Intake rows would quietly do nothing and read as broken; the panel subtitle says "not on the board" instead. THE TWO RAILS ARE INDEPENDENT (Ray, 17 Sep 2026: "2 panels button should be independent of each other — when a tab is clicked on Playbook the Retainer panel popped up again even though [I] clicked ✕ to close"): render() refreshes the docked retainer on every tab click, brand change and 120s poll, and the fcc-hours-dock event carrying that refresh was read as a REQUEST TO OPEN — so the rail re-appeared each time and openRail persisted it, undoing the ✕ in the remembered state too. dockShow(name, opts) now stamps detail.user and ONLY a click on an hours dot sets it; everything else (host re-render, the 5-minute reload, a posture save) is housekeeping. Contents still refresh while the rail is closed so it is current when reopened, and each rail keeps its own state (fcc-ck-l / fcc-ck-r).
GET  /aiquote (+/api/aiquote, /api/aiquote/saved, /api/aiquote/plantask) → Quote generator (docs/FeedSpark_AIQuote.html): AI field-generation quote per client × market (catalogue of Tachyon fields, £/SKU · one-off hours · monthly hours per line + maintenance hours, generation method per line), the other quote types (systems = FeedHero dashboards per market per month, feeds = generic / bespoke (ASPL) / local inventory per month, retainer hours one-off + monthly, automatic setup hours per service from the retainer or one-off), the new-products monthly bundle sized from /volume — its card is TWO NAMED HALVES since Ray's 17 Sep 2026 ask ('within AI quote the new products — pull in the product volume bar chart pls to make it easier to convince for client — make sure its organised'): the EVIDENCE half draws the Product Volume module's own bar chart (12 monthly arrival bars off the feed's fs:date_of_birth histogram, same validated blue, same peak scaling, same translucent running month, so the quote can never show a client a shape /volume would disagree with; the series is built from the CALENDAR, never from the keys in dob.m, so a month with no arrivals is a real zero in its own slot rather than a silently closed-up gap) with three headline numbers (last full month · run-rate / month · last N complete months), the fs:date_of_birth foot, a coverage caveat only under 90%, and a ?client=&market= deep link into /volume (the page now honours it — Workflow's new-products card links the same way); when a feed carries no first-seen dates the DAILY IN-FLOW stands in, labelled as the different measure it is, and with neither there is an honest sentence pointing at the override. The COMMERCIAL half holds the priced figure, the estimate/override, the band ladder and the bundle summary. Two name collisions are pinned by markers because both failed silently: the card's KPI helper is updKpi (a second kpi() in the same IIFE re-points the hero row too) and the commercial box wears .ucm (.cm is the charge-mode toggle — inline-flex, nowrap — which threw the column's children clean out of the grid), saved-quote finance tracker (frozen snapshots, stage rail, ✉ Draft email two tones via the askdraft rails, → Intake), ⇩ Export = Excel workbook with live formulas. CFO REWORK (15 Sep 2026, via Ray: 'irrespective of the incorrect discount, it's mixing set-up costs with monthly costs so it's hard to easily see the initial one-off costs vs the ongoing costs'): EVERY surface reads as TWO BLOCKS, one-off apart from ongoing, each with its own discount and total — the page tiles (Initial · one-off FIRST, Ongoing · monthly second, Settings), the bottom line + KPIs, ⧉ Copy text, the client email / brief (qBlock) and the tracker detail. FINANCE'S OWN BOOK (Ray, 16 Sep 2026: 'this is the output excel that our Finance team wants to see — so stick as close to this format as possible — especially section that covers One-off section and Monthly cost', with FeedSpark_Reiss_Quote_Sept_26.xlsx attached — the committed reference is reference-files/quote-templates/): the EXPORT is now a replica of that book, in their order — 4 spacer rows, then their header block (Client / URL / Client Contact / Agency / Contact down A with the values in B; Created By / Date Created / Quote Valid / Reference / Reviewed down K with the values in M; the 36pt 'Quote: Core Requirements' box merged across D5:I9), then BAND 1 **MONTHLY RECURRING COSTS** (section 'System / Platform Costs' = the FeedHero dashboards + every feed + the monthly Tachyon update → 'Total System Costs', pale-pink Net, note 'Total Tech Costs'; section 'Account Management & Data Optimisation - ManPower (Days / Month)' = every hour sold per month — a monthly optimisation line, the maintenance on a one-off line, the monthly retainer — written as DAYS at =Dayrate → 'Total Managed Service Time'; then 'Total Monthly Cost', RED Net), BAND 2 **ONE OFF OPTIONAL COSTS: PERFORMANCE BOOSTER PACK** (section 'Optional Costs: Performance Booster - Initial Issue Fix / Data Optimisation' = the initial AI data optimisation, per-SKU lines at £/SKU and one-off hours lines as days → 'Total Performance Booster Costs', RED), BAND 3 **ONE-OFF COSTS: SET-UP & CONFIGURATION** (section 'Core Set-Up & Initial Optimisation' = the set-up fee, the automatic setup hours from the retainer or at the day rate, and the one-off retainer hours → 'Total Set-Up & Configuration Costs', RED), then BAND 4 **QUOTE SUMMARY** — ours, because their book totals each band but never adds the two one-off bands together, which is the one figure the CFO asked for: Total Monthly Cost · Total One-Off Costs · Total Annual · pro-rated · Year 1. Their column map and styling verbatim: A 24.4 · B 64.4 WIDE AND EMPTY so the name in A spills across it (so each line's descriptor + Ray's note live in M, Additional Notes, 86 wide) · D:F Quantity / Cost / Total Cost under '<MKT> Market' · H 'Discount % (Exc Man Power)' · I 'Discount £ (Exc Man Power)' · K 'Net Cost'; Calibri 12; fills F2F2F2 band heads, DEEBF7 inputs, E2EFDA header values, FFE1E1 section subtotals, FF0000 block totals; gridlines off at 55% zoom, landscape A4; tab '<Client> - Cost Detail'; `Dayrate` is a DEFINED NAME holding the block rate so their =Dayrate cells resolve; day quantities use their 0.00" Days" format with the value UNROUNDED so D×E is exact. Their formulas verbatim: F =IFERROR(D*E,"-") · I =F*H · K =SUM(F-I) · subtotals =SUM over the Net column. TWO deliberate departures, both for accuracy: money shows pence ("£"#,##0.00 — their "£"#,##0 prints £446.25 as '£446', so a block total would look like it did not add up) and the date reads dd/mm/yyyy, not their mm-dd-yy. NO VAT ANYWHERE (Ray, 16 Sep 2026: 'just remove VAT in general, as it's known-given') — the same rule their book follows: vatOn() is false, there is no VAT toggle, no VAT row and no Net row on the tiles (with VAT gone the net WAS the total), and every figure on every surface is ex VAT; a SAVED snapshot keeps its own `vat` flag so the finance tracker never relabels a figure already signed off. THE DISCOUNT: the sheet's own header reads 'Discount % (Exc Man Power)' yet the discount was written onto every hours row — now MAN POWER (hours at the day rate: retainer, setup hours, one-off / monthly hours lines, maintenance) is NOT discounted unless '+ man power' (#discman, r.discMan) is ticked for that quote, when the header drops the caveat; scope all / one-off / monthly / selected lines still applies on top; snapshots carry discMan and ✎ Edit restores it (pre-rule snapshots restore ticked so their figures hold). Ray's CFO offered his own template as a base — drop it into reference-files/ and the export can be re-mapped onto its exact rows. AI MODE ATTRIBUTES — A SECOND PRICING MODEL, BY DATA SOURCE (Ray, 17 Sep 2026: 'mirror this artifact to import inside the quote generator for AI, using the exact same calculation logic — I'd like this module and interface better as well but adapt to our FCC colour thematic & text & branding & emoji'): a fifth quote type `aim` whose card prices Google's conversational six PLUS product_highlight by WHERE EACH FIELD'S DATA COMES FROM, which is the whole commercial question for these attributes and the thing a £/SKU rate cannot express — ⊞ master feed (configuration hours, nothing monthly) · ⇩ scrape (configuration hours + ONE scrape run a month covering EVERY scraped field, charged once, never per field) · ✦ Tachyon AI (set-up days per field + £/product over the range once, then £/product on the new ones, with a monthly minimum measured across the AI FIELDS ONLY — reading it as a floor on the whole card would swallow the scrape run and undercharge by £100) · ⚙ FeedHero rule (the hours to build it); impossible routes are not offered and the row says why (AI cannot invent a document_link, related_product or variant_option; popularity_rank is computed in FeedHero from the brand's own Google Ads data and nowhere else). The engine is the builder's OWN, unchanged — £695 day rate / 8h = £86.875/h, 2h a feed field, 2h a scraped field, £100 scrape run, 2 days + £0.10/product for AI, 2h a FeedHero rule, £100 AI floor, 10% volume buffer, 10.5% fallback newness — held UNROUNDED and rounded for display only, and kept liftable between AIMODE:ENGINE markers for tools/test_aimode.mjs (100 assertions in qa_gate/presync/validate, run in a bare sandbox so the block can never reach for a page global). VOLUMES: the catalogue in scope is the page's OWN product-type scope (one scope per quote, never two that can disagree) but the UNIT is this card's own — AI generation is normally charged per ITEM GROUP, not the SKU rule the catalogue above runs on, and a feed with no g:item_group_id has the toggle refused rather than faked. NEW PRODUCTS A MONTH = three reads, strongest first, with the card always naming the one it used: (1) the Monthly update — new products card above it (Ray, same day: 'only thing to keep is our monthly update - new products view, it looks better' — so the builder's whole newness STEP is deliberately NOT imported and this card draws no second arrivals chart; that card's whole-market SKU figure is taken down to this scope and unit by the scope's share of the feed), (2) the feed's own first-seen dates, captured by pullFeed on the SAME stream as the product-type index (AIM_DOB_PATS resolves fs:date_of_birth and its aliases; undated items are left out of BOTH sides rather than counted as old, and at group level a parent is dated by its first row), (3) FeedSpark's 10.5% working estimate; a typed figure beats all three, the buffer goes on top and the result rounds UP. THE ARRIVALS CHART IS IN THIS CARD TOO (Ray, 18 Sep 2026: 'why don’t you bring the new product volume arrival bar chart that looks really beautiful over to this section too?, and at the same time, I don’t see the pull live feed selected from here') — drawn by the Monthly update card’s OWN updBars off ONE shared series builder (updMonthRows / updDayRows, now called by both), so the quote, that card and /volume can never draw one feed three ways; what the bars ARE is stated under them, since they are the WHOLE market’s arrivals while the card may price a product-type subset at item-group level (caption switches to ‘this card prices on these products’ only when the scope IS the whole catalogue at product level). The chart does NOT need the feed pull (arrivals come from /api/volume), but the three scoped rows do: UNSIZED (no feed pulled, no headline typed) they read a DASH rather than the market figure — printing ‘1,534 / month at this scope’ under ‘0 products in scope’ is the card contradicting itself on screen, which is what Ray screenshotted — and the ⟳ Pull live feed button now sits IN the card (the hero’s own pullFeed(), several screens up before, so the card gave an instruction with no action beside it). One trap pinned: the arrivals fetch resolves ONCE and BOTH cards read that record, so ensureVolume redraws both — redrawing only the Monthly update card left AI Mode on its ‘Reading this feed’s arrivals history…’ placeholder while the card above it already had twelve bars. It rides the same rails as every other type — totals, tracker, client email, brief — and in the Finance workbook SPLITS ACROSS THREE BANDS because its three costs are three different things (monthly run + AI months → System / Platform Costs; the per-product generation → Performance Booster; every route's configuration hours → Set-Up & Configuration as days at the Dayrate); writing it into one band would put set-up back inside the monthly cost, the exact thing the CFO asked us to stop doing. The discount follows the sheet's own header: configuration hours are man power and are not discounted unless '+ man power' is ticked, while the per-product generation always is (each line carries `man` and `gen` apart for this). A saved quote freezes its ROUTES so ✎ Edit reproduces what finance signed off, and the snapshot's rates are deliberately not written back over the shared rate card. TWO PRE-EXISTING BUGS this surfaced, both pinned: `.qp` is the page's shared pill LOOK, so the scope quick-pick handler without a data-qp guard answered to the quote-type buttons and market chips too and set scope={mode:'manual',manual:NaN} — PICKING A QUOTE TYPE SILENTLY ZEROED THE PRODUCT SCOPE and every SKU count with it; and the summary tiles share one set of subgrid row tracks, so a row added to a tile without adding a track printed through the row below. SAVING IT, AND REWORDING IT (Ray, 18 Sep 2026: "i cannot save AI Mode quote btw - also allows all text can be edited please"): the save guard asked `t.inc` (ticked Tachyon fields) and `t.x` (systems / feeds / retainer), the only two line types the day it was written — so a quote made ENTIRELY of routed AI Mode attributes counted as EMPTY and the save refused, telling him to add a line he had already added (the monthly new-product bundle alone failed the same way, found on the way past). `qLineN(t)` counts every line type in ONE place — Tachyon fields + extra lines + routed AI Mode attributes + the bundle — asked in LINES not money, because a £0 line is still a line somebody put on the quote, and the refusal on a genuinely empty quote now names every way to fill it. THE COPY: the injected editor's default selector is written for the decks (h1-h5, p, li, td, th, .lede, .callout …) and every explanation on this page is a `.ch` block, which that list does not name — so the one thing Ray reads before showing a quote to a client was the one thing he could not reword. The page declares its own `window.DECK_EDITOR_SELECTOR` (default + `.card > .ch, .qs-h, .aim-note, .aim-lbl`) rather than widening the shared default, whose keys are POSITIONAL and would renumber every deck's saved edits at once; ONLY PROSE is listed — a derived line (#aim-srcline, #aim-word, the .hint counts, the figures beside the labels) is never editable, because an edited sentence freezes the number inside it and a frozen number on a client quote is a wrong number. And because the editor keys each element ONCE at load, copy a render redraws could never hold an edit: the AI Mode card's prose — the scrape note, both box headings and all ten foot row labels — now lives in the TEMPLATE, with `aimFoot()` writing only the structural choices (which newness row applies, the chart, the buffer figure) and `aimNums()` the figures by id, one writer both the render and the refresh call; the newness box is left alone while somebody is typing in it. DELIVERED (Ray, 18 Sep 2026: "put in a button for 'Delivered' meaning work delivered — And obviously, if it had been added to the plan, then it can follow the tracking of the whole workflow intake, right? When you have a confirmation from ASPL, you know this work has been delivered"): a stage between **In action** and **Billed** — the real order, the work lands then it is invoiced — that anyone can press AND that moves itself. → Intake writes a plan task titled 'AI Field Quote — <REF> — …' and the Workflow composer saves a brief with that task, so the quote's own REF is already in the ticket: `loadQBriefs()` ties them on that machine-shaped TOKEN and nothing else (the same rule briefmatch.js's ibfcode follows), LONGEST ref first so QT500-2 is never taken for QT500, a foreign `b.client` breaking the match outright, newest ticket winning a re-brief. `WF_DELIVERED` = done/running/analysis/confirmed — every stage from 'Done — ASPL' onwards means the work was delivered. `autoDeliver()` moves a quote ONCE and stamps `q.aspl` — so a human's later correction stands — and never touches one already Billed or Declined, never moves one backwards; the row wears a live `.t-wf` chip (stage · whose court, linking to /workflow) or an honest "no ticket yet" on a filed quote nothing matches, finance gets a **Delivered · not billed** KPI, and the pipeline is re-read every 120s and on tab-visible. NOTHING ANNUAL, ON ANY QUOTE (Ray, 18 Sep 2026: "move annual cost lines or anything related to annual cost (pro-rata) not neccessary (Across all quotes)"): a Year-1 total adds a one-off to twelve months, re-mixing exactly what the CFO rework pulled apart, so every annual and pro-rated COST figure is gone from every surface — the AI Mode card's "Year one" row (the two answers, charged once and charged monthly, now read as a pair under one rule), the summary tile's "Annual (× 12)", the bottom line's Year-1 span and its "(x a year)" hint, ⧉ Copy text, the client email, the brief, the finance tracker, and the workbook's BAND 4 (Total Annual, Pro-rated, Year 1 Total — the pro-rated row also ASSUMED a January contract year-end, so on any other client it printed a figure nobody had agreed); `year1` left the engine too, so no surface can quietly print one again. What SURVIVES is new products a year — a count of products, not a cost. Harness: nothing multiplies a monthly figure by twelve, pinned as a forbidden pattern, and three markers from PRs #329/#363/#379 are retired to say so. THE BUNDLE'S MONTHLY FIGURE IS A TYPED BAND PRICE (Ray, 21 Sep 2026, QT261571: "at which point the new-product updates for this quote gets to 15,100/month?"): nothing computes it — monSub += updGBP(), the band box on the SHARED rate card; £15,100 was two characters typed in front of a pre-filled "100", saved on the keystroke house-wide with no ceiling. The band boxes now select on focus (AND swallow the click's mouseup, which otherwise collapses the selection), a band more than 5× its FeedSpark default is ringed off-scale with "⚠ was £100 ↺" on the chip, a warning under the bands and ⚠ on the quote total, the total row names the figure ("up to 1,000 band · £X/mo flat"), a saved quote FREEZES its own upd.gbp so ✎ Edit reproduces what finance signed off (thawed by pinning/↺ auto/editing a band/moving the estimate), "draft prices" now means no band priced yet, and a £0 discount says why ("⚠ £0.00 · no line ticked"). Pre-existing and fixed alongside: the .qs-col subgrid tiles could not shrink (every row ~10px over its tile on main). Docs: docs/AIMODE.md. THE AI FEED GENERATION BUTTON IS GONE (Ray, 21 Sep 2026: "remove AI feed generation Quoting button -- we have Spark AI now"): the picker offers sys/feed/ret/aim, a fresh record opens on Spark AI ({aim:true}); the `ai` type survives as LEGACY — a record or ✎ Edit'd snapshot carrying types.ai shows its fields card and a dashed legacy button until unticked, never again after; a types-less pre-11-Sep snapshot is still {ai:true}; scope-card and upd-card carry data-tp="ai aim" (a card lists every type it belongs to, shown while any is on) so Spark AI keeps the product-type scope and the Monthly update bundle; the Fields-selected / SKUs-quoted KPIs show only with the legacy type on; A HIDDEN CARD NEVER PRICES (Ray, 21 Sep 2026: "a fresh Reiss quote still have these numbers - refresh it?" — £1,818.14 + £260.31/mo with nothing ticked on screen): a pre-types client record now opens on Spark AI and its ticked per-SKU lines were still summed, so fOn(id) is gated on typeOn('ai') — a line is ON only while its type is; ↺ Start fresh (#fresh, confirm) resets the client record to blank, saved quotes + rate card untouched. THE TRACKER DECLUTTERS (Ray, 21 Sep 2026: "the saved quotes getting super cluttered - expand horizontally if needed, buttons should be presented cleaner - maybe in a different format to save space"): one line per quote (~48px, was ~150) — the stage rail is a STEPPER (the current stage the only word, dots behind filled green / ahead hollow, Declined the ✕, every dot one click + named in tooltip/aria-label, header "Stage — click a dot to move it"), the actions are ONE ROW of 26px icons (✉ → ✓ ＋ ❐ ✎; done = green with the who/when in the tooltip; the proposal trio grouped in .t-grp; the six never wrap, only the live 🎫 ticket chip may; legend under the table), compact dates (tkWhenC), "ex VAT" once in the Total header, min-width 1000 so 1280 has no scrollbar, and on ≥1400px the card steps out of the column to the viewport gutter. FIELD COLLISION FIXED ("Invalid Date Invalid Date" on QT263819-4): stamp() wrote the last-update time onto q.upd — the field the snapshot keeps the new-products BUNDLE on — so the tracker printed an object as a date and every stage move / owner change / → Intake on a bundle quote overwrote its bundle with a number; the stamp is q.lu, tkLast() reads lu → legacy numeric upd → t, qUpd() is the one reader of the bundle (object only). WHAT SETS EACH OPTION APART (Ray, same day: "the summaries below each option should be clearer … provide both details (delta changes between option) but not too cluttered"): two lines per option, both written by the PAGE off the snapshots, never Tachyon — includes (Spark AI with route mix · bundle + band · cohort · unit · discount · scope) and, against the option BEFORE it (a ladder), what changed: + added (green) / − dropped (orange) / ~ re-routed, band, scope by SHAPE (SKU count only when both sides know it), cohort, unit, discount, then the £ movement one-off and monthly apart; four clauses then "+N more" (full list in tooltip), "same lines" for a duplicate; the build in progress vs the last saved option; ⧉ Options carries "Compared with option N: …". Harness tools/test_aimode.mjs (268). THE SCOPE COLUMN SAYS WHAT WAS QUOTED (Ray, 21 Sep 2026: "can scope be more details (should be market + quote type 'Spark AI + New dashboard + new feed..' from selection)"): qTypesWord(q) reads the frozen lines — Spark AI ×N · New dashboard ×N · New feed · LIA feed · Retainer Nh/mo · New products bundle · Setup fee — on line one, market · product scope on line two (only when a line is priced on products), the full includes line in the tooltip, header "Quoted · scope"; the orange type chips under the client name are gone. ONE TAB PER OPTION IN THE EXPORT (Ray, 21 Sep 2026: "when export quote - export Option on seperate tab in the same excel"): exportTabs() → one workbook, one Finance-format tab per option of the proposal in play ("Option N · <ref>", plus "Option N · this build" for an unsaved addition; a quote outside a proposal = one tab as before) — each option rendered by withSnap(q, fn) (the snapshot read into a TEMPORARY record via applySnap, the mapping ✎ Edit uses, then client/market/record/edit state restored; no render/save/toast), quoteSheet({ref, at}) carrying the option's own reference + save date; xlsxBytes(tabs) renders every sheet against ONE style table, a drawing per sheet, Dayrate defined per sheet (localSheetId). EVERY SAVED LINE IN THE BREAKDOWN (Ray, 21 Sep 2026, sending the expanded ⌄ row of a Reiss GB quote: "each quote breakdown should have every line saved as this example"): the row listed the legacy fields, extra lines, bundle, setup and discount but NOT the Spark AI attributes, so a Spark AI quote's breakdown read as a total with nothing under it — aimDetailRows(q) renders the SNAPSHOT's own q.aim.lines (never re-priced off the live rate card): a caption row (attributes · units · cohort · new a month · Spark AI subtotal) then one row per attribute with its route (AIM_ROUTE_LABEL; the AI floor = 'monthly minimum'), the saved detail sentence and its money (one-off with hours / monthly / the saved 'Included' note), every cell gated on £, money on one line (.dl.ai). ONE BLOCK PER UNIQUE QUOTE (Ray, 22 Sep 2026: "add rows between unique quote so its easy for me to recognise — also, 2 options within 1 quote is considered unique"): the tracker reads as blocks with a spacer row (tr.t-gap, a subtle 1px DOTTED rule drawn by the cell's ::before on a transparent row — Ray, 22 Sep 2026: "looks weird and uneven … replace it with dotted lines instead and make it just subtle"; the earlier 14px and 4px bands of page background read as a thick, uneven strip against the white card) between them — tkGid names a block by its proposal id, else the root of its version chain (q.prev walked back), else the quote itself; a block takes the position of its latest activity, options sit in option order, versions newest first, and the gap is written only where the block changes (never inside one, never first; an expanded ⌄ row stays inside its block). A SAVED QUOTE EXPORTS ITS OWN LINES (Ray, 21 Sep 2026, on the most recent Reiss option: "the downloaded Excel … the number is not matching"): withSnap restored the snapshot's SETTINGS and then let the sheet re-derive every line from live state — Spark AI units off the feed index (✎ Edit clears it; a fresh session never has it → generation priced on 0 items, the AI floor read as the whole monthly), dashboards/feeds/setup hours and Spark AI rates off TODAY's rate card, Spark AI configuration hours at the retainer block =Dayrate (£595/8h) where the card prices them at the Spark AI day rate (£695/8h); reproduced £1,785 / £200 against £3,070.90 / £200. The sheet now reads the FROZEN snapshot (withSnap sets FROZEN; quoteSheet → snapLines/snapTotals/snapDay/scopeTextSnap), exportTabs renders every saved option from its snapshot — the one under ✎ Edit INCLUDED (snapTab; a build nobody saved is the live one), ⇩ CSV likewise (sheetForExport), Spark AI hours as days at the Spark AI day rate as a VALUE never =Dayrate; and on screen ✎ Edit keeps the saved units/new-a-month (r.aim.fz keyed to scope·unit·cohort, used only while no live index — aimFz) so a signed-off quote never re-prices on 0 items; pulling the feed thaws it. ONE MORE BUG THIS UNCOVERED: a FLEX row ignores `hidden` — the UA's `[hidden]{display:none}` loses to any class that sets display, so `el.hidden=true` left the row on screen with an empty value in it (the newness pair drew BOTH, "Arrivals run-rate 0 / month" above the typed % box, and the quote summary's AI Mode rows had been showing empty since they shipped); `.aim-r[hidden],.qs-r[hidden]{display:none}` states it once. IT IS CALLED **SPARK AI** (Ray, 21 Sep 2026: "can you rename the section into Spark AI"): every label, the quote-type button, the summary rows, ⧉ Copy text, the client email, the brief and the workbook row names; the RECORD KEY is untouched (`types.aim`, `q.aim`, `AIM_*`, `data-tp="aim"`) so every quote finance already signed off still reads, and a forbidden marker keeps the old label from creeping back. GENERATE FOR — THE ARRIVAL COHORT (Ray, same message: "within the charge per product ID, allow selection — for example, if clients only want to optimize for new collections … add manual selection of products to be generated, optimized based on date of birth as well. For example, any product that arrives after August 2026"): CHARGED PER answers what ONE UNIT is, so WHICH of them we generate is its own control, applied on top of the product-type scope rather than replacing it — a select whose months come from the feed's own `fs:date_of_birth`, each option carrying what picking it would leave in scope, so a month the catalogue does not reach cannot be picked. The pull now buckets first-seen dates by MONTH (rowsM / parsM / ptM beside the existing fixed twelve-month counts — the shape /volume's arrivals engine already keeps, bounded by the months the feed spans, not its row count) and aimCoSeries builds the months, the options and the selected count in ONE pass for the current unit + product-type scope, so the list can never disagree with the figure it produces; the cutoff is a `YYYY-MM` string compare, never a timezone. THREE rules keep the quote honest: (1) A COHORT NARROWS THE ONE-OFF, NEVER THE MONTHLY — the range is generated once so a cohort makes it smaller, but every product arriving next month is in the cohort by definition, so `aimUnits()` is the cohort while `aimPerMonth()`/`aimNewness()` read `aimScopeUnits()`, the product-type scope alone; scaling the ongoing figure down by the cohort's share would undercharge the part of the service that never ends, and the card SAYS SO on screen; (2) AN UNDATED PRODUCT IS NOT IN THE COHORT — it cannot be shown to have arrived after the cutoff, so it is left out and the hint names how many, the same rule the newness read follows; (3) A SELECTION IS NEVER SILENTLY CLEARED — narrowing to a product type the cohort does not reach keeps the month in the list reading 0, because a select showing "Every product" while a cohort of 0 is applied would be the card lying about what it priced. It changes what the client is buying, so it travels with the snapshot, ✎ Edit, ⧉ Copy text, the client email, the brief and the finance workbook's notes (a quantity alone does not say which products). QUOTE OPTIONS — 1, 2, 3, 4 (Ray, 21 Sep 2026: "each piece of quote presented should be selectable as an option (1,2,3...). When asking a client to buy a service, you need to provide one, two, three, or four options … after the quote summary section, if the proposal has three options, showcase the three lines of options after each generated quote. AM can continue to build different quote as additional options … especially in the saved quote / finance tracker, highlight which option the client has chosen and approved. and use that data for future analysis"): AN OPTION IS A QUOTE — nothing new holds one, a saved quote carries `prop {id,n,label}` and a proposal is every live quote sharing that id, so an option keeps its own ref, lines, client email, brief and place on the finance rail, and ✎ Edit still versions it (a v2 of option 2 is ordinary; the superseded one leaves the proposal as an EARLIER VERSION and its replacement INHERITS the option number, so an edited option is counted once). ➕ Add option on any tracker row loads that quote into the builder and makes the next Save a SIBLING — deliberately NOT the ✎ Edit mode, which would version it; a standalone quote becomes option 1 the moment a second is wanted, and the next free number is taken. THE STRIP sits immediately after the quote summary (`#opt-strip`, renderOptStrip) showing one line per option — glyph, label, ref, what it includes, one-off + monthly — plus the unsaved build, and it STAYS UP after a save because that is "after each generated quote"; the proposal is the one IN PLAY (being added to, under ✎ Edit, or just saved this session), never guessed from the client, and every write to the saved store redraws it or it reads one option behind. THE CHOICE lives on the winner with the NEWEST stamp winning on read (two AMs can each click before their stores merge), choosing un-chooses the siblings, and it is a TOGGLE because a client may change their mind; it NEVER touches the stage rail — a sibling reads NOT TAKEN, never Declined, because Declined is finance's word for a quote they turned down and not-taken is the client picking a sibling: different facts about different people. THE MONEY: three options at £5k are one £5k opportunity, so `countedIds` gives every figure on the rail exactly ONE quote per proposal — the chosen option, or the lowest-numbered until the client picks — and the board states the rule; without it the pipeline would have read as three deals. An option the client did not buy is never auto-delivered by the ASPL match, and an earlier version offers no ✓ Chosen (a button that would silently do nothing). ANALYSIS: a line under the KPIs reads proposals decided vs open, which option NUMBER is chosen most often, and chosen value against the top option of each. ⧉ Options copies every option as ONE client-facing comparison — what each includes, not just a price, ex VAT with the validity, like every other client exit. Docs: docs/AIMODE.md
GET  /tasks (+GET /api/taskmanager) → FS TASK MANAGER (Ray, 16 Sep 2026: "lets build a new module as FS Task Manager (to show all capabilities of this new feedspark-reports mcp please) — Allow more area where you can also create a search bar for each AM to work inside the pull-in report via the MCP, and a quick pull-out report—either a pie chart or any type of chart—based on the hours of billable versus non-billable"): the reports database itself on one page, the other half of the TM integration above (that one maps HOURS onto figures the FCC already shows; this one makes the DATABASE searchable). ALL FOUR MCP reads have a home — get_client_list (the Accounts & hours balance tab: every market live and stopped, group, AMs, allowance, used, balance), get_task_list_for_client (the Tasks tab + the chart + every KPI: one row per booked task with owner, day, status and its TWO hour columns), get_tickets_for_client (the Client tickets tab — the queue the hours lane listed as "not yet": subject, status, age, idle, messages, tasks spawned, hours consumed) and get_ticket_detail (the ticket drawer). ITS OWN ROTATION ON THE SAME FIRING: tmPull chases hours onto tickets in a 21-day window with hot brands weighted ×2; the module needs EVEN coverage of twelve months, so worker.js `tmBookPull` runs right after it on :15/:45 sharing the SAME MCP session (one handshake) — get_client_list(all) + BOOK_MARKETS(2) get_task_list_for_client(limit 1600) → KV tmbook:<client> + BOOK_QUEUES(1) get_tickets_for_client → KV tmtick:<client>, index tmbookidx, state tmbookst; STALEST FIRST with a never-read market always leading (else a market at the end of the roster starves), ~39 worked markets = the book turns over ~2×/day; only markets carrying a block or booked hours are pulled (a stopped market with neither has no work to find) though the roster stays whole for the Accounts tab; ?sync=N (OWNER) reads N more now and the page's ⟳ Sync more loops it so nobody waits on a fresh deploy. NOTHING IN GIT — hours, task titles and client contact addresses are per-client commercial data, KV only, `ops/reports/` ignored wholesale so a session cannot commit what it pulled; the page is SERVED SCOPED (only the client records the signin is allowed are read) and the module is grantable as `taskmanager`. THE SEARCH BAR is a grammar driving tasks + tickets + chart + breakdown + KPIs at once: bare words search the whole record, "quoted phrases" match as one, `-` negates, repeats of a field OR while different fields AND; A COMMA IS OR AND A SPACE IS AND (Ray, 16 Sep 2026: "this search bar allows multiple filters separated by commas … 'Febin,Vitus' with no space after the comma") — two names side by side already meant rows naming BOTH, which is the right default and is unchanged; a LIST is the other question and could not be asked, so `Febin,Vitus` asks it. The comma binds ACROSS a space (`Febin, Vitus` is the same list — half the team types the space and reading it as AND would answer "no rows" to a query that plainly means two people), a trailing comma mid-typing is simply not a term yet, a lone `,` filters nothing rather than everything out (the "N of M" chip and footer label key on the PARSED terms so the page never claims a filter that is not narrowing), and inside "quotes" a comma is punctuation — a quoted phrase stays ONE literal, carried through tokenisation on a sentinel. On a field it is the shorthand for repeating it (`owner:Febin,Vitus` ≡ `owner:Febin owner:Vitus`, each alternative through the cat alias map), while the range and bound fields (from/to/min/max) and two-state bill: keep their existing repeat behaviour rather than inventing an alternation nobody means. ONE definition serves both boxes — the plain-substring pane filter under the tab header reads the engine's own `orTerms`, so a comma cannot mean one thing there and another in the grammar bar above it; client:/brand:, owner:/who:, market:/mkt:, am:, cat: (opt/tech/feat/acct/other + their own words), status: (the database's word OR the bucket), bill:yes|no, from:/to:/month:, min:/max: — field values match EXACTLY OR BY PREFIX of 2+ chars (client:rei → Reiss; client:eiss → nothing; market:E never matches DE); `/` focuses, ?q= deep-links, ＋ Save this view per device, breakdown rows toggle their own filter in. bill:yes and bill:no are NOT opposites — a part-billed row answers to BOTH, and reading bill:no as "nothing was billed" would hide the majority of the book. TAGS — A SECOND AXIS (Ray, 17 Sep 2026: "there will be a tagging system, a labeling system of which task is urgent, which task is from agency work, and which task is technical … the goal is to highlight how many hours are spent on urgent stuff that should have been spent on optimisation. Later on, I will do mass tagging across all rows if possible or by either keyword or using Excel that I can import and export"): `cat` is read off the title and says WHAT THE WORK WAS; a TAG is a person's judgement about WHY IT HAPPENED, and the two are deliberately not merged — an urgent job is usually still useful work, which is exactly why the displacement never shows up in `cat` alone. Checked against the real book first (Reiss GB, 1,200 rows): `priority` is the constant 20 on every row and `task_source` is empty, so nothing here duplicates a field the database already has. KEYED ON THE TASK'S OWN list_id (100% populated, unique), never on wording — the rotation re-packs every row ~2x/day, so a tag keyed on a title would come unstuck the first time one was edited; a row that arrives without an id shows 'no id' rather than being keyed on something that drifts. Seeded vocabulary = Ray's three (Urgent `displaces`, Agency work, Technical), editable in the 🏷 Tags & rules dialog (add/remove a tag, flip which ones count as displacing). THE HEADLINE is the 'Where the hours went' card above the chart, reading the CURRENT search so narrowing to a client or a quarter re-asks the question of that slice: displaced hours · optimisation hours (cat:opt) · the ratio · and COVERAGE, which travels with every figure — a 4%-judged book would otherwise report '2.1h urgent' and read like good news, so under 50% coverage the verdict says FLOOR, not answer, and untagged is stated as NOT YET JUDGED, never 'not urgent'. A HUMAN ALWAYS BEATS A RULE, including an empty list ("I looked, none apply") — else a keyword rule would re-apply its tag every render and nobody could take one off; 'Clear' removes the record entirely and hands the row back to the rules, which is a different intention and a separate control. MASS TAGGING acts on whatever the search already resolved (no second selection model to drift out of step with the filter); KEYWORD RULES are plain substrings over title/notes/both — never a regex from the page, since an AM typing `(` would throw inside the render loop — each previewing what it would touch AND how many rows it will leave alone because a person already decided them, with the book's own top titles offered as one-click rules. `tag:`/`label:` joins the grammar (`tag:urgent,agency`, `tag:none`, `tag:any`, `-tag:`), `tag` joins the chart dimensions, and because ONE TASK CAN CARRY SEVERAL TAGS the tag dimension is multi-valued: a task lands in every one of its buckets, untagged gets its own, and groupBy returns `multi`/`placements` so the surface says the hours legitimately sum to more than the book rather than quietly double-counting. EXCEL ROUND TRIP: the export gains a Tags column beside Type and already carried Task id, which is what the import keys on; ⇧ Import tags reads .xlsx (docs/xlsx_engine.js gained a reader — ZIP central directory + DecompressionStream 'deflate-raw', shared strings, inline strings) or .csv (quoted commas, doubled quotes, CRLF, a header below a title row), then PREVIEWS THE DIFF and saves nothing until confirmed — rows the file does not mention are never touched, so importing a filtered sheet cannot wipe the book, and unknown ids / unknown tag names are reported rather than applied. Store = shared state `tmtags` (taskId → {client,tags,by,at}, 'field'-scoped so a scoped signin stays inside their clients) + house-wide `tmtagdef` (vocabulary + rules), on /api/state with kvmerge + X-Sync-Base and a retrying push — a displacement figure that differed per screen would be worse than none. A HAND-SET TYPE THAT SURVIVES THE SYNC (Ray, 17 Sep 2026: "Can Type also be edited on FCC and made changed data sticky from either tag or excel import too?, because the daily report fetched from the MCP will actually overwrite?") — he is right about the mechanism: `cat` is derived by classifyTask(title) in normTask AND PACKED INTO the KV row, so every tmBookPull re-derives it and anything written onto the record is gone within ~12h. So the override is NEVER written onto the record: shared-state ns `tmtype` (taskId → {client,cat,t,by,at}, 'field'-scoped), applied as a decoration AFTER unpack from a store the pull never writes. Click the Type chip → the five categories + 'Read from the title', which DELETES the override rather than writing the derived value (else the row would stop following an improved classifier forever, for a reason nobody recorded). THE TITLE IT WAS JUDGED AGAINST travels with it: a rename in the reports database keeps the override (it is keyed on list_id, not wording) but marks it STALE with a red ! and a tooltip — dropping it silently loses a judgement, keeping it silently hides that it was made about different wording. It overrides the FIELD, so the `cat:` search, every grouping, the chart and the displacement headline all follow it with no reader taught about overrides; the chip is ringed when hand-set and the export carries a 'Type set by hand' column so a round trip cannot launder a judgement into something that looks derived. A TAG NEVER DRIVES THE TYPE, deliberately — Type is what the work WAS, a tag is why it happened, and the displacement figure compares one against the other; wire them together and the comparison measures itself. ⇧ Import edits reads Tags AND Type of work (the WORD the export wrote, matched case-insensitively) on the same preview-diff-confirm rails. NOTE THE KNOWN DIVERGENCE: the brand one-pager's 'Where the retainer went' donut calls classifyTask directly (tools/reporthours.mjs), so it still shows the DERIVED type until that surface reads tmtype. GUARD: typeOf/tagsOf key on taggable(), not `id != null` — a row whose id is 0 has no id, and every such row would otherwise share the key '0' so one override would leak onto all of them. THE CHART: series ALWAYS billable vs non-billable, axis = whatever you split by (everything=donut, client, owner, cat, month, market, am, status, task, tag) AND A SECOND + THIRD NESTED SPLIT (Ray, 17 Sep 2026: "allow secondary and tertiary axis split as well, so you can see more granular breakdown, almost like AdWord campaigns") — AdWords nests ROWS (Campaign › Ad group › Keyword) and keeps the metrics in the columns, which decides the one thing that could have gone wrong: the extra dimensions nest the ROWS, they never become the series, so every node at every depth keeps its own billable/non-billable split. `then` selects offer every dim except 'total' and except those already used above them (owner within owner is one child per parent), the third is disabled until the second is chosen, and the form selector switches to a 'Nested breakdown' option it only owns while nesting applies rather than sitting greyed on a stale label. src/taskbook.js groupNested (NEST_CAPS 12/8/6 per level; the tail folds into one 'Other (N more)' that KEEPS ITS OWN CHILDREN so the fold is not a dead end) + flattenNested (parents before children, each row carrying its full path and a `leaf` flag). Rendered as indented SVG bars — SVG like every other form, so ⬇ PNG and the hover tooltip work unchanged — SCALED TO THE WHOLE VIEW not per parent, so a child bar and a top-level bar of the same length mean the same hours; the ⊞ table indents the same hierarchy; ⎘ Table copies ONE COLUMN PER LEVEL and LEAF ROWS ONLY, since a pasted table mixing parents with children would double every total anyone summed; ?dim2=&dim3= deep-link. Children always add up to their parent EXCEPT through the multi-valued `tag` dim, where the tree carries `multi` and the verdict says the rows sum to MORE than the total above; a capped level says how many it folded. THE CHART: series ALWAYS billable vs non-billable, axis = whatever you split by (everything=donut, client, owner, cat, month, market, am, status, task, tag), forms donut / stacked / 100% / horizontal bars, reading the CURRENT search result so it is never a different population from the table under it, stating its own verdict in words; exits ⬇ PNG (2000px, footer-stamped with window + query), ⎘ Copy table, ⬇ CSV of these rows, 🔗 Copy link; palette = the Volume module's validated pair (billable #2563EB/#4C82E0, non-billable #ED6F0B/#C67B28), cat dots = the one-pager's retainer palette, categories from tools/reporthours.mjs so the two can never disagree. BILLABLE AND NON-BILLABLE ARE NEVER MERGED — time_taken is charged, time_taken_nonbill is time we spent and did not charge; both are real delivered effort so both count toward what an account cost, but they answer different questions, so every total carries the split and bill+nonbill=total is asserted on every aggregation path. TWO TRAPS IN THE SOURCE, handled in the sync: from_date is NOT applied (verified 16 Sep 2026 — a task list asked 2026-07-01 returned February, a ticket pull asked 2026-08-01 returned 2024), so the window is applied in packMarket/packQueue; and pulls are newest-first and capped, so a market whose deepest row starts AFTER the window opens is stored full:false, shown PARTIAL in the coverage drawer and contributes the rows it has rather than a part-year read as a whole one. 0000-00-00 = undated (in the totals, in no month, and the page says so); tickets windowed by LAST ACTIVITY, same 12 months; what has not been read is ABSENT NOT ZERO and the source line says how many markets are read and how stale the oldest is. Engine cloudflare/feedspark-deck/src/taskbook.js; the page carries a behavioural twin between /* ENGINE:START */ and /* ENGINE:END */ which tools/test_reporttasks.mjs (296 assertions, in qa_gate/presync/validate) LIFTS OUT BY NAME, running the same table against both AND lifting `tmBookPull` out of worker.js against an in-process stub MCP (no credential / refused credential / the real pull / the rotation moving on / a partial pull refused as a full year / an unreachable endpoint failing up rather than writing a half-built index). DECIMAL ALIGNMENT IN THE HOURS COLUMNS (Ray, 17 Sep 2026: "why these numbers are not aligned vertically, you kept making this issue btw") — table.st td.num was already text-align:right + tabular-nums, so every cell's right edge lined up; the actual bug is that right-align only lines cells up on their LAST character, and hrs() prints a bare '1' for a whole hour against '0.5' for a half (two characters shorter), so a whole-hour row sits two character-widths right of where '1.00' would put its ones digit the moment it stacks in a column against decimal rows — the same shape of bug on every hours column that stacks rows (Tasks, the ⊞ breakdown table twice, Tickets, Accounts, each one's totals footer). hrsCell(n) fixes every value to the quarter-hour grain the data is actually booked in (.00/.25/.50/.75 via toFixed(2)) so every value in a stacked column is the same width and the decimal points land in the same place; hrs() itself is untouched and still used for one-off figures that never stack (KPI tiles, chart labels, drawer stats, tooltips) where the trimmed trailing zero reads better. tools/test_reporttasks.mjs lifts hrsCell out of the page by name and asserts every td.num hours cell calls it rather than the bare hrs(), so the bug can't come back one column at a time. THE IMPORT PREVIEW HAS TO BE ON SCREEN (Ray, 22 Sep 2026: "import edits dont do anything anymore"): ⇧ Import edits read the file, matched every row on its Task id and built the whole diff plus the Apply button — at x:1449 in a 1440px window. The reader never broke; its HOST did. The preview was written into #tgbd, which PR #436 turned from a centred modal into the right-hand TAGS RAIL (.bd-box parked at translateX(102%) until something adds .on, the old .bd-dim rule deleted in the same rewrite) — so a correct dialog was painted just past the edge of the screen and clicking the button did, visibly, nothing. Sharing that host cost a second thing nobody had noticed: host.innerHTML wiped the rail, and rulesOpen() REUSES an existing .bd-box rather than rebuilding the shell, so it could find the import's box, skip building #tg-body and throw on the next line. The preview now has its OWN host #impbd and its own form — a MODAL, deliberately, where the rail is a landmark: the rail is what you work beside while you tag, this is a decision (read the diff, apply or cancel, gone). A scrim with the card nested inside it so a backdrop click is unambiguously a click outside, Esc closing it BEFORE anything underneath, focus on the button you are being asked to press, a full sheet under 760px. Matching, diff and apply are untouched. THE TRIPWIRE RENDERS IT (tools/check_tmimport.js, presync) because a source assertion could not have caught this — the markup in one file, the rule that hid it in another, shipped by a different PR, every string still right: it drives the real page through Chromium, imports a real CSV and asserts every EDGE of the dialog is inside the viewport, the card is inside its scrim, the rail is whole underneath it, Apply writes the judgements onto the rows, Esc closes it and it fits 390px; six fail on the pre-fix page, the first in Playwright's own words (element is outside of the viewport). One trap the harness itself had to learn: the real /api/state PUT echoes the MERGED map back and the page adopts what it returns, so a stub answering {} wipes every tag the instant it is saved. A SAVED VIEW IS THE SHAPE, NEVER THE ACCOUNT (Ray, 23 Sep 2026: "in this chart dissection > allow option to save a view and that same view can be applied across different client"): a view is what the chart splits by, how deep it nests, which form draws it, which series it counts, what the labels say and the rest of the query — the ACCOUNT is not part of that shape, it is the thing the shape gets pointed at, so the two get their own controls on one row under the split block. ACCOUNT rewrites ONLY the `client:` clause (the split, the nesting, the form, the series and every other search term stay exactly as they were) and SAVED VIEW restores all of the above and runs against whichever account is picked beside it — build the reading once on Superdry, save it, pick Reiss. The client term is LIFTED OUT when a view is saved and put back when it is applied (engine liftClient/withClient/accountPick/packView/viewQuery/sanitizeViews in src/taskbook.js, twinned into the page, one assertion table over both) rather than riding inside the view and quietly answering about Superdry on a screen headed Reiss. FIVE judgement calls, each of which could have gone wrong silently: a NEGATED account is lifted too (leaving `-client:Superdry` in a view and applying it TO Superdry returns nothing and reads as a broken view — the clause comes out whole and what it was travels as `was`, named on the card and in the save confirmation); a name carrying a SPACE or a COMMA is quoted back in (`client:House of Bruar` reads as three terms, `client:Monsoon, Accessorize` reads as a LIST of accounts, either way the view lands on the wrong rows); the picker FOLLOWS THE QUERY wherever it came from (a typed term, a preset chip, a breakdown-row click, a deep link) rather than being a second source of truth about what is on screen; THREE states because two would lie — one account, every account, and a query naming two or excluding one reads as NEITHER and says so in its own option, while a typed prefix (`client:rei`) is offered verbatim as "rei — as typed" rather than snapped to a name nobody typed; and PER DEVICE like every other reading preference here (fcc-tm-meas/-lab/-cleg/-untag), store fcc-tm-chartviews bounded at twelve, never /api/state — a view describes how one screen reads the book, and handing one to a colleague is what 🔗 Link is for. The search bar's own button is renamed ＋ Save this search, since two buttons called "save this view" on one page would be two names for two different things. ONE ROW, THREE MENUS (Ray, 23 Sep 2026, counting the controls: "you see how many button there are here ? - reorganise them to make it easy to see please, or add pop up menu/ on left and right to be less cluttered"): twenty controls over three rows are now SIX on one — ACCOUNT · SPLIT BY · ⊞ Break down ▾ · SHOW AS · ⚙ Display ▾ · ★ Views ▾; the three that decide what is on screen stay plain selects and the rest fold (Break down = the two `then` splits, Display = Include/Labels/on-chart naming/Hide-Not-yet-tagged/table, Views = the saved list + Save + Delete). FOLDING MAY COST A CLICK, NEVER THE TRUTH: each closed button states its own off-default state (the nesting path in words, an orange dot, the open view's name) and the subtitle beside the title names every choice a fold could bury (including the two it never used to mention — not-yet-tagged hidden, table below), all re-read by ctlChips() on every render so a restored view or deep link puts the row back in step; one panel at a time, Esc closes the open one before anything under it, the panel's side is MEASURED against the card (the row wraps, so which button is last moves with the width) and under 760px a panel becomes a card-width sheet. The nodes MOVED, they were not rebuilt — every id (cdim2/cmeas/clab/cleg/cuntag/ctab/cview/vsave/vnm) is the one the restore and the harnesses already reach for. THE TRAP, AND A PRE-EXISTING ONE IT EXPOSED: a flex row IGNORES `hidden` (the UA's [hidden]{display:none} loses to any class that sets display) so all three panels painted open on load — and the same rule had been costing the page two live bugs, `#vdel` (a .btn) and `#cuntag-l` (a .tog) having NEVER been hidden, so "Hide Not yet tagged" showed on every dimension since it shipped; one rule states it for all of them and the tripwire asserts what is PAINTED, not what the property says (trusting the property is what let this same bug ship open on the AI Quote card). NAMING THE MARKS ON THE CHART, NOT IN A BLOCK BESIDE IT (Ray, 23 Sep 2026, drawing two curved arrows onto a donut: "actually checkbox 'legend on chart' - the label can appear with an arrow and light italic directly on chart like this"): a swatch row asks the eye to carry a colour across the card and back, a leader puts the name where the wedge is — so on the PIE and DONUT the toggle (now "Name the marks on the chart", because it does two different things) draws each slice's own name beside it: a curve out of the arc in the slice's OWN colour, a dot where it leaves the ring, the name in light italic and its hours in bold; every other form keeps the key across the top (bars and columns carry their names on the axis, a line chart genuinely needs a key). FOUR rules keep it from becoming the mess a bad leader layout is — the reason to refuse a label is NO ROOM not a small number (the cap is the column's own height in labels, filled BIGGEST-FIRST, with only a hairline floor under it; this shipped wrong once with an angle-only 2.5% floor that refused eight ordinary 12%-of-the-ring slices, and check_tmviews fails on exactly that); nothing crosses (laid out per side in the order the slices sit round the ring, pushed apart to LEAD_GAP, a column past the box SHIFTED WHOLE since clamping each label lands two on one line); the NAME takes the truncation never the figure; and whatever is not labelled keeps a swatch, so no wedge is ever identified by colour alone. The real Lato italic is loaded (ital,wght@…;1,400) — a synthesised oblique at 400 is a smear and these labels are the chart's only naming of a slice. Both are pinned in tools/check_tmviews.js (54 assertions) + source assertions in tools/test_reporttasks.mjs (877). Harness: the engine half in tools/test_reporttasks.mjs (both copies), and the half a source read cannot reach in tools/check_tmviews.js (Playwright, presync) — it builds a reading on Superdry, saves it, reads the stored record back to prove it carries no account, points it at Reiss and at House of Bruar and checks the chart is drawn from THOSE rows, changes the shape, restores the view and asserts it lands on Reiss with no trace of Superdry; three assertions fail the moment packView stops lifting, four more if the picker stops following the query. Docs: docs/TASKMANAGER.md
GET  /api/hours (+ docs/hours_widget.html, KV tmtrail, state:hourspost) → THE HOURS BADGE, EVERYWHERE (Ray, 16 Sep 2026: "this hour report should appear everywhere in Watcher, for example with workflow, and flag whether the client is negative or not … the trajectory of the past three months of client activity per hour … a hovering pop-up, so it doesn't clutter the current dashboard … display it within brand[ dossier] and other areas in the F[C]C that are appropriate for an AM to decide whether to continue the task with the current hours. Obviously, there are cases where a client is negative, but because of relationship smoothing, the AM may still continue the task"): the third lane of the TM integration — /api/tm puts hours on a FIGURE (Leadership), /tasks makes the DATABASE searchable, this one puts the balance AT THE MOMENT OF THE DECISION. docs/hours_widget.html is injected on every app page beside the editor/presence/Feed Chat/phone layers and renders an 8px state DOT on any element carrying `data-hrs="<client>"` plus ONE popover shared by every dot (hover 120ms in / 260ms out, click pins, Esc closes, bottom sheet under 760px) — EXCEPT where a page mounts `#fcc-hrs-dock` (Workflow's left rail, 16 Sep 2026), where the same body() renders INTO the page, hover opens nothing and a click loads that client into the panel — the dot is the entire footprint on the host page, which is "doesn't clutter the current dashboard" taken literally. AUTOMATIC COVERAGE: any module page picking a brand from its own `<select id="brand">` gets a badge that follows the selection with NO change to that page (KWCal, Feed Lab, Volume, Overlays, Schedule, AI Quote). EXCEPTION RULE `data-hrs-flag`: on a long table (Workflow intake, the /tasks accounts tab) the dot is drawn only for tight/over/served/held — a healthy dot repeated down 500 rows is decoration, not information. SIX STATES: none (never synced — an unread client is NOT zero hours), ok, tight (under a quarter of the block left), over, SERVED (negative AND the team chose to continue) and held (negative, new work on hold). RELATIONSHIP SMOOTHING IS A FIRST-CLASS STATE: the balance is a FACT the reports database states, the posture is the team's DECISION about it, and the two are stored and displayed apart — without `served` a deliberate call to carry an account through a renewal would render as an unhandled red alarm on every page and AMs would learn to ignore the dot; the popover therefore never says STOP, it states the balance, states the decision and leaves the call with the AM, and where a posture is set an optional one-line WHY travels with it (the judgement is the part the next person needs; the state alone only says somebody clicked a button). The posture lives in shared-state namespace `hourspost` (/api/state, kvmerge, X-Sync-Base, client-scoped like every Workflow route) so one AM's call is the team's call. THE TRAIL: tmBookPull already reads each client's task rows, so TB.trailOf folds them into a compact `tmtrail` record on the SAME firing (3 months × [billableQ, nonbillableQ, n] quarter-hour ints) and GET /api/hours serves it merged with the balance (tmidx) and the posture, scoped per signin; the popover draws three STACKED bars (billable below, non-billable above, 2px surface gap, direct value labels, legend carrying both numbers = the relief the palette validator requires on the light orange; #2563EB/#ED6F0B light, #4C82E0/#C67B28 dark, both validated). THREE HONESTY RULES in the maths, because this is read at the moment someone decides whether to keep working an account: the CURRENT MONTH IS PARTIAL (hatched and labelled, never plotted as finished — else every account looks like it fell off a cliff on the 3rd), a PARTLY-READ BOOK IS A FLOOR (read/total travel with the numbers so the popover says "4 of 6 markets read" rather than implying the whole account), and BILLABLE/NON-BILLABLE NEVER MERGE; the trend refuses a direction on fewer than two COMPLETE months and calls anything under 8% flat. ANCHORED ON: every Workflow ticket card + the ticket modal + the intake table (flag) + the client-filter chips, the Command Center brand dossier (the name, PLUS an Hours card in the portfolio band fed by window.FCCHours with no extra fetch, refilled on the `fcc-hours` event), Leadership's Hours & commercial cards, /tasks' accounts table (flag) + its "Hours by client" bars. SKIPPED: client decks (internal commercial data) and the embedded Feed Chat iframe (a popover in there would be clipped). NO CLIENT HOURS IN GIT — the widget bakes no figures and names no clients; its only source is the route. Harness tools/test_hoursbadge.mjs in qa_gate/presync/validate LIFTS the widget's hand-written engine twin out by name at /* FCC-HOURS:ENGINE-END */ and runs it against the SAME assertion table as src/taskbook.js. Docs: docs/TASKMANAGER.md
GET  /schedule (+GET /api/schedule) → SCHEDULED WORK module — ALSO FEEDS THE HOURS POPOVER (Ray, 16 Sep 2026: 'This pop-up over retainer should also include the skipped schedule work … i prefer the dotted green and orange bar for go and skip that you have, just try to fit 3 at least in that popup'): src/schedwork.js › skipDigest derives a TINY record (top 3 tasks per brand, a 12-char strip each — g go / s skip / n undecided / - NOT SCHEDULED, which is never a skip) that /api/schedule writes to KV `schedskip` as a side effect and /api/hours serves, so the badge costs one KV get instead of a multi-tab workbook parse; WRITTEN ONLY FROM AN UNSCOPED READ (the route's cadence is already client-filtered, so a scoped signin would persist a partial house and every other brand would read as 'no skips'); the popover renders the strips in /schedule's OWN colours (#2E7D32 go / #ED6F0B skip / outline not-scheduled) so the two surfaces can never disagree, says how many tasks it left out, and shows an all-green brand rather than an empty block. The hours trajectory beside it stays THREE months (TRAIL_MONTHS) — the 12-month view is the go/skip strip, not the hours bars.  (Ray, 15 Sep 2026: 'Scheduled Work vs. Workflow work … each week will be dated in hidden sheets … keep track of the clients that are in my brand dossier, see the cadence of which column P AM status indicated a skip, track how many consecutive months the task was skipped'): the content team's 'Scheduled Title and Keyword Optimisation' sheet (16yYMQ50__qv-9V-mtIebm8Ih0-2huE452bgRz3f70Tw — the ASPL weekly schedule of Keywords / Titles / Product Types / Data tagging / Social+Short titles per client × market, the AM answering Go ahead or Skip) is a stream of work OUTSIDE Intake. Engine src/schedwork.js (pure, harness tools/test_schedule.mjs in qa_gate/presync/validate): the visible Content-team-task-summary tab = one row per client × task × week since Jun 2025 (week in `Dates`, the AM's call in `AM Status` = column P, `Reason` for negative-hours go-aheads); the 76 HIDDEN weekly tabs before it ('KWs 1906', 'Titles 0201', 'PTs 1206' … back to Nov 2024) carry the AM's call in `AM Confirmation` + the ASPL outcome in `Final Status`, with the week ONLY in the tab name (DDMM, no year → dated as the most recent such date on or before the consolidated tab's first week); 20 header layouts, so columns resolve BY NAME; decision precedence AM Status → AM Confirmation → Final Status (Skip/Cancelled/No/Not for this week = skip; Go ahead/Yes/OK/Approved = go; bracketed notes = none); sheet clients map onto the FCC roster (dossier KV ∪ PLAN_SHEETS ∪ DEFAULT_FEEDS) accent/space-blind + first-word ('Benefit Cosmetics' → Benefit), unmatched stay sheet-only; CADENCE per brand × market × kind: a month is skipped when it was scheduled and never went ahead, an absent month is neither a skip nor a break, streak = trailing decided months skipped (+ weeks, hours of scheduled work skipped in the streak, last go-ahead, skip rate). Worker: live read through the service account when the sheet is SHARED with it (1 meta + batchGet of every tab UNFORMATTED, KV schedwork 6 h, ?refresh=1; page names the SA email to share with when it is not), else the committed snapshot ops/schedule/scheduled_work_<date>.json (rebuilt by `python3 tools/schedule_ingest.py <xlsx export>` — the Drive text export loses tab names, the xlsx keeps them + hidden flags); rows scoped per signin like every Workflow route; ?lite=1 = cadence + brand summary. Page docs/FeedSpark_Schedule.html: source line (live / cached / snapshot + the share hint), KPIs (brands on the schedule vs dossier, tasks on a streak, longest streak, latest week skip·go, hours skipped 90d), brand cards (worst streak, tasks on a streak, hours), the cadence table (12-month strip per task, streak, since, last go-ahead, skip rate, hrs, latest week + reason class; dossier brands by default, 'whole sheet' + 'show dropped' toggles, deep link ?b=<Brand>&k=<kind>), row → drawer with every week's AM word / outcome / hours left / days / comment. Workflow Intake gains the ⏭ Scheduled work hand-off band (#sw-band, reads ?lite=1, brand chips → /schedule?b=). Docs: docs/SCHEDULE.md
GET  /feedchat                  → Feed Chat ENGINE page — OUT of the module nav (Aug 2026): surfaced instead as the FLOATING BUBBLE bottom-right on every app page (docs/feedchat_widget.html injection; click → chat window HOVERS the current page, iframe of /feedchat?embed=1 which strips topbar/hero/foot; 3 window sizes S/M/L remembered in localStorage fcc-feedchat-size; Esc/✕ close, full-screen on phones; the page stays URL-reachable for QA/deep links). The chatbot itself: AM chatbot over the live feeds — plain-English questions ("is there duplicated title in Reiss UK Shopping feed?") routed to DETERMINISTIC checks (dup title/id/image, missing_<field>, title_len, caps, html_desc, kw_sat, price_zero, audit); /api/claude parses intent + composes the 2-3-sentence verdict (heuristic parser fallback when no key — answers stay computed either way); the browser streams /api/feed/proxy once per brand|market into window.__feedchatcache ({id,ti,de≤400,im,pr,sp,br,gt,pt,av,kw} rows; numbered product_type 2..10 = kw flag, excluded from PT winner pick), every number computed in-page never by the LLM; answer bubble = verdict + stat tiles + offender table + ⬇ full offenders CSV (blob) + → Brief this fix (/workflow?brief= cat technical, task 'Feed Fix - <check> - <Brand> <MKT> - MMYY'); multi-turn context (LAST client/market: "and in DE?" reuses the brand); suggestion chips bypass parsing (structured); unknown brand → roster nudge; the check library is a VISIBLE question stack (QSTACK grouped clickable questions — in the welcome, on 'help'/what-can-you intents, and appended brand-scoped under every default audit when a brand is mentioned vaguely; chips one-click run with brand prefilled); the stack is AM-EDITABLE in-page (✎ Edit questions: reword, per-question alt phrasings, ＋ Add onto any existing check id, hide/show — shared via KV qbank on /api/feedchat kvmerge, git QSTACK = seed) and routing is £0-FIRST: precise keyword rules → bankRoute (rare-token-weighted match over wording+alts, brand/market stripped then resolved separately, unknown-brand "in the X feed" guard nudges instead of silently using context) → /api/claude ONLY for phrasing neither matches when a key exists (free routes also skip the composed verdict) — the module never NEEDS the Anthropic key
GET|POST /api/tasks/remind      → due-today task reminders (owner-only): the 12:00 GMT cron firing emails each owner (ray/steven — OWNER_EMAILS in taskremind.js) their plan tasks due TODAY whose status is still Open-bucket (taskremind.js mirrors the PAGE's bucketOf — Briefed ≠ open; month-section yyyy-mm-01 dates are never deadlines); runs off the plan warm's freshly parsed tasks, once per UTC day (KV taskremday), delivered via the Label Guard outbox/Gmail bridge with day-scoped ids (double-send impossible) + `sig` field. GET = dry preview, POST = fire now ({force} re-runs)
GET  /api/buildlog              → Build Log feed (GitHub PRs/branches/overlap, KV-cached 10 min; optional GITHUB_TOKEN secret)
GET|PUT /api/buildqueue         → Build Log "not built yet" queue (kvmerge-backed, concurrency-safe)
GET  /api/buildsuggest          → SUGGESTED NEXT BUILDS (Ray, 22 Sep 2026: "within Build Log also start suggesting the top five new features to build for FCC to improve revenue, churn, client retention, AM efficiency, increase billable hours, or reduce the time taken for each account work"): a wishlist in git would suggest the same five features whatever the book was doing, so every candidate play instead carries a RULE that reads the FCC's own live stores and either produces a number or produces nothing. src/buildsuggest.js (pure) holds TEN plays — over-servicing report (tmidx negative balances), skip win-back (schedskip), quote chase + invoice handoff (saved quotes gone quiet / Delivered-not-Billed), auto-chase (briefs not moving for ten days), gap-to-quote + content-quality fix packs (goldenidx), coverage sweep (wired feeds never scanned), quiet-account radar (no brief in sixty days), arrivals auto-brief (voldobidx, LAST COMPLETE month — the running one is a part-month and would read as a collapse) — each scored `lever weight × evidence size × effort factor`, the formula printed in the panel footer from the engine's own BASIS object so the two can never explain a number differently. THREE rules make it a backlog rather than an oracle: an UNREAD signal is never a zero (a store nobody has synced is listed with the reason, because "no negative balances" and "the Task Manager never synced" are opposite findings), nothing already queued / in build / shipped is suggested (keys matched against the buildqueue AND the PR history — and ＋ Queue it writes the rule's OWN title, which is what makes the play drop off the next load), and the candidate list is FIXED AND FINITE on purpose: an LLM asked to invent features would invent the numbers under them too. Owner-only like the rest of the Build Log; the panel is the first one on /activity#build. Harness tools/test_buildsuggest.mjs (48 assertions — incl. that one play's title can never suppress another, which caught a real collision on its first run) in qa_gate/presync/validate. Docs: docs/BUILDSUGGEST.md
GET  /api/news                  → industry news digest, git-bundled from `docs/news_digest.json` (Text module). The Command Center pops it to the AM once per digest `id` (read state in localStorage `fcc-news-read`); the 📣 topbar button reopens it. Rewritten every weekday morning by the news Routine, which follows `.claude/skills/feedspark-news-digest/SKILL.md` — that skill (not this file) holds the source list and the editorial bar
GET  /deck/yumove               → YuMOVE strategy deck (git-bundled + injected editor)
GET  /api/edits?page=<slug>     → return a page's saved edits as JSON
PUT  /api/edits?page=<slug>     → save an edit patch (merges with existing)
DELETE /api/edits?page=<slug>   → clear a page's saved edits
GET  /api/template              → info only; pages are git-bundled (push to main to change them)
GET|PUT /api/briefs             → Workflow brief pipeline store (KV `briefs`; ONE RECORD PER REAL TASK means SAME WORK, not similar wording (Ray, 16 Sep 2026, pointing at his own Brief Ledger: REIS-20260910-02 Cashmere/Merino and REIS-20260914-01 Gifting, both briefed by Steven, both at Test running — "not appearing in InTake or my project plan"): refreshDupMap anchored each ticket to its nearest Intake row at taskDice ≥ .45 and collapsed every ticket sharing an anchor onto one primary, and a collapsed duplicate is skipped by reconcileBriefTasks, hidden from the board, the KPIs, the SLA and the courts. On a module task family ("Keywords Optimisation - <theme> - Marketing Planner - <MMYY>") the shared scaffolding IS most of the words, so two tickets differing only by the theme — the one part naming the work — score .6–.8 and sailed past both the .45 anchor bar and the .6 loose bar: every Reiss keyword optimisation collapsed into one, and three distinct themes vanished. sameWork/soleWords now decide it — two names are one record only when NEITHER names something the other doesn't, singular/plural-canonical so Keyword/Keywords is not a difference; a fuller version of the same name still collapses, two that each carry their own subject never do. Deliberately conservative: a duplicate left showing twice is visible and one click to merge, a wrongly collapsed ticket is work that silently ceases to exist. THREE more links in the same chain, each on its own enough to lose the task: reconcileBriefTasks was called ONLY from fetchLive's success handler, so when the plan read beat the briefs fetch BRIEFS was still {} — the pass saw nothing and was never called again for the life of the tab (it now also runs when the briefs land and on the 60s poll, self-guarded on LIVE_CONN); manualTasks suppressed the local stand-in row "once the real plan contains it" on the FUZZY sameTask, so a freshly-filed row hid behind a sibling in the same family (exact wording now — the recurrence case had already needed that rule); and shApply DELETED any shared-state key missing from the server's map, so shBoot's own reply, snapshotted before the row existed, wiped a task filed seconds earlier out of the board while it sat in the sheet — it now skips deletions for a namespace this tab has unpushed (SHDIRTY), the rule bfPoll already follows for briefs. The same fix lands on the two sibling guards — briefRow ("is this task already in Intake?") is EXACT wording, since the fuzzy test matched a sibling theme and stranded the ticket, and b.planned is a HINT not a veto, so a ticket wrongly stamped filed (or one whose sheet write never landed) is healed on a later open rather than stranded for good. BRIEFED BY (Ray, same day: "any '-' brief by is obviously Ray - so add me name too"): briefedByCell names the owner on any brief raised before the `by` field shipped — a DISPLAY fallback, dotted and titled as the inference it is, never backfilled onto the stored ticket; access.js displayName turns ray@feedspark.com into "Ray" (the owner's row used to read the literal word "Owner") and /api/access?me=1 carries ownerName for EVERY signin so the whole team reads the same name; per-key merged via kvmerge.js + X-Sync-Base — same for /api/clients). DURABLE + LIVE (Ray, 16 Sep 2026, with the evidence: Steven raised Cashmere/Merino and Gifting through the module, the [FS Brief] emails went out, and Ray's board still read NOT BRIEFED with the tickets in neither his Intake nor his pipeline): the save was fire-and-forget — ONE PUT, `.catch(function(){})`, and the composer toasted "Brief saved to pipeline" BEFORE the server answered, so a failed save left the team's only record of the work in one person's localStorage with the brief email already sent. persist() now marks the map dirty and bfPush RETRIES with backoff (2s→60s), flushes on `online`, and only advances BRIEFS_BASE off a response it actually ADOPTED (a stale map carrying a fresh read-stamp deletes by absence on the next whole-map save); bfWarn keeps "⚠ Brief not saved to the pipeline yet — still on this device. Retrying…" ON SCREEN until it lands. AND THE ONES ALREADY STRANDED (Ray, 16 Sep 2026, from Steven's screen: Cashmere/Merino and Gifting reading RUNNING off tickets nobody else's pipeline had): a brief whose save failed lives only in that browser's mirror, and the boot replaced BRIEFS with the SERVER's map — so the next load dropped it and the next save overwrote the mirror; the retry saved new work but would have quietly finished off the old. bfRescueLocal() unions the mirror back in on boot and publishes, CREATE-ONLY (a brief the server has is never touched) and never resurrecting one this device already synced — bfMarkSynced() stamps `sv` on every brief after a successful push, so a LATER absence reads as somebody's deletion rather than a lost save; it toasts what it published rather than doing it silently. Because MANUAL intake rows are shared too (/api/state), the same open also publishes the intake row, and resyncPlanWrites re-sends any row the Project Plan sheet never confirmed, and bfConfirm turns the composer's toast into "syncing…" → "saved to pipeline · <id>" only once the server has it. And the board READS the pipeline again: bfPoll (60s + on tab-visible, skipped while this tab holds unsaved work) — Workflow fetched /api/briefs once at boot and never again, so a brief a colleague raised after your page loaded simply never appeared. NOTE the deliberate gap this exposed: an [FS Brief] email from @feedspark.com is excluded from the Gmail capture by the internal-sender carve-out, so a colleague's brief surfaces ONLY in the pipeline — which is why its durability is not optional. Harness: `tools/test_teamsync.mjs` (in presync) — browser-level, TWO Playwright contexts = two real users with separate localStorage against an in-process stub of /api/briefs, /api/state and an idempotent /api/sheets/append: a brief reaching the store and an already-open board, a refused save never reported as saved and landing on retry, the poll never overwriting unsaved work, the stranded-brief rescue (published once, a synced-then-absent brief never resurrected), a COLLEAGUE's unconfirmed plan row written to the sheet by a browser that never created it, the exact-wording settle, a briefed ticket with no plan task being filed beside a fuzzy sibling with no theme collapsed into another, and the ledger naming the owner. [8] [9] [10] [12] fail on the pre-fix page
GET  /api/hours → …AND THE AM'S ADDRESS, FOR THE CC (Steven Opuni via Ray, 17 Sep 2026: "Would it be possible for 'Draft in GMail' button to automatically CC the AM of the account in the email draft?"): the Task Manager states the AM as a NAME (primary_am 'Ray'/'Steven' — the whole live book of 71 markets), never an address, so access.js › amEmail resolves it against the access directory the owner maintains, then the addresses the worker holds for certain (ownerEmail + taskremind's OWNER_EMAILS), and returns NULL rather than falling back to <name>@feedspark.com — nearly every FeedSpark address fits that shape but andrew@aroxo.com doesn't and 'Tech-am' plainly doesn't, and a brief copied to an address that doesn't exist (or to the wrong person) is worse than no copy. /api/hours carries `amEmail` beside `am`, so the Workflow composer reads it off the hours badge's own fetch with no extra request. WHY IT MATTERS BEYOND COURTESY: an [FS Brief] from @feedspark.com is excluded from the Gmail capture by the internal-sender carve-out, so a colleague's brief reaches the FCC through the pipeline ALONE — CC'ing the account's AM puts it in a mailbox the push DOES read, where recoverBriefsFromEmail() rebuilds the ticket from the brief's own machine-readable block. Workflow: ccList/ccParam add `&cc=` to every link that drafts a brief (composer #bg-gmail, the ticket modal's re-draft, both batch drafts — the union of the batch's AMs, deduped; the chase re-open is deliberately excluded, it threads an existing conversation rather than drafting), NEVER copying the person drafting, and the composer states which of three honest cases holds — an address to CC (tickbox, on by default, remembered per device fcc-bg-cc), an AM with no address on file (named, not guessed), or you are the AM; a brand whose hours were never read claims nothing either way. The helper is accountAm() NOT amOf() — the page already has amOf(client)→AM NAME that the AM filter, workload panel and timeline call, and a hoisted second declaration handed all eight callers an object where they expect a string. Harness tools/test_amcc.mjs (34 assertions, the page's rules LIFTED BY NAME) in qa_gate/presync/validate. Docs: docs/PLAYBOOK.md
GET|PUT /api/access             → INDIVIDUAL ACCESS (Ray, Aug 2026): per-user client-scoped Workflow. `?me=1` = caller's own scope (any signin); bare GET/PUT = the directory, owner-only (KV `accessdir`; git seed in src/access.js = Radostina→House of Bruar until first save; owner edits via the 👥 Access button in Workflow's intake live-bar). Scope resolution (src/access.js): owner → full house; directory row OR client-team ALIAS auto-rule (email local part slug-matches a client name: houseofbruar@feedspark.com → House of Bruar, zero config, accent/case-folded) → scoped; unassigned → full house (scoping only NARROWS). Enforcement is SERVER-SIDE on the Workflow data routes: /api/briefs GET filtered + PUT re-injects foreign briefs before the kvmerge pass (a scoped whole-map save of a partial view would otherwise tombstone the rest of the board — tools/test_access.mjs pins this, wired into qa_gate/presync/validate); /api/gmail/intake items+calls filtered (unattributed rows stay owner-side); dismiss/calls-PUT/techam 403 outside scope. Workflow page mirrors it: 🔒 badge, client roster locked, plan rows filtered via inScopeClient. MODULE ACCESS (Ray, Sep 2026: "select individual module access for each person"): a directory row ALSO carries `modules` — the grantable feature modules (`MODULES` in access.js: workflow, deck-builder, feedlab, labels, ptypes, golden, volume, overlays, kwcal, aiquote, pricer) that signin may open. Absent/null = ALL (every existing signin unaffected); [] = none (landing only); owner = all. Enforced SERVER-SIDE on page serves (accessOf → `moduleDeniedHtml` 403 for a route in `MODULE_PATHS` outside the grant; the landing `/` is never blocked) and mirrored in the chrome by `MODGATE` — an injected `window.__FCCMOD` (the caller's granted slugs, or null=all) + a tiny script that hides ungranted `.tb-modules` links at runtime, so the STATIC nav stays byte-identical (check_nav parity holds). /leadership + /activity are NOT grantable — they stay owner-only. Client scope (`clientMatch`) and module grant are independent (a full-house signin can still be module-restricted). The 👥 panel gains per-person module chips (All/None); `/api/access` GET ships the `MODULES` registry, `?me=1` carries `modules`; sanitizeDir validates slugs. Seeded: Andrew (andrew@aroxo.com) — listed, unrestricted, tune in the panel. tools/test_access.mjs pins the dimension. 👁 VIEW-AS (owner preview): 👁 on an Access-panel row sets cookie `fcc-viewas=<email>` (2h max-age) → the worker — ONLY when the real Access identity is OWNER_EMAIL (viewAsOf; inert for everyone else, can never widen) — resolves accessOf AND every owner gate (realOwner: /activity, /leadership, /api/activity, pushlog, tasks/remind → 403 + exit link via viewAsExitHtml) as that signin, so Ray sees exactly their FCC; /api/access?me=1 carries viewAs{real,as} → injected docs/viewas_widget.html renders the fixed "Viewing as X · scope [Exit]" pill on every app page (inert without server confirmation); previewing yourself = no-op
GET|PUT /api/state              → SHARED WORKING STATE (Ray, 15 Sep 2026: "when Steven or any other user in the FCC uses the keyword calendar or any button or briefing, the updates are not seen by everyone but only by themselves because the data is saved locally … ensure this dashboard works simultaneously for all users and that nothing is kept for a specific user only, except me"). The Workflow board's project-plan overlays were localStorage, so an AM's status change, owner, due date, rename, hand-added row, deletion, hide or recurrence was invisible to the team and died with their browser profile. Thirteen namespaces (src/sharedstate.js STATE_NS: taskstatus, taskdue, taskowner, taskrename, deleted, hidden, recur, esrc, csrc, manual, wfremoved, senderbrand, runsla, pbtax, rollout) each get their OWN kvmerge envelope under `state:<ns>` — independent merges, per-key tombstones, the same X-Sync-Base read-stamp rule /api/briefs uses, so two people editing different tasks both survive. Client scoping mirrors /api/briefs exactly: a namespace declares how an entry maps onto a client ('key' = "<client>|<task>", 'self' = the key IS the client, 'field' = the value carries .client, null = house-wide config), a scoped signin sees only theirs, and every foreign entry is RE-INJECTED before the merge — their whole-map save of a partial view would otherwise tombstone the rest of the board (tools/test_sharedstate.mjs pins that trap, in qa_gate/presync/validate). An unknown namespace can never become a KV key. Page side (Workflow): the maps are handed out ONCE by shMap and only ever mutated in place, so every `var X=shMap(ns)` holder stays live; localStorage is the OFFLINE MIRROR (first paint never waits on the network, an offline edit still shows and is kept) and the truth is the store; a failed push RETRIES with backoff and flushes on `online` and on the poll, so an offline edit still reaches the team without another edit to trigger it; the first full render is chained off BOTH the briefs and the shared state; shPoll (60s + on tab-visible) brings a colleague's change in without a reload; and first open on a device PUBLISHES whatever that browser was holding alone (union only — it can never remove another user's). The Command Center dossier reads/writes the SAME taskstatus store, and the Playbook's tuned taxonomy syncs through pbtax. WHAT STAYS ON THE DEVICE, deliberately: theme (fcc-theme), nav collapse, panel open/closed (fcc-kw-band, fcc-bp-open, fcc-bl-open, fcc-aiq-open, fcc-pricer-fg), plan-window length (fcc-kw-win), chat size (fcc-feedchat-size), density/demo/reference toggles (fcc-ovl-dense, lg-demo, gr-demo, pt-ref, lg-ref, gr-ref, fcc-pricer-actuals), a personal filter (fcc-filters) and the display cap (fcc-briefcap) — those describe one screen, not the work
GET|POST /api/claude            → Tachyon copilot proxy to Claude Messages API (needs ANTHROPIC_API_KEY secret)
GET  /api/tm  (+POST /api/gmail/push {tmpush}) → TASK MANAGER hours (Ray, 16 Sep 2026: "connect to FS's Task Manager which has an hours report on every task and map it back into our system"). SOURCE = the team's custom connector MCP `feedspark-reports` (mcp.dashboard.feedspark.com/mcp — tools get_client_list / get_task_list_for_client / get_tickets_for_client / get_ticket_detail), NOT an HTML scrape (the login was IP-rejected from a sandbox and the MCP superseded it). get_client_list = one row per client x market from sh_merchants with the hours ALREADY computed: allowance (monthly retainer block), used_hours (this billing cycle), balance (remaining incl. carried), balance_health (negative/warning/healthy/zero/no_allowance), primary_am, market_flag (0 live / 2 FS-internal / 1 stopped — ELC sits on 1 despite being worked). The shared pure src/tmparse.js rolls markets up PER BRAND (aggregateClientList: sums, worst-market health, markets by used; brandKey folds YuMove/YuMOVE) — the grain Leadership works in. AUTOMATIC SYNC (Ray, 16 Sep 2026: "an automatic scan or push … has to sync four to six times a day … If a brief is being sent from FCC and users are logging hours, reporting to clients then that sync must be automatic. I don't need a manual push or manual pull"): the WORKER PULLS THE MCP ITSELF — no agent, no Action, no Claude session. wrangler.toml cron `15,45 * * * *` (its own firing = its own subrequest budget) → worker.js › tmPull: tmMcp is a minimal Streamable-HTTP MCP client (JSON-RPC over POST to https://mcp.dashboard.feedspark.com/mcp; initialize → notifications/initialized → tools/call; JSON or SSE answers parsed by src/tmmcp.js › parseRpc; Mcp-Session-Id honoured) authenticated with the Worker SECRET `TM_MCP_TOKEN` (Ray set it in CF 16 Sep) as `Authorization: Bearer` — the endpoint answers 401 with no OAuth discovery, so if the team's check reads another header set the VAR `TM_MCP_AUTH` to its name (e.g. X-API-Key; 'raw' = the secret carries its own scheme), `TM_MCP_URL` overrides the endpoint; no redeploy for either. Per firing: ONE get_client_list (flag_scope live) → aggregateClientList → tmStore (the ONE writer both lanes share: tm:<client> + tmidx, a record re-written only when its figures moved — sigOf — so a quiet firing costs one KV write) + TM_TASK_PULLS=4 get_task_list_for_client reads (from_date 21 days back, limit 200; rows carry `raw` ≈1.9KB each) on the MARKET ROTATION (planPulls: only worked markets — hours or a block this cycle, or a brief in flight per briefCodes over KV briefs — scored by staleness ×2 when hot ×1.5 when carrying hours, so a never-read market always leads and none starves) → tmtasks:<client> {markets:{GB:{n,total,hours{h,nb,sc},byRef,byOwner,months,recent≤60 compact rows}}} and tmhours = BRIEF ID → HOURS: a TM task carrying the [ibfref:…] token in its title OR notes (the composer writes it into every [FS Brief] subject; the team keeps it on the task — real specimens: title-token, notes-token, and a Batch-Setup task whose notes quote the brief) is THAT ticket's work — time_taken (billable) / time_taken_nonbill / time_schedule summed per ref across its tasks, owners joined, done count, state = worst of done/test running/with client/on hold/open; EXACT TOKEN ONLY, a tokenless task is the brand's own work and never lands on a ticket; mergeHours replaces a ref's record when its market is re-read and keeps a ref the window rolled past (hours are final). tmstatus = the honest state {state ok|no_token|unauthorized|unreachable|error, at, ok_at, fails, error, auth (header name), url (host), clients, markets, tasks, refs, pulled[], changed, rot} — never the secret. READS: GET /api/tm (Access-gated, scoped per signin: owner = whole book, a client-scoped signin only their clients) = the rollup + `status`, source 'live' once anything was pulled/pushed else 'seed'; ?client= = record + the tmtasks book; ?hours=1 = tmhours filtered by the brief's client; ?pull=1 = OWNER-ONLY sync-now (runs tmPull, &pulls=N≤8, logged 'tm-sync-now'). SURFACES: Leadership › Hours & commercial — block ← allowance, Used ← used_hours (TM chips; a HAND-SET figure always wins, an edit clears the flag), balance readout, and the source line tells the truth (LIVE synced N min ago · markets · ticket hours on N briefs / waiting for TM_MCP_TOKEN / REFUSED — check the token or TM_MCP_AUTH / unreachable since … showing the last good sync) with ⟳ Sync now; Workflow — every ticket card wears ⏱ total hours (tk-hrs; tooltip billable · non-billable · scheduled · tasks · owners) and the modal gets a TM pill + "Hours logged in the Task Manager" rows (date · task · owner · state · hours), read from /api/tm?hours=1 at boot, every 5 min and on tab-visible. NO CLIENT HOURS IN GIT: the figures live in KV only (the earlier committed snapshot + seed were removed once the pull was automatic — the push classifier had rightly flagged per-client hours going to GitHub); before the first sync /api/tm says source 'none' and Leadership shows the hand-set defaults with "no synced hours yet"; POST /api/gmail/push {tmpush:[…]} (same key + Access bypass as the XML scan) remains for an external producer — both lanes write through tmStore. Known gap: ELC sits on market_flag 1 in the TM, so flag_scope live omits it — the fix is in the TM, not here. Harness tools/test_tmmcp.mjs (pure module: JSON/SSE parsing, auth modes, rotation, ibfref sums on the real specimen shape; then tmMcp/tmStore/tmPull LIFTED from worker.js by name and run against an in-process stub MCP — 401, session id, SSE + JSON results, 500, change detection, rotation, X-API-Key mode; plus the cron/route/page wiring) + tools/test_tm.mjs (rollup, health, no-hours-in-git, store/scope) in qa_gate/presync/validate. Docs: docs/TASKMANAGER.md
GET  /i18n/engine.js · /i18n/vi.json → UI LANGUAGE (Ray, 15 Sep 2026: "build completely a VI (Vietnamese) toggle for my view only"): the DOM-translation engine (docs/i18n_engine.js, UMD, node-tested) + the hand-written seed dictionary (docs/i18n/vi.json — ~1,800 entries covering every app page's chrome, numbers templated as {n} so one entry answers every count; protected tokens = brand/client/platform/product names + SKU/AM-style acronyms, never translated, plural-canon SKUs≡SKU). The widget (docs/lang_widget.html, `#lang-tgl` EN/VI pill in the topbar) translates every text node + title/placeholder/aria-label/alt in place, MutationObserver keeps re-rendered content translated, exact originals restored on the way back; never touches inputs' values, textareas, contenteditable, the dossier's data-ed fields, code, the Templates copy bodies (the Copy button reads them) or the wordmark. Widget injected ONLY for the real owner identity (realOwner — never another signin, never under view-as)
GET|POST /api/i18n?lang=vi      → owner-only runtime lane for strings the seed cannot answer: GET = the learned map (ONE KV key i18n:vi — no per-string KV traffic); POST {lang,strings≤80} = translate the misses via Tachyon (Claude, glossary system prompt, whole-word protected tokens enforced server-side by i18nKeeps), cache into the same key, return the map; no ANTHROPIC_API_KEY → error:'no_key' and the widget degrades to seed-only (title says so, zero further POSTs). Harness tools/test_i18n.mjs (engine rules, seed integrity, worker parity + gating) in qa_gate/presync/validate; docs/I18N.md
```
- **Injected on app pages** (not client decks): the **ⓘ COLLAPSE** (`docs/instr_collapse.html` —
  Ray, 17 Sep 2026: "if any subtext is longer than 1 sentence - hide with [i] button pls across the
  platform": EVERY explainer element on an app page — the subtext classes, hero intros, a paragraph
  under a heading or directly inside a card/section — is judged by SENTENCE COUNT, not by a class
  list: more than one sentence (and a dozen words, so a two-clause data line never counts) collapses
  behind ONE ⓘ per card riding the card's own heading, one sentence stays visible; `data-instr` = an
  explicit opt-in that collapses regardless (the chart verdict lines Ray crossed out), `data-no-collapse`
  on an element or ancestor = the opt-out; never inside a table/form/list item/modal/chat stream/ticket
  card/answer bubble; the `sentences()` counter protects e.g./i.e./vs./etc./initials/months/decimals/
  numbered steps; remembered per device (localStorage fcc-instr-open) under a STABLE key = the card's
  id or heading, re-applied on every DOM mutation pass; harness tools/test_instr.mjs in
  qa_gate/presync/validate), the **SKIM VIEW** (`docs/digest_widget.html` — Ray, 18 Sep 2026,
  holding up the Meta Ads Manager app on an iPhone 16 Pro: "the whole dashboard is not functional on
  mobile users … it only shows what necessary to be shown … only necessary information for AM to make
  decisions while using mobile phone and make it as on the go as possible … if you go on the desktop view
  that would be more if needed. Allow using a lot of collapse and expand feature": under 760px every page
  opens as a digest — the hero + a 3-up KPI band stay, and EVERY section below becomes one tap row (its own
  heading restyled: chevron · title · a digest read off the content — the page's `data-m-digest` word, else
  KPI headlines, `.al-row` alert counts, table rows, repeated items) with the content folded behind it. Two
  shapes, one mechanism: a block that CONTAINS its heading (section.cat/.card/.panel) and a heading above
  its SIBLINGS (Workflow's h2s, Leadership); nothing removed, no node moved — the fold is a class the desktop
  media query never applies, the heading keeps its buttons (i-popover, ⓘ, the page's own ▸ Show — a heading
  that already toggles is left to the page and styled into the row rhythm). Closed by default when the page
  has ≥2 sections (one is nothing to skim among), a section appearing AFTER boot opens (it arrived because
  the reader did something), per-device memory (localStorage fcc-m-open), #hash opens its fold, tables show
  8 rows behind "Show all N", Expand all / Collapse all above the first row, `window.FCCDigest`
  state/expandAll/collapseAll. Census at ship: Leadership 5,694→979px, Roadmap 9,862→844, Readiness
  6,332→844. Tripwire: tools/check_mobile.js rule 6 asserts the fold at first paint on a fresh device, then
  expands everything before its desktop-parity count; pins in tools/test_mobile.mjs. Docs: docs/MOBILE.md),
  the live editor widget (its STALENESS REPORT IS A TOOLBAR NOTE, NEVER THE RED BANNER — Ray, 21 Sep
  2026: "remove the red banner on top on the dashboard pls - i hate that it always appear (the deck
  template has changed)": the shape fingerprint moves on every push that changes an editable count
  and an app page ships several times a day, so "the deck template has changed … everything was
  applied" fired on EVERY load about nothing; `reportStale` → `showStale` now renders a `.de-stale`
  chip on the editor toolbar — grey when everything still applied, orange when entries were skipped,
  opening a `.de-stalepanel` card with the detail + the Clear-stale-entries button — and a dot on the
  ✎ handle ONLY when there is something to act on; `.de-warn` stays the voice of a save that failed or
  edits about to be lost (NOT_SAVED, unsaved-work restore, load failure, failed reset, ?raw=1);
  pinned in tools/test_editor.mjs), **FCC-PRESENCE**
  (`docs/presence_widget.html` — Google-Docs-style live avatars in the topbar: each open page
  heartbeats `POST /api/presence` per minute while visible, worker stamps the Access identity
  into KV `presence`, avatars = active ≤3min with green dot + name·page·ago tooltip; popover
  shows ONLY who is ACTUALLY LIVE right now — no always-on watchlist (Ray, Sep 2026: "only
  allow me to see who is actually live and using the dashboard"), so an offline teammate simply
  doesn't appear; PLUS an OWNER-ONLY "Team · last seen" roster in the popover (Ray, Sep 2026:
  "the activity for the user that last logged in … last seen at timestamp and date … everyone
  in my access") — everyone who has signed in (real identity + exact last-seen date/time, KV
  `presence` kept 90 days) ∪ anyone assigned in the access directory who hasn't logged in yet
  ("not seen yet"); server-gated (realOwner → `roster` on POST /api/presence; the shared live
  view stays live-only); heartbeats deliberately NOT in the activity log), the **UI language toggle** (docs/lang_widget.html — Ray's owner-only EN/VI pill, see /i18n above), the **PHONE LAYER** (docs/mobile_widget.html, every signin, Ray 15 Sep 2026: "complete overhaul for UX UI for mobile version — MIRROR desktop setting": under 760px the SAME module nav node becomes a fixed bottom bar of icon+label tap targets — same anchors, same order, the viewer's own ▦ bundling; the desktop collapse toggle collapses the bar; the ▦ sheet lists every module with labels; the presence popover, view-as pill and customizer become bottom sheets; the topbar is one 52px row; controls pages used to hide under 900px are shown again (Golden Record per-attribute actions, the brief composer's context rail, AI Quote rate columns, the home CTA); anything wider than the screen — tables, min-width workbenches, the quote grid — pans inside its own frame via the sweep ("pan, don't crush"); inputs 16px, buttons ≥36px. Tripwires: tools/check_mobile.js (Playwright, presync: 390px render of every app page — no overflow, header ≤64px, bar carries every module, every desktop-visible control visible) + tools/test_mobile.mjs (node, qa_gate/presync/validate). Docs: docs/MOBILE.md), the **Feed Chat bubble** (docs/feedchat_widget.html — the chatbot
  hovering bottom-right, see /feedchat above), the **HOURS BADGE** (docs/hours_widget.html — the
  8px retainer-state dot on any `data-hrs="<client>"` element plus its hovering popover, see
  /api/hours above; every signin, never on client decks) + the **Tachyon copilot**
  (`docs/tachyon_widget.html`, reads `window.PLANTASKS`, calls `/api/claude`).
- **Shipped panel** (`docs/shipped_widget.html`, OWNER-ONLY like the /activity board it mirrors,
  Ray 16 Sep 2026: "a pop-up module (right hand panel slide) for the activity build log … I'm
  working on multiple features across multiple modules, so sometimes I forget what has actually
  been done … then prompt the user to close the tab"): a handle on the RIGHT EDGE of every app page
  opens a slide-over listing every merged PR, read from the SAME `/api/buildlog` the Build Log tab
  uses (one source, two surfaces). Anything that shipped SINCE YOU LAST LOOKED gets its own band at
  the top NAMING THE BRANCH — the thing that identifies which of a dozen open tabs is now finished —
  with "that session is finished, you can close its tab"; it prompts, never closes or navigates.
  TWO ANTI-NOISE RULES: the FIRST EVER load is SILENT (it seeds the seen-set rather than announcing
  forty historic PRs, which would teach you to ignore the badge on day one), and OPENING THE PANEL
  IS THE ACKNOWLEDGEMENT (no separate "mark read"; a re-read never re-announces the same batch). The
  seen-set is localStorage `fcc-shipped-seen` — deliberately NOT shared state, since what one screen
  has been shown is not a team fact. A stale GitHub read says so rather than implying nothing
  shipped. Harness tools/test_shipped.mjs in qa_gate/presync/validate. Docs: docs/SHIPPED_PANEL.md
- **Secrets**: `ANTHROPIC_API_KEY` powers Tachyon (`wrangler secret put ANTHROPIC_API_KEY`); both
  the copilot and Gmail/plan live-sync degrade gracefully until their credential is set.
- **Pages = git**: `docs/FeedSpark_Command_Center.html` (`/`) and `docs/YuMOVE_Strategy_Review_Jul26.html`
  (`/deck/yumove`) are imported into the worker as Text modules (root `wrangler.toml` `rules`).
  **Add a page = add an import + one line in the worker's `PAGES` map.** Push to `main` → **GitHub Actions
  runs `wrangler deploy`** → live. No `PUT /api/template`. `wrangler.toml` lives at the **repo root** (deploy
  from root). KV edits are namespaced per page (`edits:<slug>`), so pages never collide.

### Deploy pipeline (GitHub Actions) — ALWAYS verify end-to-end
Deploys run via **GitHub Actions `wrangler deploy`** on every push to `main` (`.github/workflows/deploy.yml`);
a green run means the new version is live (synchronous edge publish — no CF git-integration build/promote
stall, no "nudge" commits). PRs are gated by `validate.yml` (dry-run build + inline-script check). Full
root-cause history, protocol + runbook: [`docs/DEPLOY_PROTOCOL.md`](./docs/DEPLOY_PROTOCOL.md).

> ⚠️ **A feature is NOT done until it is confirmed LIVE on the FCC — always watch the deploy end-to-end.**
> Never report a change as shipped on the strength of a merge alone. After merging to `main`:
> 1. Confirm the **Deploy** Action run went **green** (GitHub Actions tab / `actions_list`).
> 2. Confirm the worker actually re-published — its Cloudflare `modified_on` advanced (Cloudflare MCP
>    `workers_list`) or the Deploy run succeeded.
> 3. Verify the live build at **`/api/version`** — the returned `sha` matches the merge commit.
> 4. Sanity-check the actual page/feature is present.
>
> If a deploy fails, read the Action log, fix forward, and re-verify — don't leave a feature half-published.
> (Access-gated endpoints can't be curled from CI, so `modified_on` + a green run are the machine-checkable
> signals; `/api/version` is the human check once logged in.)

### Multi-session development (4–5 parallel Claude Code sessions)
Trunk-based: each session = its **own short-lived branch** off latest `main` (`claude/<module>-<slug>`),
small module-prefixed PRs (`[Workflow] …`), never a shared branch. Default one session per module
(Workflow / Command Center / Deck Gen / Worker / other pages) as a **guideline**; crossing is fine if you
check open PRs + `claude/*` branches first and **sequence** same-file edits. A `SessionStart` hook
(`.claude/settings.json`) auto-fetches main + reports in-flight `claude/*` branches + overlap at
session start. Before every PR: **`/presync`** (pre-approved skill) or
**`bash tools/presync.sh`** (merges latest main + re-validates); unattended builds gate on
**`bash tools/qa_gate.sh`** (validation-only, exit 0 = shippable — use as the `/goal` stop
condition). Every `create_pull_request` triggers a hook nudging `subscribe_pr_activity` (PR
babysitting by default). Overlap safeguards, both inside presync:
the **overwrite tripwire** (`docs/feature_manifest.json` checked by `tools/check_markers.js` — when you
ship a feature into a shared file, add its marker in the same PR) and the **overlap detector**
(`tools/overlap.sh` — also run it at task START; 🔥 hot-file overlap = sequence, don't parallel-edit).
**Phone tripwire (Ray's standing rule, Sep 2026): every app page mirrors its desktop feature set at 390px** — `tools/check_mobile.js` in presync fails the build on sideways overflow, a header over 64px, a lost module bar or any control visible on desktop but hidden on the phone; the shared layer is `docs/mobile_widget.html` (see docs/MOBILE.md) — fix a page there or in its own CSS, never by hiding the control.
**Nav-parity tripwire (Ray's standing rule): the module menu stays IDENTICAL on every app page** —
`tools/check_nav.js` (in presync + qa_gate + validate.yml) fails the build if any page's `.tb-modules`
nav drifts from `docs/FeedSpark_Workflow.html` (the canonical). Adding a module = add its link to
EVERY nav-bearing page in the same PR, `.on` only on the page's own link.
If presync's merge touched a file you're editing, re-run your QA — a clean git merge is not an intact
feature. **Merge autonomy (Ray's standing rule): NEVER wait for Ray to merge.** Once presync +
`validate.yml` are green, the session opens **and merges** its own PR (squash) immediately — human
approval is not a gate; the only reason to hold a merge is 🔥 overlap sequencing with another session.
After merge: verify LIVE per the rule above, then restart the branch from latest main.
Full protocol: [`docs/WAYS_OF_WORKING.md`](./docs/WAYS_OF_WORKING.md).

### Brand dossier — portfolio band (Aug 2026)
Each dossier card opens with an async **portfolio band** (`dz-port`, filled by `portFill`):
**Activity** (PLANTASKS record — total/open/done-30d/overdue + briefs in flight from `/api/briefs`
+ kw moments from `/api/kwcal`), **Health** (composite 0–100, transparent named deductions:
overdue, guard alerts via `/api/labels/alerts?by=client` — worker splits `labelalerts`/`ptypealerts`
keys per client — audit avg, keyword saturation, plan score), **Feed audit** (per wired market:
Feed Lab score via `/api/feed/audit` + % keyworded, honest "not scanned"), and **Suggested next
moves** — ranked P1–P3 deterministic rules deep-linking /labels /ptypes /workflow /feedlab /kwcal.
All computed, £0, cached per session; existing dossier blocks (score panel, tests, materials,
attach flows) unchanged. **Golden Record card — two labelled scores per market** (Ray, 17 Sep
2026: "there should be two scores appearing for each market, each brand... carefully label them
so we don't mistake. Feed scorecard and content quality score"): the compact card's ring +
legend still lead with the brand-wide averages, and below them a per-market row list (capped at
8, matching Feed audit's own cap — "+N more" names what's cut rather than hiding it) shows each
scanned market's feed scorecard AND content quality score side by side as `feed`/`content`
pills, a market never scanned reads "not scanned". Same numbers as the Estate scorecard's own
`.est-scores` pairing on `/golden` and the "Full snapshot" popup, so the brand reads the same
figure everywhere. Rendered only for brands with more than one market — a single-market brand's
ring above already IS that market. Harness: `tools/test_dossiertiles.mjs`.

### Command center data — the project plans (ATRT tracker DISCONNECTED, 22 Sep 2026)
- Ray: "Can you disconnect the ATRT checker entirely from FCC? … I just use Workflow to monitor the
  tasks across all clients from their own project plan. So the number that you showed there should
  also reflect it from Workflow, not ATRT." The tracker export (`docs/atrt_data.json`), its splice
  tool (`tools/sync_atrt.py`), the `window.ATRT` global and every spliced region are GONE.
- The hero strip on `/` (Active tasks · Overdue · Due next 7 days · Tests running · Accounts) is
  computed in the page by `renderHero()` from `PT` — `window.PLANTASKS` baked at build, replaced
  brand by brand as `/api/plan/live` lands — under **Workflow's own rules**: `wfDate` is Workflow's
  `parseUKDate` ported verbatim (`tools/test_hero.mjs` asserts the bodies identical), overdue = dated
  before today and not Done, the team's shared overlays (`/api/state` taskstatus / taskdue / deleted /
  hidden) applied first. Overdue and due-soon need the live due column, so they read "—" until the
  first plan syncs; the synced line says "live HH:MM · N of M plans live". The dossier list, health
  model, Monday catch-up and the Project plans modal read the same `PT` (`p.open`, `p.over`).
- Harness `tools/test_hero.mjs` in qa_gate / presync / validate.

---

## Key technical concepts

- **Conversational attributes** (Google, 2026): question_and_answer, document_link, related_product, item_group_title, variant_option, popularity_rank — submitted via supplemental data source
- **AI-Ready Feeds**: FeedSpark framework for preparing product data for agentic commerce (ChatGPT Shopping, Perplexity, Google AI Mode)
- **Agentic commerce protocols**: UCP (Google), MCP (Anthropic/Shopify), ACP (OpenAI/ChatGPT)
- **Golden Record**: 99.9% attribute completeness target across all feed dimensions
- **MASK structuring**: Title format — Brand + Material + Fit + Colour + Use-case (80-120 chars)
- **GPC**: Google Product Category — fixed taxonomy, not generated by AI
